{
  "name": "gdd-opencode-plugin",
  "version": "0.1.0",
  "description": "gdd worker OpenCode plugin: auto-claim (git lock) + Progress Log appender + done auto-sync",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
