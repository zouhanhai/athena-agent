{
  "name": "athena-opencode-plugin",
  "version": "0.1.0",
  "description": "athena worker OpenCode plugin: auto-claim (git lock + kanban index) + Progress Log appender",
  "private": true,
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
