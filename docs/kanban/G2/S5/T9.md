---
id: t9
title: "G2.S5.T9: Fix docling picture descriptions (OPENROUTER_API_KEY base64 command string)"
layer: T
parent: G2.S5
owner: eng-director
status: done
assignee: opencode
session_id: ses_g2s5_t9
started_at: 2026-08-06
blocked_by: []
acceptance_criteria:
  - "PDF/doc images produce text descriptions in parsed Markdown (not `<!-- image -->` placeholder)"
  - "parse_doc.py gets a valid (decrypted) OpenRouter API key"
  - "Verify: re-parse a PDF with images → Markdown contains image descriptions"
  - "Both LightRAG + llm_wiki receive the image text"
---

# G2.S5.T9: Fix docling Picture Descriptions (OpenRouter key issue)

## Problem

T6 enabled picture descriptions in `parse_doc.py`, but PDF images still appear as
`<!-- image -->` placeholders with no description. Root cause:

The athena server process env has `OPENROUTER_API_KEY` as an **unexecuted base64 command string**:
```
OPENROUTER_API_KEY=$(echo c2stb3It... | base64 -d)
```
When `parse_doc.py` reads `os.environ["OPENROUTER_API_KEY"]`, it gets this literal
`$(echo ... | base64 -d)` string (NOT the decrypted key). OpenRouter auth fails →
`do_picture_description` silently degrades → images stay as placeholders.

## Implementation

1. Ensure athena server is started with a **decrypted (plaintext)** `OPENROUTER_API_KEY`
   in its process env (the base64 `$(echo ... | base64 -d)` form must be resolved by the
   starting shell, not passed literally).
   - Fix the launch command / wrapper so `$(echo ... | base64 -d)` is evaluated at shell
     level before the node process starts.
2. OR make `parse_doc.py` self-sufficient: detect the `$(echo ... | base64 -d)` form and
   decrypt it before using the key.
3. Verify: re-parse a PDF with images → Markdown has descriptions (e.g. "Figure: ...").

## Reference
- `docs/kanban/G2/S5/T6.md` (original picture-description ticket)
- `server/scripts/parse_doc.py` (build_pipeline_options, reads OPENROUTER_API_KEY)
- athena server launch: `src/index.ts`, env source `~/.bashrc`

## Log
[2026-08-06] opencode completed picture-description fix:
  - Root cause confirmed: server process env had
    `OPENROUTER_API_KEY="$(echo <b64> | base64 -d)"` as a literal unexecuted
    command string; parse_doc.py sent that as the Bearer token → OpenRouter 401
    → `do_picture_description` silently off → `<!-- image -->` placeholders.
  - Fix (both options): `parse_doc.resolve_openrouter_key()` detects the
    `$(echo <b64> | base64 -d)` form and base64-decodes it before use (robust
    against any launch method); the athena server was also relaunched with a
    plaintext `sk-or-v1-...` key so the process env is correct.
  - Live verify: parse_doc.py run with the literal command env logs
    "OPENROUTER_API_KEY was an unexecuted base64 command; decrypted it" and
    `luesen_test.pdf` (5 images) now produces descriptions (Munich region map
    with CALEO Office / ZOB München / GAMBINO HOTEL; shopping centre; gambino
    hotel CINCINNATI) instead of bare `<!-- image -->` placeholders. The
    enriched Markdown flows through the dual pipeline to both LightRAG + llm_wiki.
  - Tests: 6 new Python unit tests (`npm run test:docling`, 10 total pass);
    server TS suite 114 pass, typecheck clean.
