---
id: t5
title: "G2.S5.T5: Wiki auto-hierarchy — use llm_wiki agent classification for wiki tree"
layer: T
parent: G2.S5
owner: eng-director
status: approved
assignee: opencode
session_id: ses_g2s5_t5
started_at: 2026-08-06
blocked_by: []
acceptance_criteria:
  - "Ingested docs are classified into llm_wiki category dirs (entities/concepts/sources/comparisons/synthesis/queries)"
  - "wiki/index.md lists pages grouped by type"
  - "Wiki tree in frontend shows hierarchy (not flat root)"
  - "Uses llm_wiki's LLM agent classification (wiki.write_page / agent runtime), not direct root-write"
---

# G2.S5.T5: Wiki Auto-Hierarchy

## Problem

Current ingestion writes Markdown directly to `<project>/wiki/` root, so all wiki pages
appear flat (no tree). llm_wiki has built-in auto-organization: its LLM agent classifies
content into category dirs and builds `wiki/index.md`, but our ingest bypasses this.

## llm_wiki auto-structure (from project.rs)

```
wiki/entities/       Named things (models, companies, people, datasets)
wiki/concepts/       Ideas, techniques, phenomena
wiki/sources/        Papers, articles, talks, blog posts
wiki/queries/        Open questions under investigation
wiki/comparisons/    Side-by-side analysis of related entities
wiki/synthesis/      Cross-cutting summaries and conclusions
wiki/index.md        All pages grouped by type
wiki/log.md          Research activity log
wiki/overview.md     Project overview
```

## Implementation

1. **Trigger llm_wiki agent classification on ingest** (instead of direct root write):
   - Use llm_wiki's agent runtime / `wiki.write_page` tool (via chat agent or direct API)
   - Or: `POST /api/v1/projects/{id}/chat` to have its LLM agent classify + write pages
2. **Verify**: ingested doc lands in correct category dir + `wiki/index.md` updated
3. **Frontend**: Wiki tree now shows hierarchy (left tree with folders)

## Reference
- `docs/kanban/G2/S5/Spec.md`
- `~/llm_wiki/src-tauri/src/commands/project.rs` (category structure)
- `~/llm_wiki/src-tauri/src/agent/runtime.rs` (wiki.write_page tool)

## Log
[2026-08-06] opencode completed wiki auto-hierarchy:
  - `LlmWikiClient.classify` (POST /projects/{id}/chat) asks the llm_wiki agent to
    classify a page into one of entity/concept/source/query/comparison/synthesis,
    with `parseClassification` validating the agent's JSON reply.
  - `KnowledgeIngestService.ingestLlmWiki` now classifies each page, writes it under
    `wiki/<category>/` with llm_wiki frontmatter (type/title/created/updated), and
    rebuilds `wiki/index.md` grouped by frontmatter type (best-effort, never fails ingest).
  - Deterministic local-keyword fallback (`localClassify`) when the agent is unavailable.
  - 8 tests added (ingest.test.ts + llmwiki.test.ts); full suite 105 pass, typecheck clean.
