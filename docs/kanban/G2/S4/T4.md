---
id: t4
title: "G2.S4.T4: Knowledge search + panel tests"
layer: T
parent: G2.S4
owner: eng-director
status: done
assignee: opencode
session_id: ses_g2s4_search
started_at: 2026-08-06
completed_at: 2026-08-06
blocked_by: ["t2", "t3"]
acceptance_criteria:
  - "Search box in Knowledge/Wiki panels calls /api/kb/search and displays results"
  - "vitest tests for kb API layer + panel components"
  - "All web tests pass (existing 53 + new)"
  - "CALEO theme consistency"
---

# G2.S4.T4: Knowledge Search + Tests

## Task

Add knowledge search to the panels and comprehensive tests.

## Implementation

### 1. Knowledge Search
- Search box (in Knowledge or Wiki panel): input → `kb.searchKnowledge(query)` → results list
- Display results: relevant snippet + source (LightRAG vs llm_wiki)
- Click result → navigate to relevant view (wiki page or graph context)

### 2. Tests (vitest)
- `api/kb.ts` unit tests (mocked fetch)
- KnowledgeView component test (graph renders from mock data)
- WikiView component test (tree renders + content loads)
- Search test (results display)

### 3. Verification
- All web tests pass (existing 53 + new)
- Manual check in browser (Vite dev :5173)

## Reference
- `docs/knowledge-rag-design.md`
- `docs/kanban/G2/S4/Spec.md`
- Existing tests: `web/src/__tests__/`

## Log

- **2026-08-06 (ses_g2s4_search)**: Added knowledge search to the Knowledge panel.
  - `web/src/views/KnowledgeView.vue`: search box in the panel header (`t-input` + Search button, Enter/clearable) → `kb.searchKnowledge(query)`; results list replaces the graph body while active, each hit shows title + source badge (RAG/Wiki) + snippet + path.
  - Clicking a wiki result navigates to `/wiki?path=...` (deep link); RAG summary is shown inline. "Back to graph" clears the search view.
  - Tests: `knowledge-view.spec.ts` +5 search tests (results render with badges, wiki navigation, empty, error, back-to-graph); mounted with a memory router for `useRouter`.
  - Verification: web typecheck clean; web tests 80/80 pass.
