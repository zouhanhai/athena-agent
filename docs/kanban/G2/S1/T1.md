---
id: t1
title: "G2.S1.T1: Install and configure LightRAG"
layer: T
parent: G2.S1
owner: eng-director
status: approved
assignee: opencode
started_at: 2026-08-05
blocked_by: []
acceptance_criteria:
  - "LightRAG installed via pip on 6900XT"
  - "Configured DeepSeek as LLM (embedding + generation)"
  - "Configured Postgres + pgvector as vector storage"
  - "NetworkX as graph storage (POC)"
  - "Config file/dir written and loadable"
---

# G2.S1.T1: Install and configure LightRAG

## Task

Install LightRAG on 6900XT and configure DeepSeek + Postgres/pgvector + NetworkX.

## Implementation

1. **Install**: `pip install lightrag` (core) + `pip install lightrag[api]` (server with UI)
   - Use a dedicated Python venv (e.g. `~/lightrag-venv`) to avoid polluting system python
2. **Create config** for LightRAG:
   - `LLM`: DeepSeek (`deepseek-v4-flash` or `deepseek-chat`) as both generation + embedding
   - DeepSeek API key from `~/.pi/agent/auth.json`
   - `vector_storage`: pgvector (Postgres already running on 6900XT)
   - `kv_storage` + `doc_status`: Postgres
   - `graph_storage`: NetworkX (POC, file storage)
3. **Verify**: config dir/file exists and LightRAG imports without error

## Reference

- Spec: `docs/kanban/G2/S1/Spec.md`
- Goal: `docs/kanban/G2/Goal.md`
- Design: `docs/knowledge-rag-design.md`
- 6900XT: SSH as `hh`

## How to Locate Reference Docs

- `parent: G2.S1` → `docs/kanban/G2/S1/Spec.md`
- `parent: G2` → `docs/kanban/G2/Goal.md`

## Notes

- Use a dedicated venv (`~/lightrag-venv`) to keep system Python clean
- DeepSeek handles both embedding + generation
- Postgres already running (weknora/qm database, add a `lightrag` db)
- Use **implement** + tdd (verification) + code-review

## Dependencies

- None (first ticket of G2.S1)

## Log

### 2026-08-05 (opencode) — done
- Installed LightRAG 1.5.5 (`lightrag-hku[api]`) in dedicated venv `~/lightrag-venv` (system python had no pip/ensurepip; bootstrapped via `--without-pip` + get-pip.py).
- Created Postgres `lightrag` db with `vector` extension (pgvector 0.6.0); set `hh` role password for TCP (asyncpg).
- Config at `/home/hh/lightrag/.env` (+ workdir `rag_storage`, input dir `inputs`):
  - LLM: openai binding → OpenRouter base (`https://openrouter.ai/api/v1`), model `deepseek/deepseek-v4-flash`, key from `~/.bashrc` OPENROUTER_API_KEY.
  - Embedding: openai binding → OpenRouter, `qwen/qwen3-embedding-8b`, `EMBEDDING_DIM=1024` (matryoshka; full 4096 dims exceed pgvector HNSW 2000-dim cap).
  - `LIGHTRAG_KV_STORAGE=PGKVStorage`, `LIGHTRAG_VECTOR_STORAGE=PGVectorStorage`, `LIGHTRAG_GRAPH_STORAGE=NetworkXStorage`, `LIGHTRAG_DOC_STATUS_STORAGE=PGDocStatusStorage`.
- Server booted cleanly on 0.0.0.0:9621, created PG tables + HNSW indexes (entity/relation/chunk), `/health` reports healthy with correct config.
- Verification script: `/home/hh/lightrag/scripts/verify_t1.sh` (all checks pass).
