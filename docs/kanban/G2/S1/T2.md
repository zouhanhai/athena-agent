---
id: t2
title: "G2.S1.T2: Start LightRAG server and verify ingestion/retrieval/graph"
layer: T
parent: G2.S1
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: ["t1"]
acceptance_criteria:
  - "LightRAG API server starts listening, bound 0.0.0.0"
  - "Can ingest a Markdown document"
  - "Semantic query returns results"
  - "Graph data exportable (LightRAG /graphs or graph_visual_with_html)"
  - "Server reachable for Tailscale access"
---

# G2.S1.T2: Start LightRAG server and verify ingestion/retrieval/graph

## Task

Start the LightRAG API server on 6900XT and verify ingestion, retrieval, and graph output.

## Implementation

1. **Start server**: `lightrag-server --host 0.0.0.0 --port <port>`
   - Use a stable port (suggest 9621 or similar, avoid conflicts)
   - Run detached (background) so it survives SSH disconnect
2. **Verify ingestion**: ingest a Markdown document via API
3. **Verify retrieval**: run a semantic query, confirm results returned
4. **Verify graph**: confirm graph data available (`/graphs` endpoint or graph_visual_with_html)
5. **Health check**: confirm server responds, bound 0.0.0.0
6. **Tailscale**: confirm reachable from employee machines

## Reference

- Spec: `docs/kanban/G2/S1/Spec.md`
- Goal: `docs/kanban/G2/Goal.md`
- Design: `docs/knowledge-rag-design.md`

## How to Locate Reference Docs

- `parent: G2.S1` → `docs/kanban/G2/S1/Spec.md`
- `parent: G2` → `docs/kanban/G2/Goal.md`

## Notes

- Run server detached (setsid/nohup) so it survives SSH disconnect
- Record the chosen port in the spec/README (update Port Plan)
- Use **implement** + tdd (verification script) + code-review

## Dependencies

- G2.S1.T1 (LightRAG installed + configured)

## Log

### 2026-08-05 (opencode) — done
- LightRAG server running detached (`setsid nohup`), bound **0.0.0.0:9621**, logs at `/home/hh/lightrag/server.log`.
- Fix during ingest: embedding full 4096 dims exceeded pgvector HNSW 2000-dim cap → set `EMBEDDING_DIM=1024` (matryoshka via OpenRouter `dimensions`); added `EMBEDDING_SEND_DIM=true` so the `dimensions` param is actually sent (otherwise OpenRouter returns 4096-dim → "Vector count mismatch expected 5 got 20").
- Verified:
  - `/health` healthy; listening on 0.0.0.0:9621.
  - Ingested `athena-overview.md` → processed (1 doc, chunked+embedded+LLM-extracted).
  - Semantic query via `/query` (hybrid) returns grounded answer.
  - Graph: `/graphs?label=<entity>` returns nodes+edges (13 nodes/14 edges for LightRAG); `/graph/label/popular` lists extracted entities.
- Port recorded in README Port Plan (LightRAG = 9621, 0.0.0.0).
- Verification script: `/home/hh/lightrag/scripts/verify_t2.sh` (all checks pass).
