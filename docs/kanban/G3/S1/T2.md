---
id: t2
title: "G3.S1.T2: Local Athena default declaration (owl logo)"
layer: T
parent: G3.S1
owner: eng-director
status: done
assignee: opencode
session_id: ses_63e24c068cab46ac
started_at: "2026-08-08"
blocked_by: []
acceptance_criteria:
  - "Athena default agent seeded"
  - "owl logo assigned"
  - "capabilities reflect knowledge assistant"
  - "queryable"
---

# G3.S1.T2: Local Athena default declaration (owl logo)

## Task

Seed default Athena agent: knowledge assistant (llm_wiki + LightRAG, file upload, knowledge-graph Q&A), owl logo.

## Implementation

### 1. Seed default Athena agent
- Seed on server start: alias=Athena, owner=system, capabilities={system:athena,mcp:[lightrag,llm_wiki],tools:[file_upload,knowledge_graph_qa]}
- **owl logo asset: `web/public/athena-logo-ai.png`** (existing file, served from web/public)
- logo_url → `/athena-logo-ai.png`
- verify queryable via GET /api/agents/Athena

## Reference
- `docs/kanban/G3/S1/Spec.md`
- `docs/g3-requirements.md`

## Recommended Skills (OpenCode)

- **implement**: seed the default Athena agent; use `codegraph_explore` to see how the server seeds/initializes on start (e.g. `server/src/index.ts`)
- **tdd**: test that Athena default agent is queryable after server start
- **code-review**: review against this Spec after implementing

## Reference
- `docs/kanban/G3/S1/Spec.md`
- `docs/g3-requirements.md`
- Athena owl logo: `web/public/athena-logo-ai.png`

## Log

### done (opencode, ses_63e24c068cab46ac, 2026-08-08)

Implemented + verified the Local Athena default declaration (TDD).

- `server/src/agents/registry.ts` — added `seed(): Promise<void>` to `AgentRegistry` (idempotent):
  - `MemoryAgentRegistry.seed()` inserts `DEFAULT_ATHENA` if absent
  - `PostgresAgentRegistry.seed()` eagerly ensures table + inserts Athena (`ON CONFLICT DO NOTHING`)
- `server/src/app.ts` — `buildApp` registers an `onReady` hook that seeds the default registry
  on server start (skipped when a custom registry is injected, so route tests keep exact counts)
- `server/test/athena-seed.test.ts` — 4 new tests (RED→GREEN): DEFAULT_ATHENA shape (owl logo
  `/athena-logo-ai.png`, knowledge-assistant capabilities), idempotent memory seed,
  queryable via `GET /api/agents/Athena` after server start, owl logo asset exists
- Verify: `npm test` 178 pass / 0 fail (incl. 4 new); `npm run typecheck` clean
- Code-review: all T2 acceptance criteria met (seeded, owl logo, knowledge-assistant
  capabilities, queryable); no blocking standards findings
