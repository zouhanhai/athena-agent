---
id: t1
title: "G3.S1.T1: Agent registry data model + CRUD API"
layer: T
parent: G3.S1
owner: eng-director
status: done
assignee: opencode
session_id: ses_24471c16eb231a8f
started_at: "2026-08-08"
blocked_by: []
acceptance_criteria:
  - "agents table in PG with all fields"
  - "CRUD API endpoints working"
  - "capabilities shape {system,mcp[],tools[],skills[],specialty,description} (Vercel-inspired, §4.3)"
  - "route tests green"
---

# G3.S1.T1: Agent registry data model + CRUD API

## Task

Create Postgres agents table + CRUD API.

## Implementation

- agents table: id/alias/owner_employee_id/logo_url/capabilities(jsonb)/runtime/timestamps
- routes/agents.ts: POST /api/agents, PUT /api/agents/:alias, GET /api/agents, GET /api/agents/:alias
- tdd tests for CRUD + validation

## Recommended Skills (OpenCode)

- **implement**: implement per this ticket; use `codegraph_explore` to understand existing route patterns (e.g. `server/src/routes/kb.ts`, `server/src/routes/chat.ts`) before writing the new `agents.ts`
- **tdd**: write tests at the public seam (the routes) first, then the implementation
- **code-review**: review against this Spec after implementing

## Reference
- `docs/kanban/G3/S1/Spec.md`
- `docs/g3-requirements.md`
- Existing route patterns: `server/src/routes/kb.ts`, `server/src/routes/chat.ts`

## Log

### done (opencode, ses_24471c16eb231a8f, 2026-08-08)

Implemented agent registry data model + CRUD API (TDD, 13 new route tests).

- `server/src/agents/registry.ts` — data model + `AgentRegistry` interface:
  - `AgentCapabilities` shape `{ system, mcp: string[], tools: string[], description? }`
  - `AgentRecord` = id/alias/owner_employee_id/logo_url/capabilities/runtime/created_at/updated_at
  - `MemoryAgentRegistry` (tests + dev fallback), `PostgresAgentRegistry` (pg + pgvector-free jsonb
    `agents` table, lazy `CREATE TABLE IF NOT EXISTS` + `ON CONFLICT` upsert; maps unique violation → 409)
  - `DEFAULT_ATHENA` seed: alias=Athena, owner=system, logo=`/athena-logo-ai.png`, runtime=server,
    capabilities={system:athena, mcp:[lightrag,llm_wiki], tools:[file_upload,knowledge_graph_qa]}
- `server/src/routes/agents.ts` — `registerAgentRoutes`:
  - `POST /api/agents` → 201 (409 on duplicate alias), `PUT /api/agents/:alias` → 200 (404 unknown),
    `GET /api/agents?ownerEmployeeId=` → list, `GET /api/agents/:alias` → single (404)
  - Validation: alias/owner_employee_id required, capabilities shape enforced, logo_url/runtime type checks
- `server/src/app.ts` — `registry` option on `buildApp`, `defaultAgentRegistry()` picks
  Postgres when `DATABASE_URL` set else seeded Memory; onClose closes registry
- `server/test/agents-routes.test.ts` — 13 tests: CRUD happy paths, 400 validation, 409 conflict,
  404 lookups, ownerEmployeeId filter, PUT logo/capabilities update
- deps: added `pg` + `@types/pg` (server/package.json)
- Verify: `npm test` 174 pass / 0 fail (incl. 13 new); `npm run typecheck` clean
- Code-review: all Spec §1/§2/§4 acceptance criteria met; no blocking findings
