---
id: t1
title: "G3.S6.T1: G.S.T board structure + md helpers"
layer: T
parent: G3.S6
owner: eng-director
status: approved
assignee: opencode
session_id: ses_44ab85fcdceaf106
started_at: 2026-08-08
blocked_by: []
acceptance_criteria:
  - "schema defined"
  - "md read/write helpers work"
---

# G3.S6.T1: G.S.T board structure + md helpers

## Task

Standardize G.S.T md structure + helpers to read/write board md files.

## Implementation

- schema for Goal/Spec/Ticket md (frontmatter)
- helpers to parse/write

## Reference
- `docs/kanban/G3/S6/Spec.md`
- `docs/g3-requirements.md`

## Log
[2026-08-08] opencode completed G.S.T board structure + md helpers (TDD, 50 new tests):
  - **`server/src/kanban/schema.ts`** — three-layer md protocol: `LAYERS` (G/S/T),
    `TICKET_STATUSES` (backlog→in_progress→done→in_review→approved, or →rejected),
    typed `GoalFrontmatter` / `SpecFrontmatter` / `TicketFrontmatter` (parent, owner,
    assignee, session_id, started_at/completed_at, blocked_by, acceptance_criteria,
    pr, branch, plus reject-flow parent_id/qa_feedback/reopen_reason), validated by
    `parseGoal`/`parseSpec`/`parseTicket`/`parseBoardFrontmatter` (throw `BoardSchemaError`).
  - **`server/src/kanban/frontmatter.ts`** — minimal YAML-subset parser/renderer (no YAML
    dep): scalars, quoted strings, inline arrays (`blocked_by: ["t3"]`), block lists
    (`acceptance_criteria:`/`- "..."`), numeric coercion; `parseBoardMd`/`renderBoardMd`
    split/join frontmatter from body, round-trip stable.
  - **`server/src/kanban/board.ts`** — ref→path mapping (`G3`→Goal.md, `G3.S6`→Spec.md,
    `G3.S6.T1`→T1.md, explicit board root), `parseBoardFile`, `readBoardFile`,
    `writeBoardFile`/`writeTicketFile` (mkdir -p + write).
  - **`server/src/kanban/index.ts`** — public re-exports.
  - Tests: `server/test/kanban-frontmatter.test.ts` (15), `kanban-schema.test.ts` (18),
    `kanban-board.test.ts` (17). Full suite 367 pass / 0 fail; `typecheck` + `build` clean.
  - Live verified against the real board: G3 / G3.S6 / G3.S6.T1 / G1.S2.T5 / G2 all parse
    (layers, statuses, sessions, inline + block arrays).
  - Acceptance criteria met: schema defined, md read/write helpers work.
