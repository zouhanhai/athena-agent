---
id: t6
title: "G3.S6.T6: Kanban docs-scan backend API"
layer: T
parent: G3.S6
owner: eng-director
status: done
assignee: opencode
session_id: ses_6b8e4f2a1c9d7035
started_at: 2026-08-08
blocked_by: []
acceptance_criteria:
  - "board constructed from md"
  - "GET /api/kanban works"
---

# G3.S6.T6: Kanban docs-scan backend API

## Task

Scan repo docs/kanban/*.md → construct board; GET /api/kanban.

## Implementation

- parse md files → board (Goals/Specs/Tickets+status)
- GET /api/kanban consumed by S5

## Reference
- `docs/kanban/G3/S6/Spec.md`
- `docs/g3-requirements.md`

## Log
[2026-08-08] opencode completed Kanban docs-scan backend API (TDD, 11 new tests → 429 pass / 0 fail):
  - **`server/src/kanban/scan.ts`** — walks `docs/kanban/*.md` and constructs the board:
    `scanBoard(root)` returns `{ goals: [{ ref, goal, specs: [{ ref, spec, tickets: [{ ref,
    ticket }] }] }], errors }` with typed frontmatter + status per layer. Natural numeric
    ordering (G1/G2/G10, T2/T10); ignores non-board files/dirs; a G dir without `Goal.md`
    is skipped; files that fail to parse are collected in `errors` (the rest of the board
    still loads). `defaultBoardRoot()` resolves the repo's `docs/kanban` from
    `import.meta.url`; `FileBoardScanner` + `BoardScanner` seam (fakable in route tests).
  - **`server/src/routes/kanban.ts`** — `GET /api/kanban` (Bearer auth, consistent with the
    Workbench GitHub APIs) → returns the scanned board; scanner failures → 500.
  - **`server/src/app.ts`** — `BuildAppOptions.board` (default `FileBoardScanner(defaultBoardRoot())`),
    wired via `registerKanbanRoutes`.
  - Tests: `kanban-scan.test.ts` (7: empty board, construction, numeric sort, ignore
    non-board files, parse errors kept, G-dir without Goal.md skipped, live repo board),
    `kanban-routes.test.ts` (4: auth required, board returned, scanner error → 500, default
    scanner reads real repo). Full suite 429 pass / 0 fail; `typecheck` + `build` clean.
  - Live verified: 3 goals / 0 errors; G3.S6 tickets T1/T5 done, T6 in_progress.
  - Code-review (2 axes): Standards — minor only (repeated read/parse blocks per layer,
    path-clump in scanner); Spec — no findings.
  - Acceptance criteria met: board constructed from md, GET /api/kanban works (consumed by S4 Workbench Kanban tab).
