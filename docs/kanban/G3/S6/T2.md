---
id: t2
title: "G3.S6.T2: Planning-agent onboarding (grill→to-spec→to-ticket)"
layer: T
parent: G3.S6
owner: eng-director
status: done
assignee: opencode
session_id: ses_6c03b6229d8b3d4c
started_at: 2026-08-08
blocked_by: []
acceptance_criteria:
  - "flow produces G.S.T"
  - "writers work"
  - "validation"
---

# G3.S6.T2: Planning-agent onboarding (grill→to-spec→to-ticket)

## Task

Flow for planning agent to produce the three layers; platform provides md writers + validation.

## Implementation

- Consultant→Goal, PM→Spec, EngD→tickets
- writers + validation

## Reference
- `docs/kanban/G3/S6/Spec.md`
- `docs/g3-requirements.md`

## Log
[2026-08-08] opencode completed planning-agent onboarding (TDD, 19 new tests):
  - **`server/src/kanban/planning.ts`** — the grill→to-spec→to-ticket flow. Soul-role
    ownership baked in via `PLANNING_OWNER` (Goal→consultant, Spec→pm, Ticket→eng-director).
    - **Writers**: `writeGoal` / `writeSpec` / `writeTicket` / `writeTickets` (batch
      auto-numbers T1..Tn) build typed frontmatter + body from drafts and write via the
      G3.S6.T1 board helpers; `buildGoal`/`buildSpec`/`buildTicket` produce the documents.
    - **Validation**: `validateGoalDraft`/`validateSpecDraft`/`validateTicketDraft`
      (title + non-empty acceptance_criteria) and cross-layer `validatePlan` (refs sit
      under their parent, no duplicate refs) — throws `PlanningError` before writing.
    - **Numbering**: `nextGoalRef` / `nextSpecRef` / `nextTicketRef` (max+1, git-atomic
      G assignment per gdd/docs/design.md §3). Live-verified on real board: G4 / G3.S8 / G3.S6.T7.
    - **Flow**: `planGoal(root, input)` allocates next G (and S/T under it), validates the
      whole plan, writes Goal.md + Spec.md(s) + T1..Tn.md, returns refs + file paths.
  - **`server/src/kanban/board.ts`** — added `BoardFileBase` (path-less board doc) so
    `writeBoardFile` accepts builder output; `BoardFile` now extends it.
  - **`server/src/kanban/frontmatter.ts`** — `renderFrontmatter` omits `undefined` values
    (was rendering `milestone: "undefined"` for omitted optional fields).
  - **`server/src/kanban/index.ts`** — re-exports planning module.
  - Tests: `server/test/kanban-planning.test.ts` (19), `kanban-frontmatter.test.ts` (+1
    regression). Full suite 480 pass / 0 fail; `typecheck` + `build` clean.
  - Acceptance criteria met: flow produces G.S.T, writers work, validation.
