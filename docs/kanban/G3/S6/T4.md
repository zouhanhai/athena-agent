---
id: t4
title: "G3.S6.T4: Full 6-role lifecycle + state machine"
layer: T
parent: G3.S6
owner: eng-director
status: done
assignee: opencode
session_id: ses_8c4d1f2a90b7e3d5
started_at: 2026-08-08
completed_at: 2026-08-08
blocked_by: []
acceptance_criteria:
  - "6 roles defined"
  - "state machine transitions"
  - "reject→re-decompose works"
---

# G3.S6.T4: Full 6-role lifecycle + state machine

## Task

6-role souls (Consultant/PM/EngD/Worker/Reviewer/Writer) + state machine incl reject→re-decompose.

## Implementation

- role souls
- state machine: backlog→in_progress→done→in_review→approved / rejected→EngD re-decompose→T1.1(parent_id/qa_feedback)

## Reference
- `docs/kanban/G3/S6/Spec.md`
- `docs/g3-requirements.md`

## Log

[2026-08-08] opencode claimed G3.S6.T4 (session ses_8c4d1f2a90b7e3d5)
[2026-08-08] opencode completed Full 6-role lifecycle + state machine (TDD, 22 new tests → kanban suite 122 pass / 0 fail; full suite 520 pass, 1 pre-existing unrelated fail (logos-default-store live test); typecheck + build clean):
  - **`server/src/kanban/roles.ts`** — the 6 role souls (Consultant / PM / Eng Director / Worker / Reviewer / Writer).
    Each `RoleSoul` carries id / name / duty / stages / output; `ROLES`, `ROLE_IDS`, `roleSoul()` lookups.
    `PLANNING_OWNER` (Goal→consultant, Spec→pm, Ticket→eng-director) moved here as the single source of truth;
    `planning.ts` re-exports it (no behavior change).
  - **`server/src/kanban/state-machine.ts`** — formal transition table `STATE_MACHINE`
    (backlog→in_progress→done→in_review→approved, or ↘rejected; approved + rejected are terminal),
    `canTransition` / `transitionsFrom` / `transitionsTo` / `transitionId` / `actorFor`, and
    `TRANSITION_ACTOR` binding each transition to its soul role (claim/report→worker, approve/reject→reviewer).
    `protocol.ts` now derives its `REPORTABLE` statuses from `transitionsTo` (state machine is the single
    source of truth).
  - **`server/src/kanban/lifecycle.ts`** — the review + rework lifecycle:
    - `rejectTicket` (in_review→rejected; requires reviewer + qa_feedback; original ticket preserved, PR kept).
    - `approveTicket` (in_review→approved + completed_at; records merged PR).
    - `reDecompose` (Eng Director re-decomposes a rejected ticket → new backlog ticket under the same spec,
      linked via `parent_id`, carrying the original's `qa_feedback` + a required `reopen_reason`; the original
      stays rejected and a Log entry records the re-decompose). The new ticket is claimable by any worker.
  - **`server/src/kanban/board.ts`** — shared `readTicketFile` helper (protocol + lifecycle both load tickets
    through it, replacing protocol's private loader).
  - Tests: `kanban-roles.test.ts` (6), `kanban-state-machine.test.ts` (8), `kanban-lifecycle.test.ts` (8,
    including a full 6-role lifecycle integration: claim → done → in_review → reject → re-decompose →
    re-claim by another worker → approve).
  - Acceptance criteria met: 6 roles defined; state machine transitions; reject→re-decompose works.
