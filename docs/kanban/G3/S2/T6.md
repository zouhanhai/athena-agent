---
id: t6
title: "G3.S2.T6: Settings as standalone sidebar tab + reorder sidebar (Knowledge/Wiki/Workbench/Uploads/Output/Settings) + Output placeholder page"
layer: T
parent: G3.S2
owner: eng-director
status: done
assignee: opencode
session_id: ses_opencode_g3s2t6
started_at: "2026-08-09"
done_at: "2026-08-09"
blocked_by: []
acceptance_criteria:
  - "Settings becomes a dedicated sidebar nav item (/settings route) opening a full page, not a dialog popup"
  - "Settings page shows Profile (display name / logo / GitHub credential) + Agents management"
  - "Theme settings are NOT duplicated on the Settings page — the sidebar ThemeToggle stays the single place to switch theme"
  - "Sidebar footer no longer renders the SettingsPanel dialog trigger (only ThemeToggle remains)"
  - "New Output placeholder page at /output (simple view; full output feature is M5)"
  - "Sidebar nav order becomes: Knowledge, Wiki, Workbench, Output, Uploads, Settings"
  - "S2.T5 behavior preserved: PUT /api/me updates profile; Agents management consolidated"
---

# G3.S2.T6: Settings as standalone sidebar tab + sidebar reorder + Output placeholder

## Task

G3.S2.T5 turned Settings into a **dialog** (`SettingsPanel.vue` opens a `t-dialog` from the sidebar footer)
containing Profile + Agents + Theme. Change Settings to a **dedicated sidebar tab** (full page), remove the
dialog, reorder the sidebar to `Knowledge → Wiki → Workbench → Output → Uploads → Settings`, and add an
**Output placeholder page** (full Output generation is M5).

Content of the Settings page:
- **Profile** section (display name / animal logo / GitHub credential) — reuse the exact form/logic
  from `SettingsPanel.vue`.
- **Agents** section (`AgentManagement.vue`) — reuse as-is.
- **No Theme section** on the page — the sidebar `ThemeToggle` remains the single theme switch
  (the in-dialog Theme section is removed).

## Implementation

1. Create `web/src/views/SettingsView.vue` — a full page containing the Profile + Agents sections
   (lift the markup + logic out of `SettingsPanel.vue`'s `t-dialog` body; drop the dialog wrapper and the
   Theme section). Layout consistent with other views (`.wiki-body`-style panel on a `--caleo-surface` card).
2. Create `web/src/views/OutputView.vue` — a placeholder page ("Output — coming in M5") so the nav item
   has a real target.
3. Routes in `web/src/router/index.ts`: add `/settings` → `SettingsView.vue` and `/output` → `OutputView.vue`.
4. `web/src/components/SidebarNav.vue`: reorder `navItems` to
   `Knowledge(/knowledge) → Wiki(/wiki) → Workbench(/workbench) → Output(/output) → Uploads(/uploads) → Settings(/settings)`
   (Settings uses `SettingIcon`); **remove** `<SettingsPanel />` from the footer (keep `<ThemeToggle />`).
5. Remove/stop using `SettingsPanel.vue` (move its logic into SettingsView; keep `AgentManagement.vue` as a child).
6. Tests: sidebar shows the 6 nav items in the exact order; /settings renders Profile + Agents (no Theme
   section); /output renders the placeholder; SettingsPanel dialog trigger gone from the footer. Keep all
   existing tests green (web `npx vitest run`, `npx vue-tsc --noEmit`).

## Recommended Skills (OpenCode)
- **tdd** / **implement** / **code-review** / **codegraph_explore**

## Reference
- `web/src/components/SettingsPanel.vue` (Profile + Agents + Theme form to lift)
- `web/src/components/SidebarNav.vue` (navItems + footer)
- `web/src/router/index.ts` (routes)
- `web/src/views/` (existing view layout pattern, e.g. WikiView/UploadsView)
- Backend unchanged — `PUT /api/me` already works (S2.T5).

## Log

- 2026-08-09 (opencode, ses_opencode_g3s2t6):
  - `SettingsView.vue` (new): full page at `/settings` with a Profile section
    (display name / animal-logo picker / GitHub token-SSH credential, `PUT /api/me`
    via `updateMe`) + an Agents section (`AgentManagement.vue` child, unchanged).
    Profile markup/logic lifted verbatim from the old `SettingsPanel.vue` dialog
    body; the `t-dialog` wrapper, `attachToPanel` plumbing, and the Theme section
    were dropped — the sidebar `ThemeToggle` stays the single theme switch.
  - `OutputView.vue` (new): placeholder page at `/output` ("Output — coming in M5").
  - Router: added `/settings` → `SettingsView.vue` and `/output` → `OutputView.vue`;
    `/kanban` kept (still exercised by layout/chat tests).
  - `SidebarNav.vue`: nav order now Knowledge → Wiki → Workbench → Output → Uploads
    → Settings (Settings uses `SettingIcon`, Output uses `FileIcon`); removed
    `<SettingsPanel />` from the footer (only `<ThemeToggle />` remains).
  - Removed `SettingsPanel.vue` (logic moved to `SettingsView`) and its spec;
    replaced with `settings-view.spec.ts` + `output-view.spec.ts`. `layout.spec.ts`
    updated: 6 nav items in exact order, footer no longer renders the dialog trigger,
    /settings + /output navigation, theme switch via the footer toggle.
  - Web `npx vitest run` 285 pass; `npx vue-tsc --noEmit` clean. Backend untouched.
