---
id: t8
title: "G3.S2.T8: Profile shows current logo (selected/highlighted) + GitHub credential mask (ghp_*****) when present"
layer: T
parent: G3.S2
owner: eng-director
status: in_progress
assignee: opencode
started_at: "2026-08-09"
session_id: "ses_t8_profile_logo_github_mask"
blocked_by: []
acceptance_criteria:
  - "Settings Profile logo picker always shows the signed-in employee's CURRENT logo, highlighted/selected (even if that logo is used by another agent and excluded from available)"
  - "GitHub credential field shows a MASK with first + last few characters (e.g. ghp_abcd****wxyz) when a token exists, so the user can compare it with GitHub — never the full secret"
  - "GET /api/me returns github_has_credential + a partial masked github_credential_masked (e.g. ghp_****abcd or ghp_abcd****wxyz)"
  - "All existing tests stay green"
---

# G3.S2.T8: Show current logo (selected) + GitHub credential mask in Profile

## Task

Two UX gaps in the Settings **Profile** section:

1. **Current logo not shown when it's "in use"**. `listLogos({excludeInUse: true})` excludes any logo used
   by another agent/employee. If the signed-in user's own logo is one of those (e.g. they picked shark,
   and shark is counted in-use), it is missing from the picker — so the user can't see/keep their current
   logo. The picker must ALWAYS show the user's current `logo_url`, highlighted (`is-selected`),
   regardless of the exclude-in-use filter.
2. **GitHub credential not shown when present**. `GET /api/me` doesn't return whether a token exists, so
   the GitHub field is always blank (`placeholder="ghp_…"`) even when the user already has a token stored.
   It should show a mask (e.g. `ghp_*****` / "a token is stored") so the user knows they have one, without
   ever exposing the secret.

## Implementation

1. Backend `GET /api/me` (server/src/routes/employees.ts): return
   `{ ...employee, github_has_credential: boolean, github_credential_type?: "token", github_credential_masked?: string }`
   by calling `registry.getGithubCredential(email)`. Derive `github_credential_masked` from the decrypted
   value in the backend: keep the first 4 + last 4 chars, middle replaced with `****` (e.g. `ghp_abcd****wxyz`).
   **Never return the full token** — only the masked string.
2. Frontend `web/src/views/SettingsView.vue`:
   - logoOptions: always include the current `logoUrl` (even if absent from `logos.value`), and keep the
     existing `is-selected` highlight on it.
   - GitHub field: when `employee.github_has_credential`, show the input pre-filled with
     `github_credential_masked` (readonly/placeholder), so the user sees first+last chars to compare with
     GitHub. Clearing/retyping a real token replaces it (submit only sends a value when the user typed a
     new one).
3. `web/src/stores/auth.ts` + `web/src/api` types: carry `github_has_credential` /
   `github_credential_type` / `github_credential_masked` through the employee shape.
4. Tests: picker shows current in-use logo selected; github field masks with first+last chars when
   has_credential; /api/me returns github_has_credential + masked (never full token). Keep all green.

## Recommended Skills (OpenCode)
- **tdd** / **implement** / **code-review** / **codegraph_explore**

## Reference
- `web/src/views/SettingsView.vue` (logoOptions, GitHub field, `is-selected`)
- `server/src/routes/employees.ts` (`GET /api/me`, `currentEmployee`)
- `server/src/employees/employees.ts` (`getGithubCredential`, `GithubCredentialInfo`)
- `web/src/stores/auth.ts`, `web/src/api/invitations.ts` (employee shape)

## Log
