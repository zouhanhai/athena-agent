---
id: t1
title: "G3.S8.T1: Remove Add Data from Knowledge + topic/localized graph views"
layer: T
parent: G3.S8
owner: eng-director
status: done
assignee: opencode
session_id: ses_7ecea0eebde0fee1
started_at: ""
blocked_by: []
acceptance_criteria:
  - "Add Data panel removed from Knowledge page (ingestion lives on Uploads)"
  - "graph 'all' view replaced: topic view or search-node local neighborhood"
  - "never auto-loads 1000+ nodes"
---

# G3.S8.T1: Remove Add Data from Knowledge + topic/localized graph views

## Task

Clean up the Knowledge Graph page: drop the duplicated Add Data panel and make the graph view usable when there are 1000+ nodes (topic-only or search-node local subgraph).

## Implementation

In `web/src/views/KnowledgeView.vue`:

1. **Remove Add Data**: delete the `showAddData` toggle button, the `.add-data-panel` block (drop-zone, URL row, task list), and related unused script state (`showAddData`, `fileInput`, `dragging`, `urlInput`, `useIngestTasks`, ingest helpers, `friendlyError`, etc.). Ingestion stays on the Uploads page.
2. **Default view**: do not load all nodes on mount. Default to a topic filter or a compact/empty state.
3. **Search-node local graph**: add the ability to search for a node and show its local neighborhood (node + 1-2 hop relations via `nodeRelations`), instead of the whole 1000+ node graph. Reuse `searchKnowledge` or a node lookup.

Follow TDD. Use codegraph_explore. When done: update ticket status=done + Log, git add/commit/push. Report completion.

## Recommended Skills (OpenCode)
- **tdd** / **implement** / **code-review** / **diagnosing-bugs**

## Reference
- `docs/kanban/G3/S8/Spec.md`
- `web/src/views/KnowledgeView.vue`
- `web/src/api/kb.ts`, `web/src/kb/graph.ts`

## Log

- 2026-08-08 (opencode, ses_7ecea0eebde0fee1): implemented via TDD.
  - Removed Add Data panel + header "Add Data" toggle from `web/src/views/KnowledgeView.vue`; deleted ingestion script state/helpers (`showAddData`, `fileInput`, `dragging`, `urlInput`, `useIngestTasks`, `friendlyError`, task stage helpers) and the whole `.add-data-panel` template/CSS block. Ingestion remains on the Uploads page. Deleted obsolete `web/src/__tests__/knowledge-add-data.spec.ts`.
  - No longer auto-loads the full graph on mount: `onMounted` only loads topics; the graph renders an "Explore the knowledge graph" prompt until a topic is chosen or a node is searched. Topic selection loads only that topic's subgraph via `getGraph(undefined, topic)`.
  - Added search-node local graph: searching a node calls `getGraph(label)` and shows only that node + its 1-2 hop relations via the new `localSubgraph()` helper in `web/src/kb/graph.ts` (keeps only edges between included nodes). Clicking a node in the graph also focuses the view to that node's local neighborhood.
  - Updated `web/src/__tests__/knowledge-view.spec.ts` with red→green tests: no auto-load, topic-scoped fetch, node search local graph, no-node-found error, clear/back behavior, node-click focus, plus `localSubgraph` unit tests.
  - Verified: `vitest` knowledge-view 19/19 green; `vue-tsc --noEmit` clean; unrelated pre-existing failures in chat/layout/uploads/workbench specs unchanged.
