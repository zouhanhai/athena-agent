---
id: t2
title: "G3.S5.T2: Per-system processing stages model"
layer: T
parent: G3.S5
owner: eng-director
status: approved
assignee: opencode
started_at: 2026-08-08
session_id: ses_d04b42333b897b68
blocked_by: []
acceptance_criteria:
  - "per-system sub-steps in task model"
  - "each has status"
---

# G3.S5.T2: Per-system processing stages model

## Task

Extend task model with per-system sub-steps (docling/LightRAG/llm_wiki).

## Implementation

- docling: read file / parse+OCR+image desc
- LightRAG: chunking / entity extraction / graph build / embedding
- llm_wiki: classify / write page / rebuild index
- each sub-step status pending/running/done/failed

## Reference
- `docs/kanban/G3/S5/Spec.md`
- `docs/g3-requirements.md`

## Log

- 2026-08-08 (opencode, ses_d04b42333b897b68): T2 done.
  - `server/src/kb/tasks.ts` — extended task model with per-system sub-steps:
    - `TaskStep { name, status, error? }` + `TaskStage.steps: TaskStep[]`.
    - docling: `read_file` / `parse_ocr_image_desc`; LightRAG: `chunking` / `entity_extraction` / `graph_build` / `embedding`; llm_wiki: `classify` / `write_page` / `rebuild_index`.
    - Each sub-step carries a status (`pending`/`running`/`done`/`failed`) via `initialSteps`, driven in `run()` (`markStageSteps`/`setStep`), reset on retry (`initialStage`), marked failed on stage failure (`fail`), done on dedup skip (`markDedup`).
  - `server/src/kb/ingest.ts` — `ingestLlmWiki` accepts an optional `onStep(step, status)` progress callback so classify/write_page/rebuild_index sub-steps reflect REAL backend phases (not faked).
  - `web/src/api/kb.ts` — mirrored `IngestTaskStep` + `IngestTaskStage.steps`.
  - `web/src/kb/ingest.ts` — shared `initialStages()` optimistic stage tree used by addFile/addUrl/restorePersisted.
  - `web/src/views/UploadsView.vue` — per-system task cards now render each sub-step with status mark (✓/…/✕/○) and failed-step error tooltip.
  - Tests: `server/test/kb/tasks.test.ts` (+5: sub-step seed, all-done on success, docling sub-step failure, llm_wiki sub-step failure with classify done + later failed, retry resets sub-steps). `web/src/__tests__/uploads-view.spec.ts` (+1 sub-step rendering). Server suite 434 green; web 258 green (3 pre-existing unrelated failures: knowledge-view / knowledge-add-data).
  - LightRAG sub-steps currently marked running→done/failed around the ingest API call; real backend polling + chunk progress → T3.
