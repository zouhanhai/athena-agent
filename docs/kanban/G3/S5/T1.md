---
id: t1
title: "G3.S5.T1: Uploads page as independent tab"
layer: T
parent: G3.S5
owner: eng-director
status: approved
assignee: opencode
started_at: 2026-08-08
session_id: ses_a3f9c1e7d5b2a4f6
blocked_by: []
acceptance_criteria:
  - "Uploads nav item"
  - "upload area works"
  - "task list"
---

# G3.S5.T1: Uploads page as independent tab

## Task

Uploads as independent sidebar tab (upload area + task list).

## Implementation

- Uploads nav item + page
- upload area (drag/drop/select/URL) reuse G2
- task list

## Reference
- `docs/kanban/G3/S5/Spec.md`
- `docs/g3-requirements.md`

## Log

- 2026-08-08 (opencode, ses_a3f9c1e7d5b2a4f6): T1 done.
  - `web/src/views/UploadsView.vue` — independent Uploads page (header + upload area + task list).
  - Upload area reuses the G2 Add Data logic via `useIngestTasks` (`web/src/kb/ingest.ts`): drag/drop + click-to-select file input + URL ingest row, wired to `POST /api/kb/ingest` / `POST /api/kb/ingest-url`.
  - Task list: one card per upload (filename, status badge, t-progress, per-system stages Parse/LightRAG/llm_wiki, Retry on failed stage, Remove), polled from `GET /api/kb/task/:id`.
  - `web/src/router/index.ts` — added `/uploads` route.
  - `web/src/components/SidebarNav.vue` — added "Uploads" nav item (UploadIcon); sidebar stays pure navigation.
  - Global Chat panel (G3.S3) is already rendered app-wide by `App.vue` and now shows context "Uploads" (`PAGE_LABELS` already had `/uploads`).
  - Tests: `web/src/__tests__/uploads-view.spec.ts` (9 tests) — nav item, route navigation, upload area, file/URL submit, stage rendering, retry, global chat presence + context. Full web suite green apart from 3 pre-existing unrelated failures (knowledge-view / knowledge-add-data).
  - Per-system sub-steps + LightRAG chunk progress + real backend status → S5.T2/T3.
