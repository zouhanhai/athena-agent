---
id: t5
title: "G3.S5.T5: Show source images in llm_wiki pages (pure-text for LightRAG) — export images + copy beside page + serve to frontend"
layer: T
parent: G3.S5
owner: eng-director
status: approved
assignee: opencode
session_id: ses_g3s5t5
started_at: 2026-08-09
blocked_by: []
acceptance_criteria:
  - "docling exports extracted images to disk (--images-dir) during ingest, referenced in the markdown"
  - "llm_wiki page file (wiki/<topic>/<name>.md) keeps the image references, and the image files are copied beside it so the page renders WITH images in the llm_wiki reader"
  - "LightRAG still ingests the SAME markdown but sees only text (image alt-text / VLM description) — image bytes never affect LightRAG chunking"
  - "a wiki-image route serves the image bytes so WikiView renders <img> with a working URL"
  - "VLM picture descriptions (alt text) are preserved so image content stays RAG-searchable"
---

# G3.S5.T5: Show source images in llm_wiki pages (pure-text for LightRAG)

## Goal (user's words)

When reading an .md file in **llm_wiki**, it should show **both the text AND the original document images**,
at the same relative position as in the original — like viewing the source file but as Markdown.
Meanwhile **LightRAG must be unaffected**: it should still see **pure text** (the VLM description docling
generates), NOT image references/bytes.

In short: the current `<image>` placeholder becomes a **link to a stored image file** so that when the
md is opened in llm_wiki the image auto-loads and displays; but LightRAG still sees the plain-text file.

## Architecture (already favourable — confirm, don't break)

Current ingest flow (server/src/kb/ingest.ts `ingestMarkdown`):
- docling parses source → markdown containing `![<VLM description>](images/xxx.png)` (image ref + alt text)
- `ingestLlmWiki(fileName, content)` writes the **full markdown** to `wiki/<topic>/<name>.md` (line 486)
  → llm_wiki reads this file; it will render images **if the image files sit beside it**
- `ingestLightRag(frontmatterContent)` sends the same markdown to LightRAG → LightRAG stores **text only**
  (image refs collapse to the `![alt]` text = the VLM description) → chunking unaffected ✅

So the split is already correct. The gap is ONLY the **image files**: docling doesn't save them because
`docling.ts` `parse()` calls parse_doc.py **without `--images-dir`**.

## Notes / gotchas

- **docling `picture_area_threshold` (default 0.05)**: images smaller than 5% of the page area are
  **NOT sent for VLM description** (docling/models/picture_description_base_model.py:88). They are still
  present as `PictureItem` and **still exported** via `--images-dir`, so they WILL show in llm_wiki —
  but they get **no alt-text description** (so their content is not RAG-searchable).
- If the product wants small images described too, lower the threshold via
  `PictureDescriptionApiOptions(picture_area_threshold=...)` in `parse_doc.py` (e.g. 0.01 or 0.0).
  This is an **optional knob**, not required for the image-display goal. (Done: set to 0.01.)

## Long documents: table of contents + heading anchors

For large multi-section files, when reading a page in llm_wiki the reader should offer a
**table of contents (TOC)** and **jump-to-section links** so the user can navigate a long doc directly
to a fixed chapter. This is a **renderer-level** enhancement (no ingest change needed):

- Add **heading anchors**: `markdown-it-anchor` → each heading (`#`/`##`/`###`) gets a stable `id` and a
  permalink/scroll target, so anchors like `#section-2` resolve to the right heading.
- Add **TOC**: `markdown-it-table-of-contents` (or a small custom extraction) renders a nested list of
  headings at the top of the page; clicking an entry scrolls to that heading (`#id`).
- Frontend `WikiView`: render the TOC above the page body (optionally collapsible), and ensure heading
  `id`s are stable/slugified. Keep it theme-aware (uses `--caleo-*` vars).
- Pure frontend/renderer change — LightRAG and the stored markdown are untouched.

## Implementation

1. `server/src/kb/docling.ts` `parse()`: append `--images-dir <outDir>/images/<stem>` to the parse_doc.py
   argv; return the images dir in `DoclingParseResult`.
2. `server/src/kb/ingest.ts` `ingestLlmWiki` (and the task-queue path in `tasks.ts`): after writing the
   wiki page, **copy the extracted image files into the same target dir** (`wiki/<topic>/images/`) and,
   if needed, rewrite the markdown image refs so they resolve relative to the page. The wiki page's
   frontmatter/content is unchanged apart from the image path normalization.
3. `server/src/routes/kb.ts`: add `GET /api/kb/wiki/image?path=` (same `isSafeWikiPath` style guard) that
   streams the image bytes from the wiki dir. Update `web/src/views/WikiView.vue` so `renderMarkdown`
   output rewrites `<img src="images/...">` → `<img src="/api/kb/wiki/image?path=<resolved>">`.
4. Do NOT touch the LightRAG path — it already gets pure text. Add a test asserting LightRAG content has
   no image refs (or only alt text) while the wiki file keeps them.
5. Tests: docling returns images dir; images copied beside wiki page; wiki image route serves bytes;
   frontend image src rewritten to the served URL; LightRAG content remains text-only.

## Recommended Skills (OpenCode)
- **tdd** / **implement** / **code-review** / **codegraph_explore**

## Reference
- `server/src/kb/docling.ts` (`parse`, `DoclingParseResult`, `defaultSharedInputDir`)
- `server/scripts/parse_doc.py` (`--images-dir`, `export_to_markdown(image_export_dir)`)
- `server/src/kb/ingest.ts` (`ingestLlmWiki`, `writeFile`, `withFrontmatter`), `server/src/kb/tasks.ts`
- `server/src/routes/kb.ts` (`readWikiPage`, `isSafeWikiPath`), `server/src/kb/retrieval.ts`
- `web/src/views/WikiView.vue`, `web/src/kb/markdown.ts` (`renderMarkdown`)

## Log

[2026-08-09] opencode completed G3.S5.T5 (ses_g3s5t5):

- **docling image export**: `docling.ts parse()` now appends `--images-dir <outDir>/images/<stem>` and returns
  `imagesDir` in `DoclingParseResult`. `parse_doc.py` was migrated to the current docling 2.118 API
  (`export_to_markdown(image_export_dir=...)` is gone): `generate_picture_images=True` +
  `save_as_markdown(artifacts_dir=<relative>, image_mode=REFERENCED)` → images written to
  `<outDir>/images/<stem>/` and markdown refs are relative (`![Image](images/<stem>/image_x.png)`), so the
  refs resolve unchanged when the images are copied beside the page. VLM picture descriptions are preserved
  (verified: description text block still emitted next to the image ref).
- **copy beside page**: `ingestLlmWiki` gains an optional `images` param; `copyExtractedImages` (recursive,
  injectable fs) copies the exported images into `wiki/<topic>/images/<stem>/` after the page write — no ref
  rewriting needed (the copy preserves the relative layout the refs already use). Task queue (`tasks.ts`)
  threads `task.images` through and retains it for retry.
- **serve**: `GET /api/kb/wiki/image?path=` (guarded like `isSafeWikiPath`, resolves against the on-disk wiki
  dir via `retrieval.readWikiImage`) streams image bytes; `defaultRetrievalService` honours `LLM_WIKI_WIKI_DIR`
  to match ingest. `web/src/kb/markdown.ts` rewrites `<img src="images/...">` →
  `/api/kb/wiki/image?path=<resolved>` when a `pagePath` is passed; `WikiView` passes it.
- **LightRAG untouched**: still ingests the same markdown text only (image bytes never reach it); test asserts
  no served URLs/base64 in LightRAG content while the wiki file keeps the refs.
- **TOC + heading anchors (renderer-only)**: added `markdown-it-anchor` (stable slugified heading ids +
  subtle `#` permalink) and `markdown-it-table-of-contents` (nested `.wiki-toc` rendered at the top of long
  pages); `WikiView` renders it and `scrollIntoView`s the target heading on click (the pane is the scroll
  container). Theme-aware via existing `--caleo-*` vars. Stored markdown + LightRAG untouched.
- **Tests**: docling returns images dir; images copied beside page (incl. no-images skip); wiki image route
  serves bytes / rejects unsafe paths / 404; frontend image src rewritten; LightRAG text-only; TOC/anchor
  rendering + scroll click. Fixed 2 stale `tasks.test.ts` assertions to the single `chunking_embedding` step
  model (G3.S5.T4) and updated `derive_stem` test to keep-extension behavior. Server 184/184 pass +
  `tsc --noEmit` clean; web 281/281 pass + `vue-tsc --noEmit` clean; `test:docling` 13/13 pass.
