---
id: t1
title: "G3.S4.T1: Workbench page with 3 tabs (Code/Issues/Kanban)"
layer: T
parent: G3.S4
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-07T23:58:51Z
blocked_by: []
session_id: ses_89d6c35513a4c0a9
acceptance_criteria:
  - "Workbench nav item"
  - "3 tabs render"
  - "global chat present"
---

# G3.S4.T1: Workbench page with 3 tabs (Code/Issues/Kanban)

## Task

Workbench page with GitHub-style 3 tabs; global chat on right.

## Implementation

- Workbench nav item + page
- [Code][Issues][Kanban] tabs (TDesign)
- Global chat (S3) on right

## Reference
- `docs/kanban/G3/S4/Spec.md`
- `docs/g3-requirements.md`

## Log

### 2026-08-08 — opencode (ses_89d6c35513a4c0a9) — done

Frontend shell only; the tab contents arrive in T2/T3/T4. S3 global chat was already mounted in `App.vue`.

**New** `web/src/views/WorkbenchView.vue`
- GitHub-style `t-tabs` with 3 panels: **Code** (default), **Issues**, **Kanban** — each a placeholder panel describing what lands in T2/T3/T4.
- Headless tab state (`activeTab`) typed as `code | issues | kanban` so T2/T3/T4 mount real content into the same panels.

**Routing / nav**
- `web/src/router/index.ts`: added `/workbench` → `WorkbenchView` (lazy import).
- `web/src/components/SidebarNav.vue`: added **Workbench** nav item (`TerminalIcon`) between Knowledge and Kanban.

**Global chat**
- Already global in `App.vue`; chat store's `PAGE_LABELS["/workbench"]` existed → the panel shows "Context: Workbench" on the page at no extra cost.

**Tests** (TDD — red first, `workbench-view.spec.ts`)
- Sidebar renders the Workbench item; clicking navigates to `/workbench`.
- 3 tab labels render; Code is default; clicking Issues/Kanban switches the visible panel.
- Global chat panel stays mounted on `/workbench`; chat `page` = `/workbench` and header shows "Context: Workbench".

**Verification**: `web/src/__tests__/workbench-view.spec.ts` (8) + `layout.spec.ts` (18) green; full web suite 192/195 (3 failures are the pre-existing knowledge-view/graph ones — confirmed failing on the pre-change tree via stash); `vue-tsc` clean; `vite build` succeeds.
