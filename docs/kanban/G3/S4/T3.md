---
id: t3
title: "G3.S4.T3: Issues tab (GitHub style)"
layer: T
parent: G3.S4
owner: eng-director
status: done
assignee: opencode
started_at: "2026-08-08"
session_id: ses_7d3b5a1c9e0f8d6a
blocked_by: []
acceptance_criteria:
  - "issues list renders"
  - "scoped to user"
---

# G3.S4.T3: Issues tab (GitHub style)

## Task

GitHub-style Issues tab: issue list.

## Implementation

- issue list (open/closed, labels, assignees)
- consumes S6 GitHub API

## Reference
- `docs/kanban/G3/S4/Spec.md`
- `docs/g3-requirements.md`

## Log

### 2026-08-08 — opencode (ses_7d3b5a1c9e0f8d6a)
- **Backend (S6 API extension):**
  - `server/src/github/client.ts`: `GithubIssue` now carries `assignees: string[]`; `listIssues` accepts an optional `state` filter (`open` | `closed` | `all`, default `open`) via `listCollection`.
  - `server/src/routes/github.ts`: `GET /api/github/repos/:owner/:repo/issues?state=...` validates and forwards the state filter (400 on invalid), scoped to the signed-in user's credential.
- **Frontend (Issues tab, GitHub style):**
  - `web/src/api/github.ts`: `GithubIssue` + `GithubIssueState` types and `fetchIssues(sessionToken, owner, repo, state)`.
  - `web/src/components/IssuesTab.vue`: repo selector, Open/Closed/All state filter buttons, GitHub-style rows with state icon, title link, labels, assignees, `#n opened/closed by ...` meta. Mirrors CodeTab patterns (empty/no-session/error states, scoped to session token).
  - `web/src/views/WorkbenchView.vue`: Issues tab now mounts `IssuesTab`.
- **Tests (TDD):**
  - Server: `listIssues` state filter + assignees mapping (`github-client.test.ts`), route state pass-through + auth (`github-routes.test.ts`).
  - Web: `fetchIssues` (`github-api.spec.ts`), new `issues-tab.spec.ts` (7 cases), `workbench-view.spec.ts` unchanged and green.
- Server `npm test` 418/418 pass; web `vitest` 239/242 (3 pre-existing KnowledgeView failures unrelated to this ticket, present on base commit); `vue-tsc`, `vite build`, server `tsc` all clean.
