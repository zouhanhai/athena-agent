---
id: t4
title: "G3.S4.T4: Kanban tab (board from docs-scan)"
layer: T
parent: G3.S4
owner: eng-director
status: done
assignee: opencode
session_id: ses_bca5ae735ee2f439
started_at: 2026-08-08
blocked_by: []
acceptance_criteria:
  - "board renders"
  - "shows G.S.T + status"
---

# G3.S4.T4: Kanban tab (board from docs-scan)

## Task

Kanban tab: board from S6 docs-scan.

## Implementation

- consume S6 /api/kanban
- columns by status or G.S.T tree

## Reference
- `docs/kanban/G3/S4/Spec.md`
- `docs/g3-requirements.md`

## Log
[2026-08-08] opencode completed Kanban tab (TDD, 9 new tests → 251 pass / 3 pre-existing fail):
  - **`web/src/api/kanban.ts`** — frontend API layer for the docs-scan board: types mirroring the
    server board schema (Goal/Spec/Ticket frontmatter + `KanbanBoard`), `TICKET_STATUSES` constant,
    and `fetchBoard(sessionToken)` → `GET /api/kanban` (Bearer auth).
  - **`web/src/components/KanbanTab.vue`** — board render: a G.S.T tree header (Goals → Specs refs +
    titles) plus six kanban columns by ticket status (backlog / in_progress / done / in_review /
    approved / rejected), each card showing its G.S.T ref, title, spec ref and assignee; empty state
    without a session, error banner on API failure, scan-error count when the board carries errors.
  - **`web/src/views/WorkbenchView.vue`** — Kanban tab now renders `<KanbanTab>` (replaces the
    placeholder), `tab-panel-kanban` + `kanban-tab-host` styling.
  - Tests: `kanban-api.spec.ts` (2: GET /api/kanban with Bearer token, error propagation),
    `kanban-tab.spec.ts` (7: empty state, columns render, per-status placement, G.S.T ref + assignee
    on cards, goal/spec refs shown, empty column, error banner). Workbench view spec still green
    (3 tabs + Kanban click). `typecheck` + `build` clean.
  - Code-review (2 axes): Standards — removed dead `loading` state; Spec — no findings (columns by
    status + G.S.T tree both present, acceptance criteria met).
  - Full web suite: 248 pass / 3 pre-existing fail (knowledge-view + knowledge-add-data, unrelated,
    reproduced on clean master).
  - Acceptance criteria met: board renders, shows G.S.T + status.
