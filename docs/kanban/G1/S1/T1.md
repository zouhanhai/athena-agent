---
id: t1
title: "G1.S1.T1: 项目初始化 + Fastify 骨架"
layer: T
parent: G1.S1
owner: eng-director
status: backlog
assignee: ""
started_at: ""
blocked_by: []
acceptance_criteria:
  - "package.json 存在且可 npm install"
  - "tsconfig.json 配置 TypeScript"
  - "Fastify 服务能启动 (默认端口)"
  - "目录结构含 src/agents, src/routes, src/config"
---

# G1.S1.T1: 项目初始化 + Fastify 骨架

## Task

在 athena-agent 后端建立 Node/TS + Fastify 骨架：

1. 创建 `server/` 目录（后端根目录）
2. 初始化 package.json（npm init，含 fastify, typescript 等依赖）
3. 配置 tsconfig.json（TypeScript 严格模式）
4. 创建目录结构：
   - `server/src/agents/` — AgentSession 管理（预留）
   - `server/src/routes/` — API 路由（预留）
   - `server/src/config/` — 配置
5. 创建一个最小 Fastify 服务入口（`server/src/index.ts`），能启动并监听端口
6. 验证 `npm install` + 服务能启动

## 参考

- docs/adr/0001-node-fastify-agent-session.md
- docs/kanban/G1/S1/Spec.md

## 环境

- 开发目录: 6900XT 的 ~/athena-agent
- Node 24 + npm 11（已装）
- 默认模型: opencode-go/deepseek-v4-flash

## Log
