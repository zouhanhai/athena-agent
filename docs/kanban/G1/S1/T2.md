---
id: t2
title: "G1.S1.T2: Pi SDK Integration Skeleton (createAgentSession Verified)"
layer: T
parent: G1.S1
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: ["t1"]
acceptance_criteria:
  - "Can create an AgentSession with createAgentSession()"
  - "Confirm ModelRuntime can load DeepSeek model"
  - "session.prompt() can send messages and receive responses"
  - "Has a minimal reusable agent creation helper function"
---

# G1.S1.T2: Pi SDK Integration Skeleton (createAgentSession Verified)

## Task

Verify that Pi SDK can be embedded and work within the athena backend. This is the core engine verification for athena.

1. Create `agent.ts` in `server/src/agents/`:
   - Use `createAgentSession()` to create sessions
   - Use `ModelRuntime.create()` to initialize model runtime
   - Configure DeepSeek model (opencode-go/deepseek-v4-flash or DeepSeek API)
2. Write a minimal verification script: create session → `prompt("hi")` → print response
3. Confirm installed Pi packages can load (simple verification that memory/intercom don't error)
4. Encapsulate a reusable function (e.g. `createAgent()`) for subsequent per-employee instance use

## Reference

- Spec: `docs/kanban/G1/S1/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- Docs: `docs/pi-capabilities.md` (Pi SDK API)
- Pi SDK example: https://github.com/earendil-works/pi/blob/main/packages/coding-agent/examples/sdk

## How to Locate Reference Docs

- `parent: G1.S1` → `docs/kanban/G1/S1/Spec.md`
- → Goal: `docs/kanban/G1/Goal.md`

## Note

- AgentSession is the core of athena; this ticket is the most critical verification
- If ModelRuntime config encounters issues, first confirm auth/model availability (see opencode auth)
- Use **tdd** approach: write tests first to verify createAgentSession works

## Dependencies

- T1 (project skeleton + dependencies) complete

## Log
[2026-08-05] opencode claimed T2 (in_progress)
[2026-08-05] opencode completed T2 (done)

## Completion Summary

Pi SDK embedding verification succeeded:

- **Implementation** `server/src/agents/agent.ts`:
  - `createAgent()` reusable function: `ModelRuntime.create()` → loads DeepSeek model
    (`deepseek/deepseek-v4-flash`, auth from `~/.pi/agent/auth.json`) → `createAgentSession()`.
  - Encapsulated `prompt()` (send message and return assistant final text), `dispose()`.
  - Exposes `model`/`packages`/`extensionErrors`, supports custom `agentDir`/`cwd`/`providerId`/`modelId`.
- **Verification script** `server/src/agents/verify-agent.ts` (`npm run verify`): create session →
  `prompt("hi")` → print response, DeepSeek replies normally.
- **TDD tests** `server/test/agents.test.ts` (4 passing): create AgentSession /
  DeepSeek model loaded / prompt receives non-empty response / 10 Pi packages load without errors
  (pi-mcp-adapter, pi-intercom, pi-hermes-memory, pi-web-access, pi-crew, etc.).
- `test` script adds `--test-force-exit` (API keep-alive connections prevent process exit).

All acceptance criteria met: createAgentSession ✓ · DeepSeek model loaded ✓ · prompt Q&A ✓ ·
minimal reusable function createAgent ✓
