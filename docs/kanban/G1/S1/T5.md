---
id: t5
title: "G1.S1.T5: 端到端对话测试"
layer: T
parent: G1.S1
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: ["t4"]
acceptance_criteria:
  - "自动化测试覆盖: 对话/多轮上下文/错误处理"
  - "多轮对话保持上下文 (同 userId 复用 session)"
  - "不同 userId 隔离 (不串上下文)"
  - "测试通过 + 文档记录如何运行测试"
---

# G1.S1.T5: 端到端对话测试

## Task

验证个人对话端到端工作，覆盖核心场景。

1. 建立自动化测试（vitest 或 node:test）：
   - 单条消息 → 返回回答
   - 多轮对话（同 userId）→ 保持上下文
   - 不同 userId → 隔离上下文
   - 错误处理（空消息、无效 userId、服务未启动）
2. 测试 POST /api/chat 流式 + 非流式
3. 全部测试通过
4. 在 README 或文档记录如何运行测试（npm test）

## 参考

- Spec: `docs/kanban/G1/S1/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- 文档: `docs/pi-capabilities.md`

## 如何定位参考文档

- `parent: G1.S1` → `docs/kanban/G1/S1/Spec.md`
- → Goal: `docs/kanban/G1/Goal.md`

## 说明

- **tdd**: 这是 M1 验收的最终验证——"个人对话端到端跑通"
- 多轮上下文是验收关键（证明 session 复用正确）
- 隔离验证（不同 userId）证明每员工独立
- **代码审查通过后**，此 ticket approved 即 G1.S1 完成

## 依赖

- T4（对话 API）完成

## Log

### 2026-08-05 — opencode 完成 T5 (端到端对话测试)

新增 `server/test/e2e-conversation.test.ts`（7 个端到端用例，真实 DeepSeek）：

- 单条消息 → 非流式返回 `{reply}` + 会话落库
- 单条消息 → 流式 SSE 逐块 delta + `done` 结束
- 多轮对话（同 userId）保持上下文：第二轮能复述第一轮记忆的名字；`manager.size` 恒为 1（复用同一 session），会话历史保留 2 轮用户消息
- 不同 userId 上下文隔离：bob 会话与回答均不含 alice 的令牌，alice/bob 为 2 个独立 session
- 错误处理：空消息 / 缺字段 / 非字符串 → 400 `{error}`；对话服务未启动 → 非流式 500 `{error}` / 流式 SSE error 帧

实现改动：

- `src/agents/manager.ts` 新增 `getSessionMessages()` 公共 seam，供确定性断言会话历史
- `src/routes/chat.ts` 非流式路径补 try/catch → 500 `{error}`（原为 Fastify 默认 500）
- `README.md` 新增「测试」章节，记录 `npm test` / `npm run typecheck` 用法与覆盖范围

验证：全量 28 测试通过（含既有 21 个），typecheck + build 通过。
