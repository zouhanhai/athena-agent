---
id: t5
title: "G1.S1.T5: End-to-End Chat Tests"
layer: T
parent: G1.S1
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: ["t4"]
acceptance_criteria:
  - "Automated tests cover: chat/multi-turn context/error handling"
  - "Multi-turn chat maintains context (same userId reuses session)"
  - "Different userId isolation (no context leakage)"
  - "Tests pass + docs record how to run tests"
---

# G1.S1.T5: End-to-End Chat Tests

## Task

Verify personal chat works end-to-end, covering core scenarios.

1. Build automated tests (vitest or node:test):
   - Single message → returns response
   - Multi-turn chat (same userId) → maintains context
   - Different userId → context isolation
   - Error handling (empty message, invalid userId, service not started)
2. Test POST /api/chat streaming + non-streaming
3. All tests pass
4. Record how to run tests in README or docs (npm test)

## Reference

- Spec: `docs/kanban/G1/S1/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- Docs: `docs/pi-capabilities.md`

## How to Locate Reference Docs

- `parent: G1.S1` → `docs/kanban/G1/S1/Spec.md`
- → Goal: `docs/kanban/G1/Goal.md`

## Notes

- **tdd**: This is the final verification for M1 acceptance — "personal chat end-to-end works"
- Multi-turn context is key acceptance criteria (proves session reuse is correct)
- Isolation verification (different userId) proves per-employee independence
- **After code review passes**, this ticket approved = G1.S1 complete

## Dependencies

- T4 (chat API) complete

## Log

### 2026-08-05 — opencode completed T5 (end-to-end chat tests)

Added `server/test/e2e-conversation.test.ts` (7 end-to-end cases, real DeepSeek):

- Single message → non-streaming returns `{reply}` + session persisted to DB
- Single message → streaming SSE chunk-by-chunk delta + `done` end
- Multi-turn chat (same userId) maintains context: second turn can recall name memorized in first turn; `manager.size` always 1 (reuses same session), session history retains 2 rounds of user messages
- Different userId context isolation: bob's session and response contain none of alice's tokens, alice/bob are 2 independent sessions
- Error handling: empty message / missing field / non-string → 400 `{error}`; chat service not started → non-streaming 500 `{error}` / streaming SSE error frame

Implementation changes:

- Added `getSessionMessages()` public seam to `src/agents/manager.ts` for deterministic session history assertions
- Added try/catch to non-streaming path in `src/routes/chat.ts` → 500 `{error}` (was Fastify default 500)
- Added "Testing" section to `README.md`, documenting `npm test` / `npm run typecheck` usage and coverage

Verification: Full 28 tests pass (including existing 21), typecheck + build pass.
