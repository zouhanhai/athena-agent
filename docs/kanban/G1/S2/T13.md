---
id: t13
title: "G1.S2.T13: Sidebar Layout Adjustment + Light Theme Refinement"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: "opencode"
started_at: "2026-08-05"
blocked_by: []
acceptance_criteria:
  - "Sidebar shifted down: leave space at top, content starts aligned with main content area title (Personal Chat)"
  - "Settings button placed at sidebar very bottom, middle empty space acceptable"
  - "Light theme: sidebar light gray background"
  - "Light theme: user chat bubble blue (differentiated)"
  - "Light theme: AI reply bubble distinguished from background"
  - "Sidebar/bubbles coordinated in both dark/light themes"
---

# G1.S2.T13: Sidebar Layout Adjustment + Light Theme Refinement

## Task

Adjust sidebar layout + refine light theme layering and bubble colors.

## Current Issues

**Sidebar layout**:
1. Sidebar starts from very top; user wants it shifted down (leave space at top)
2. Settings position: user wants at very bottom, middle space acceptable

**Light theme**:
3. Still too white, sidebar needs light gray background
4. Light chat: user bubble should be blue (differentiated)
5. Light chat: AI reply bubble not distinguished from background (need to add)
6. Dark theme already has color differentiation (OK, no change needed)

## Implementation

### Sidebar layout
- Sidebar shifted down: leave padding/space at top, content starts roughly at main area title height
- Settings fixed at very bottom (footer), middle space acceptable (flex column, footer bottom-aligned)

### Light theme
- Sidebar: light gray background (e.g. #f0f1f3)
- User bubble: blue (e.g. #69b3e7 or dark blue #2d3142 with white text)
- AI bubble: light gray (e.g. #e5e7eb) distinguished from white background
- Adjust `--caleo-bubble-*` variables (dark/light two sets)

## Reference

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- Existing: `web/src/components/SidebarNav.vue`, `web/src/views/ChatView.vue`, `web/src/theme/index.ts`

## How to Locate Reference Docs

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- Theme: `web/src/theme/index.ts`
- Chat: `web/src/views/ChatView.vue`

## Notes

- Bubble variables in theme/index.ts (--caleo-bubble-bg)
- ChatView bubble class differentiates user/assistant
- Brand colors: orange #ff6633 dark blue #2d3142 sky blue #69b3e7 light gray #bfc0c0
- Use **implement** + code-review + visual verification

## Dependencies

- None

## Log

- 2026-08-05 opencode: T13 done
  - Sidebar shifted down: `.app-aside` top 24px (aligned with main content area padding, brand-name same height as "Personal Chat" title), header top padding adjusted to 16px
  - Settings at bottom: existing flex column (`side-menu { flex: 1 }`) already makes `.app-footer` sink to bottom, middle space acceptable (added structure assertion: aside last child is `.app-footer` and contains settings-trigger)
  - Light theme: sidebar `--caleo-sidebar-bg` #f5f6f7→#f0f1f3 (light gray), hover #e5e7eb; AI bubble `--caleo-bubble-ai` #f0f1f3→#e5e7eb distinguished from white panel (#ffffff); user bubble stays sky blue #69b3e7
  - Dark theme unchanged (already coordinated): AI #33373f / user orange #ff6633
  - Tests (tdd): theme.spec updated light sidebar/bubble-ai expectations; layout.spec added sidebar top offset + Settings sink to bottom assertions; total 53 tests green + typecheck + build pass
