---
id: t6
title: "G1.S2.T6: 前端测试 + 端到端对话验证"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: opencode
started_at: "2026-08-05"
finished_at: "2026-08-05"
blocked_by: ["t5"]
acceptance_criteria:
  - "前端测试覆盖 (组件/状态, mock API)"
  - "前端 npm test 通过"
  - "端到端验证: 启动后端+前端, 浏览器完成一次个人对话"
  - "G1.S2 全部 done 后 G1/M1 达成"
---

# G1.S2.T6: 前端测试 + 端到端对话验证

## Task

验证个人对话前端端到端工作，覆盖核心场景 + 确认 M1 达成。

1. 写前端测试:
   - 对话 store 测试 (发消息/状态)
   - 对话面板组件测试 (mock API)
   - 流式解析测试
   - 用 Vitest + Vue Test Utils
2. 前端 npm test 全绿
3. 端到端验证:
   - 启动后端 (server npm start)
   - 启动前端 (web npm dev)
   - 浏览器/HTTP 完成一次"发消息→收到回答"的个人对话
   - 验证流式
4. 确认 G1.S2 的 acceptance criteria 全部满足

## 参考

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`

## 如何定位参考文档

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- → Goal: `docs/kanban/G1/Goal.md`

## 说明

- 这是 M1 验收的最后验证 (个人对话前端端到端)
- 端到端验证用真实后端 (curl 或浏览器)
- **测试通过 + code-review 通过后, G1.S2 done = M1 达成**

## 依赖

- T5 (API+store) 完成

## Log

- 2026-08-05 opencode: 前端测试全覆盖 + 端到端对话验证通过, G1.S2 done = M1 达成
  - 前端测试 (vitest 33 项全绿, 6 文件):
    - `chat-store.spec.ts` 对话 store 发消息/状态 (流式写入/空消息守卫/SSE 错误/异常/loading/reset)
    - `chat.spec.ts` 对话面板组件测试 (mock API 层: 渲染/发送/typewriter/loading/错误)
    - `sse.spec.ts` 流式解析测试 (delta 顺序/跨 chunk 重组/多事件单 chunk/done/error)
    - `chat-api.spec.ts` API 层测试 (sendChat/streamChat 请求构造 + 错误)
    - `app.spec.ts` + `layout.spec.ts` 路由/侧边栏骨架
  - 端到端验证 (真实后端 + 真实前端 + 真实浏览器):
    - 启动后端 `server npm start` (:3000), 启动前端 `web npm dev` (:5173, proxy /api)
    - HTTP 直连 + Vite proxy: 非流式 POST /api/chat → {reply}; SSE (Accept: text/event-stream) 多个 delta + done 事件逐帧推送
    - Playwright 真实 Chromium 浏览器: 打开门户 → 发消息 "say exactly: browser hello" → 收到回答, 无 console/page error
    - 流式验证: 浏览器内 assistant 气泡随 delta 逐段增长 ("one two three..." → "...eight"), 打字机效果确认
  - G1.S2 acceptance criteria 全满足: ① Vite+Vue3+TS+TDesign 启动 ✓ ② CALEO 主题 (#ff6633/#2d3142/#69b3e7) ✓ ③ 侧边栏布局骨架 (Chat/Knowledge/Kanban/Wiki) ✓ ④ 个人对话面板发消息+SSE 流式回答 ✓ ⑤ 对接后端 POST /api/chat ✓
  - M1 达成: G1 acceptance (后端启动 / AgentSession 内嵌 Pi / 个人对话端到端 / 前端侧边栏骨架) 全部满足
