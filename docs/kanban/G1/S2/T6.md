---
id: t6
title: "G1.S2.T6: Frontend Tests + End-to-End Chat Verification"
layer: T
parent: G1.S2
owner: eng-director
status: approved
assignee: opencode
started_at: "2026-08-05"
finished_at: "2026-08-05"
blocked_by: ["t5"]
acceptance_criteria:
  - "Frontend tests cover (components/state, mock API)"
  - "Frontend npm test passes"
  - "End-to-end verification: start backend+frontend, complete a personal chat in browser"
  - "G1.S2 all done → G1/M1 achieved"
---

# G1.S2.T6: Frontend Tests + End-to-End Chat Verification

## Task

Verify personal chat frontend works end-to-end, covering core scenarios + confirm M1 achievement.

1. Write frontend tests:
   - Chat store tests (send message/state)
   - Chat panel component tests (mock API)
   - Streaming parser tests
   - Use Vitest + Vue Test Utils
2. Frontend npm test all green
3. End-to-end verification:
   - Start backend (server npm start)
   - Start frontend (web npm dev)
   - Complete a "send message → receive response" personal chat via browser/HTTP
   - Verify streaming
4. Confirm G1.S2 acceptance criteria all met

## Reference

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`

## How to Locate Reference Docs

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- → Goal: `docs/kanban/G1/Goal.md`

## Notes

- This is the final verification for M1 acceptance (personal chat frontend end-to-end)
- End-to-end verification uses real backend (curl or browser)
- **After tests pass + code-review passes, G1.S2 done = M1 achieved**

## Dependencies

- T5 (API+store) complete

## Log

- 2026-08-05 opencode: full frontend test coverage + end-to-end chat verification passed, G1.S2 done = M1 achieved
  - Frontend tests (vitest 33 all green, 6 files):
    - `chat-store.spec.ts` chat store send message/state (streaming write/empty message guard/SSE error/exception/loading/reset)
    - `chat.spec.ts` chat panel component tests (mock API layer: render/send/typewriter/loading/error)
    - `sse.spec.ts` streaming parser tests (delta order/cross-chunk reassembly/multi events single chunk/done/error)
    - `chat-api.spec.ts` API layer tests (sendChat/streamChat request construction + error)
    - `app.spec.ts` + `layout.spec.ts` route/sidebar skeleton
  - End-to-end verification (real backend + real frontend + real browser):
    - Started backend `server npm start` (:3000), started frontend `web npm dev` (:5173, proxy /api)
    - HTTP direct + Vite proxy: non-streaming POST /api/chat → {reply}; SSE (Accept: text/event-stream) multiple delta + done events pushed frame by frame
    - Playwright real Chromium browser: open portal → send "say exactly: browser hello" → receive response, no console/page error
    - Streaming verified: in-browser assistant bubble grows with delta chunks ("one two three..." → "...eight"), typewriter effect confirmed
  - G1.S2 acceptance criteria all met: ① Vite+Vue3+TS+TDesign starts ✓ ② CALEO theme (#ff6633/#2d3142/#69b3e7) ✓ ③ Sidebar layout skeleton (Chat/Knowledge/Kanban/Wiki) ✓ ④ Personal chat panel send message+SSE streaming response ✓ ⑤ Connects to backend POST /api/chat ✓
  - M1 achieved: G1 acceptance (backend starts / AgentSession embeds Pi / personal chat end-to-end / frontend sidebar skeleton) all met
