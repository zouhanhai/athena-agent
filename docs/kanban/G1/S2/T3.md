---
id: t3
title: "G1.S2.T3: Personal Chat Panel (Basic)"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: "opencode"
started_at: "2026-08-05"
blocked_by: ["t2"]
acceptance_criteria:
  - "/chat page has chat interface"
  - "Can input and send messages (userId manual input or default)"
  - "Calls backend POST /api/chat and displays response"
  - "Message list displayed (user messages + Pi responses)"
---

# G1.S2.T3: Personal Chat Panel (Basic)

## Task

Implement the personal chat panel — core MVP feature.

1. `/chat` page (views/ChatView.vue) chat interface:
   - Top: display current userId (MVP manual input or default)
   - Message area: scrollable list (message text)
   - Bottom: input box + send button
2. Connect to backend:
   - POST /api/chat { userId, message } → { reply }
   - Display response
3. Message states:
   - user messages (right-aligned)
   - assistant replies (left-aligned)
4. Disable button + loading state while sending

## Reference

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- Backend: server/src/routes/chat.ts (POST /api/chat)

## How to Locate Reference Docs

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- Backend API: `server/src/routes/chat.ts`

## Notes

- Non-streaming is sufficient (streaming in T4)
- Use **implement** + tdd
- Chinese comments OK, UI text in English

## Dependencies

- T2 (layout) complete

## Log

- 2026-08-05 opencode: completed personal chat panel (basic) — ChatView + POST /api/chat connection
  - `src/views/ChatView.vue`: chat interface (top userId manual input/default hermes, scrollable message area, bottom input box + send button)
  - Connects to POST /api/chat { userId, message } → { reply }; user messages right-aligned / assistant left-aligned; send button disabled + "Sending..." loading + typing indicator + error display
  - `src/__tests__/chat.spec.ts`: 6 component tests (rendering / send message calls API + bubble alignment / custom userId / loading disables / empty message not sent / error handling), mock fetch
  - Verified: vitest 13 all green, typecheck + build pass; real backend E2E: server `POST /api/chat` returns `{reply}`, reaches via Vite proxy `:5173/api`
  - Dependency: T2 complete; non-streaming only (streaming T4), T5 later extracts API layer + Pinia store
