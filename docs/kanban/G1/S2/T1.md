---
id: t1
title: "G1.S2.T1: Frontend Project Scaffold (Vue3 + Vite + TDesign)"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: []
acceptance_criteria:
  - "web/ directory exists, Vue3 + Vite + TypeScript project can start"
  - "TDesign Vue successfully imported"
  - "npm run dev can start, browser accessible"
  - "Basic components render (App skeleton)"
---

# G1.S2.T1: Frontend Project Scaffold (Vue3 + Vite + TDesign)

## Task

Establish the Vue3 frontend project scaffold in athena-agent.

1. Create `web/` directory (frontend root)
2. Initialize Vue3 + Vite + TypeScript project
   - Use `npm create vite@latest` or manual config
   - Install vue, vue-router, pinia
   - Install tdesign-vue-next
3. Configure Vite (dev server, proxy /api → http://localhost:3000)
4. Create basic entry (main.ts + App.vue)
5. Verify: npm run dev starts + browser accessible

## Reference

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- AGENTS.md

## How to Locate Reference Docs

- `parent: G1.S2` → Spec at `docs/kanban/G1/S2/Spec.md`
- Spec's `parent: G1` → Goal at `docs/kanban/G1/Goal.md`

## Environment

- Dev directory: `~/athena-agent/web` (6900XT)
- Node 24 + npm 11
- Vue3 + Vite + TS

## Dev Conventions

- Use **implement** skill
- Use **codegraph_explore** to understand existing server code
- Feature-level commits

## Log

### 2026-08-05 opencode

- Created `web/` frontend scaffold: Vue3 + Vite(7) + TypeScript + TDesign(v1) + vue-router + pinia
- `vite.config.ts`: dev server (0.0.0.0:5173) + proxy `/api` → `http://localhost:3000`, `@` alias → `src/`
- Basic entry `src/main.ts` (mounts Pinia/Router/TDesign) + `src/App.vue` portal skeleton
- Route placeholders: `/chat` `/knowledge` `/kanban` `/wiki` (placeholder pages under views/)
- CALEO theme colors injected via `src/styles/index.css` global CSS variables
- Test infrastructure: Vitest + Vue Test Utils + jsdom, `src/__tests__/app.spec.ts` smoke test
- Verification passed: `npm run typecheck` (vue-tsc) ✓, `npm test` 1 passed ✓, `npm run build` ✓,
  `npm run dev` starts HTTP 200, `/api` proxy works (500 when backend not started, not SPA fallback)
