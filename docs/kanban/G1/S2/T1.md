---
id: t1
title: "G1.S2.T1: 前端项目脚手架 (Vue3 + Vite + TDesign)"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: []
acceptance_criteria:
  - "web/ 目录存在，Vue3 + Vite + TypeScript 项目能启动"
  - "TDesign Vue 引入成功"
  - "npm run dev 能启动，浏览器可访问"
  - "基础组件渲染（App 骨架）"
---

# G1.S2.T1: 前端项目脚手架 (Vue3 + Vite + TDesign)

## Task

在 athena-agent 建立 Vue3 前端项目脚手架。

1. 创建 `web/` 目录（前端根目录）
2. 初始化 Vue3 + Vite + TypeScript 项目
   - 用 `npm create vite@latest` 或手动配置
   - 安装 vue, vue-router, pinia
   - 安装 tdesign-vue-next
3. 配置 Vite（dev server, 代理 /api → http://localhost:3000）
4. 创建基础入口（main.ts + App.vue）
5. 验证: npm run dev 启动 + 浏览器访问

## 参考

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- AGENTS.md

## 如何定位参考文档

- `parent: G1.S2` → Spec 在 `docs/kanban/G1/S2/Spec.md`
- Spec 的 `parent: G1` → Goal 在 `docs/kanban/G1/Goal.md`

## 环境

- 开发目录: `~/athena-agent/web`（6900XT）
- Node 24 + npm 11
- Vue3 + Vite + TS

## 开发规范

- 用 **implement** skill
- 用 **codegraph_explore** 理解现有 server 代码
- 功能级 commit

## Log

### 2026-08-05 opencode

- 创建 `web/` 前端脚手架: Vue3 + Vite(7) + TypeScript + TDesign(v1) + vue-router + pinia
- `vite.config.ts`: dev server (0.0.0.0:5173) + 代理 `/api` → `http://localhost:3000`, `@` alias → `src/`
- 基础入口 `src/main.ts` (挂载 Pinia/Router/TDesign) + `src/App.vue` 门户骨架
- 路由占位: `/chat` `/knowledge` `/kanban` `/wiki` (views/ 下占位页)
- CALEO 主题色已注入 `src/styles/index.css` 全局 CSS 变量
- 测试基建: Vitest + Vue Test Utils + jsdom, `src/__tests__/app.spec.ts` smoke test
- 验证通过: `npm run typecheck` (vue-tsc) ✓, `npm test` 1 passed ✓, `npm run build` ✓,
  `npm run dev` 启动 HTTP 200, `/api` 代理生效 (后端未启动时 500 而非 SPA fallback)
