---
id: t2
title: "G1.S2.T2: Sidebar Layout + Routing + CALEO Theme"
layer: T
parent: G1.S2
owner: eng-director
status: approved
assignee: "opencode"
started_at: "2026-08-05"
blocked_by: ["t1"]
acceptance_criteria:
  - "Sidebar layout (TDesign layout) has navigation items"
  - "Route config: /chat (personal chat) /knowledge /kanban placeholders"
  - "CALEO brand theme takes effect (orange #ff6633 etc.)"
  - "Route switching works correctly"
---

# G1.S2.T2: Sidebar Layout + Routing + CALEO Theme

## Task

Build the portal navigation skeleton — sidebar + routing + brand theme.

1. Build sidebar layout using TDesign Layout component:
   - Sidebar: navigation items (Chat/Kanban/Knowledge/Wiki etc.)
   - Main area: route outlet (router-view)
2. Configure Vue Router:
   - `/` → redirect to `/chat`
   - `/chat` → personal chat (placeholder)
   - `/knowledge`, `/kanban`, `/wiki` → placeholder pages
3. Apply CALEO theme:
   - Primary color orange #ff6633
   - Dark gray-blue #2d3142, light gray #bfc0c0, sky blue #69b3e7
   - Configure TDesign tokens or CSS variables
4. Verify: navigation switching + theme display

## Reference

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`

## How to Locate Reference Docs

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- → Goal: `docs/kanban/G1/Goal.md`

## Notes

- Reference WeKnora sidebar style (borrow layout, don't copy code)
- English UI (targeting German/international team)
- Use **implement** skill + tdd

## Dependencies

- T1 (scaffold) complete

## Log

- 2026-08-05 opencode: completed TDesign Layout sidebar skeleton + routes + CALEO theme
  - `src/components/SidebarNav.vue`: t-aside + t-menu(dark) nav items (Chat/Knowledge/Kanban/Wiki), `:value` bound to current route for highlighting, `to` uses vue-router
  - `src/App.vue`: t-layout + SidebarNav + t-content(router-view)
  - `src/theme/index.ts`: single-source CALEO palette + TDesign token overrides (--td-brand-color=#ff6633, --td-gray-color-13=#2d3142), injected via app root inline style
  - Routes (/ → /chat, /chat /knowledge /kanban /wiki) using T1 config
  - Tests: layout.spec.ts 7 items (nav item rendering / brand / redirect / click nav / active item highlight / CALEO theme variables), all green; typecheck + build pass
  - Dependency: added tdesign-icons-vue-next (icons), English UI
