---
id: t5
title: "G1.S2.T5: API 客户端 + Pinia 状态管理"
layer: T
parent: G1.S2
owner: eng-director
status: backlog
assignee: ""
started_at: ""
blocked_by: ["t4"]
acceptance_criteria:
  - "src/api/ 封装 POST /api/chat (非流式+流式)"
  - "Pinia store 管理对话状态 (消息列表/loading/error)"
  - "组件从 store 读状态, 不直接调 API"
  - "清晰可复用 (后续面板可复用 API 层)"
---

# G1.S2.T5: API 客户端 + Pinia 状态管理

## Task

封装 API 层和状态管理，让对话功能结构清晰、可复用。

1. `src/api/chat.ts`:
   - `sendChat(userId, message)` → 非流式 { reply }
   - `streamChat(userId, message, onDelta)` → 流式 (回调逐块)
   - 错误处理
2. Pinia store (`src/stores/chat.ts`):
   - state: messages, loading, error, userId
   - actions: send (调用 api), reset
3. 重构对话面板用 store (替代 T3/T4 的直接 API 调用)
4. 规范: 组件不直接调 API, 走 store

## 参考

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- 已装的 Pi packages 里可能有类似 (pi-hermes-memory 的 memory 等可参考)

## 如何定位参考文档

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- API: `server/src/routes/chat.ts`

## 说明

- 这是前端分层的关键 (API 层 + store)
- 后续知识库/Kanban 面板复用此分层
- 用 **implement** + tdd + code-review

## 依赖

- T4 (流式) 完成

## Log
