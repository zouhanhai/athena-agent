---
id: t9
title: "G1.S2.T9: 对话输入框多行优化"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: "opencode"
started_at: ""
blocked_by: []
acceptance_criteria:
  - "对话输入框支持多行输入 (textarea, 不只单行)"
  - "输入框稍宽一点, 高度可容纳多行"
  - "Enter 发送 / Shift+Enter 换行 (或用按钮发送)"
  - "布局与其他元素协调"
---

# G1.S2.T9: 对话输入框多行优化

## Task

优化 Chat 页底部的输入框——当前是窄长单行, 改为稍宽多行。

**现状**: ChatView.vue 底部用 `t-input` (单行), 布局是 flex 一行 (输入框+发送按钮)

**实现**:
1. 输入框改为 textarea (多行):
   - TDesign 用 `t-textarea` (或 t-input type=textarea)
   - 能显示多行内容
2. 宽度/高度调整:
   - 比当前稍宽一点, 高度 2-3 行 (auto-grow)
3. 发送体验:
   - Enter 发送, Shift+Enter 换行
   - 或保留发送按钮
4. 与发送按钮/布局协调

## 参考

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- 现有: `web/src/views/ChatView.vue`

## 如何定位参考文档

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- 对话页: `web/src/views/ChatView.vue`

## 说明

- 参考 Slack/Discord 等 IM 输入框的多行体验
- 用 **implement** + code-review

## 依赖

- 无 (独立增强, 可并行)

## Log

- 2026-08-05 opencode: T9 done
  - `ChatView.vue` 底部 composer: 单行 `t-input` 换成 `t-textarea` (多行)
  - 交互: Enter 发送 / Shift+Enter 换行 (`@keydown` 拦截 `Enter && !shiftKey` → preventDefault + send; Shift+Enter 让原生插入换行); 保留 Send 按钮
  - 布局: composer flex `align-items: flex-end` 按钮贴底; 输入框 `flex:1` 加宽 + inner textarea `min-height:66px / max-height:180px / resize:vertical` (2-3 行)
  - 注: 未用 `autosize` prop (TDesign autosize 在 jsdom 卸载时 `calcTextareaHeight` 抛未处理异常), 改用 CSS min/max-height
  - 测试 (tdd): chat.spec 选择器 `.composer-input input`→`textarea` 并新增 3 项 (textarea 多行 / Enter 发送并清空 / Shift+Enter 不发送保换行); 全量测试绿
