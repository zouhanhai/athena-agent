---
id: t4
title: "G1.S2.T4: SSE 流式对话"
layer: T
parent: G1.S2
owner: eng-director
status: done
assignee: opencode
started_at: 2026-08-05
blocked_by: ["t3"]
acceptance_criteria:
  - "前端消费后端 SSE 流式 (text/event-stream)"
  - "对话时逐块实时显示回答 (打字机效果)"
  - "流式结束处理 (done 事件)"
---

# G1.S2.T4: SSE 流式对话

## Task

让个人对话支持流式输出——体验流畅。

1. 在对话面板用 `fetch` + SSE 或 EventSource 消费流式:
   - 请求头: Accept: text/event-stream
   - body: { userId, message }
2. 解析 SSE 事件:
   - `data: {"delta":"..."}` → 逐块追加到回答
   - `data: {"done":true}` → 结束流
   - `data: {"error":"..."}` → 错误
3. 打字机效果: 边收到边显示
4. 流式中断/错误处理

## 参考

- Spec: `docs/kanban/G1/S2/Spec.md`
- Goal: `docs/kanban/G1/Goal.md`
- 后端: server/src/agents/stream.ts (SSE 实现)

## 如何定位参考文档

- `parent: G1.S2` → `docs/kanban/G1/S2/Spec.md`
- 后端流式: `server/src/agents/stream.ts`

## 说明

- 用 fetch + ReadableStream 解析 SSE (前端常用方式)
- EventSource 不支持 POST, 需用 fetch
- 用 **implement** + tdd

## 依赖

- T3 (对话面板) 完成

## Log

- 2026-08-05 opencode: 完成 SSE 流式对话
  - `web/src/api/sse.ts` (新): `consumeSSEStream(response, handlers)` — fetch + ReadableStream 解析 SSE (EventSource 不支持 POST)
    - 缓冲重组跨 chunk 边界的事件, 逐事件分发 `onDelta` / `onDone` / `onError`
    - 解析 `data: {"delta":"..."}` 逐块 / `{"done":true}` 结束 / `{"error":"..."}` 错误
  - `web/src/views/ChatView.vue`: 请求头加 `Accept: text/event-stream`, 消费流式回答逐块追加到 assistant 气泡 (打字机效果)
    - done 结束流; error 事件展示错误并清理空气泡; 网络/HTTP 错误兜底; 流式期间按钮禁用 + "Sending..."
  - `web/src/__tests__/sse.spec.ts` (新): 6 项解析器单测 (事件顺序 / done / error / 跨 chunk 重组 / 单 chunk 多事件 / 无 body 抛错)
  - `web/src/__tests__/chat.spec.ts` 重写为流式: 10 项组件测试 (Accept 头 / 打字机增量显示 / done 结束解锁 / SSE error 事件 / 网络错误 / 非 200 / 空消息不发)
  - 验证: vitest 23 项全绿, typecheck + build 通过; 真实 E2E: 起真实 Fastify 服务 (stub 流式 agent) + Vite proxy, curl 确认 SSE 帧 (`data: {"delta":...}` → `{"done":true}`) 经 :5173/api 逐块返回
  - 依赖: T3 完成; API 层抽离 + Pinia store 留待 T5
