---
id: s2.10
title: "G4.S2.T10: Remove LightRAG — decommission the LightRAG ingest stage, client, and track poller"
layer: T
parent: G4.S2
owner: consultant
assignee: opencode
session_id: ses_t10_opencode_0821
status: in_progress
milestone: M4
blocked_by: []
acceptance_criteria:
  - "No LightRAG references remain in server code/tests/deps"
  - "ingesting_lightrag stage removed from task pipeline"
  - "Server tests green + typecheck clean"
---

# G4.S2.T10: Remove LightRAG (decommission ingest stage + client)

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S2/Spec.md`).
3. **Use codegraph MCP**: `codegraph explore "lightrag"` before editing.
4. **Use `implement` + `tdd` skills**: TDD, write failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Background

G4.S2.T7 replaced LightRAG with the Neo4j lean RAG store for semantic search. LightRAG is now
fully decommissioned functionally, but its ingest stage + client remain in the codebase (dead code).
This ticket removes them.

## Task

Remove all LightRAG references from the server:
1. `ingesting_lightrag` stage in `tasks.ts` (enum `TaskStageName`, `initialStages`, the parallel
   ingest branch in `run`, `updateProgress`, `finalize` logic, `lightrag` field on task).
2. `lightrag.ts` client + `lightrag-track.ts` track poller + `LightRagTrackPoller`.
3. `ingest.ingestLightRag()` + the `lightrag` option on `KnowledgeIngestService`/queue.
4. Any LightRAG deps in `package.json`.
5. Update tests that reference LightRAG (remove/replace). Frontend `kb-api.spec.ts` etc.
6. Server tests green + typecheck clean. Empty graph/docs must still ingest via Neo4j + llm_wiki.

## Acceptance

- No LightRAG references remain; pipeline runs on Neo4j + llm_wiki only.

## Progress Log

| UTC timestamp | status | one-line progress |
| --- | --- | --- |
| 2026-08-11T16:21:00Z | in_progress | Claimed T10; deleted lightrag.ts + lightrag-track.ts + their tests |
| 2026-08-11T16:22:00Z | in_progress | Removed ingesting_lightrag stage/field/poller/nearDuplicate from tasks.ts; refiner File-B cleanup kept |
| 2026-08-11T16:23:00Z | in_progress | ingest.ts: dropped lightrag option, ingestLightRag, track/pipeline status, findNearDuplicate, LightRAG delete |
| 2026-08-11T16:24:00Z | in_progress | Rewired getGraph to Neo4j store (Neo4jRetrievalService.getGraph); removed LightRAG knowledge tools + capability declarations |
| 2026-08-11T16:25:00Z | in_progress | Updated app/agent/registry/page-context/routes wiring; rewrote verify scripts + comments |
| 2026-08-11T16:26:00Z | in_progress | Server tests (656) green + typecheck clean; web (347) green + typecheck clean |
| 2026-08-11T16:30:00Z | done | No LightRAG references remain in server code/tests/deps or web; pipeline runs on Neo4j + llm_wiki |
