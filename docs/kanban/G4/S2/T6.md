---
id: s2.6
title: "G4.S2.T6: Case-insensitive + bilingual alias search"
layer: T
parent: G4.S2
owner: consultant
assignee: opencode
session_id: 2b6a65c1-7d53-478d-8422-8cd243862a58
status: approved
milestone: M4
blocked_by: []
acceptance_criteria:
  - "See 'Acceptance' section"
---

# G4.S2.T6: Case-insensitive + bilingual alias search

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S2/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`).
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Task

Ensure node/label lookup is case-insensitive (LightRAG's `caleo`/`CALEO` bug not repeated) and that
bilingual aliases (T1) participate in keyword/BM25/full-text search.

## Steps

- Search folds case (search "zob münchen" matches "ZOB München").
- Aliases[] are included in full-text/BM25 index + queries.
- DE+EN query test: search German term → matches EN node; search EN → matches DE node.
- TDD: case-insensitive + alias search tests.

## Acceptance

- Case-insensitive node lookup + alias search both work; tests green.

## Progress Log

| UTC timestamp | status | one-line progress |
|---|---|---|
| 2026-08-11T06:13Z | in_progress | claimed (status/assignee/session_id 2b6a65c1) + kanban index regenerated + pushed |
| 2026-08-11T06:15Z | in_progress | context: Spec+Goal read; codegraph explore — case-insensitive + alias machinery already in place from T3-T5 (schema nameUpper RANGE + name+aliases FULLTEXT index, ingest stores aliases, Text2CypherRetriever folds query, Bm25 folds); missing: bilingual DE+EN acceptance tests (search DE term → EN node; EN → DE node) |
| 2026-08-11T06:25Z | in_progress | TDD GREEN: added 5 tests to store-retrieval.test.ts — lowercase 'zob münchen'→ZOB München, 'caleo'→CALEO (LightRAG bug regression), DE→EN 'Zentraler Omnibusbahnhof'→ZOB München via alias, EN→DE 'Munich Central Bus Station'→DE node via alias, fused search surfaces German query→EN node; 16/16 retrieval tests green |
| 2026-08-11T06:30Z | in_progress | live verify neo4j-spike: DE term 'zentraler omnibusbahnhof'→ZOB München (score 1.17), EN term 'munich central bus station'→DE node (2.17), lowercase 'zob münchen'→ZOB München (2.33) — all via entity_name_aliases_ftx |
| 2026-08-11T06:35Z | done | server 712/712 green (+5 new) + tsc clean; typecheck clean; ticket marked done |

