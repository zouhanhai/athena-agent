---
id: s2.9
title: "G4.S2.T9: Fix Neo4j vector search stalling (VectorRetriever Cypher hangs)"
layer: T
parent: G4.S2
owner: consultant
assignee: opencode
session_id: 73274282-f646-4329-b4b9-39e913e77bfe
status: in_progress
milestone: M4
blocked_by: []
acceptance_criteria:
  - "Neo4j vector search returns hits promptly (not hang) for a real query"
  - "Retrieval API surfaces neo4j source results alongside llmwiki"
---

# G4.S2.T9: Fix Neo4j vector search stalling (VectorRetriever Cypher hangs)

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S2/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`).
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Observed (2026-08-10, T5 validation)

T5's `VectorRetriever.search` / `Neo4jRetrievalService.search` **hangs** (no response in 60-90s) when run
against the live neo4j-spike container. Repro:
- `svc.search("ZOB München")` timed out (>60s) via `defaultNeo4jRetrieval()`.
- `VectorRetriever.search` with a fake embedding also timed out (>60s) — so the hang is in the **Cypher
  vector search**, not the embedder.
- Embedding itself is fast (3.9s, 4096-dim).
- Retrieval API `/api/kb/search` returns only `llmwiki` results (the Neo4j search hangs → allSettled
  drops it), so the neo4j source never surfaces.

Neo4j now has 15 Chunks + 14 Entities (real Infos doc ingested via T4). T3's schema + index were
"live-verified" in the spike, so likely the **Cypher SEARCH/VECTOR INDEX clause** is wrong for the real
data shape (e.g. index name, property, or the `SEARCH … IN (VECTOR INDEX …)` syntax at runtime), causing
the driver call to block or error-retry.

## Task

- Fix `VectorRetriever.search` (server/src/kb/store/retrieval.ts) so the Cypher vector search runs
  promptly against neo4j-spike.
- Verify against the live container: run the real query, confirm hits returned in <10s.
- Confirm `/api/kb/search` then surfaces `neo4j` source results alongside `llmwiki`.
- Server tests green + typecheck clean.

## Acceptance

- Neo4j vector search returns promptly; retrieval API shows neo4j results.
