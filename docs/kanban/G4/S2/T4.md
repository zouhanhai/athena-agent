---
id: s2.4
title: "G4.S2.T4: Ingest — Athena output → Neo4j (embed + index, no LLM)"
layer: T
parent: G4.S2
owner: consultant
assignee: opencode
session_id: 15484a19-a0b2-472a-8ea4-8e15fd97f547
status: approved
milestone: M4
blocked_by: []
acceptance_criteria:
  - "See 'Acceptance' section"
---

# G4.S2.T4: Ingest — Athena output → Neo4j (embed + index, no LLM)

## Progress Log

| UTC timestamp | status | one-line progress |
| --- | --- | --- |
| 2026-08-10T18:47:20Z | in_progress | Claimed T4 (15484a19-...); read S2 Spec + G4 Goal + TICKET-WORKFLOW + ADR-0008 + S1.T4/T6; codegraph-explored schema/ingest/tasks/refiner; plan: store/ingest.ts (embed+index) + embedding.ts (qwen3) + driver.ts + ingesting_neo4j stage parallel to llm_wiki |
| 2026-08-10T18:55:00Z | in_progress | TDD RED→GREEN: store-ingest.test.ts 6/6 (schema applied, Chunk w/ vector+topic+heading_path namespaced by doc id, Entity w/ aliases+nameUpper, RELATION edge by folded endpoints, Document node, MERGE-idempotent) + embedding/driver tests 5/5 (OpenRouter /embeddings, EMBEDDING_OPENROUTER_KEY, env config) |
| 2026-08-10T21:15:00Z | in_progress | Pipeline: ingesting_neo4j stage in tasks.ts (embed_store step) runs in parallel with llm_wiki; consumes refinementRef (RefineOutputRef) + documentId + title; no-op when store not wired or no Athena output; per-system failure; progress band + retry + finalize include it; wired defaultTaskQueue via defaultNeo4jIngest (NEO4J_PASSWORD-gated); 4 new tasks tests (parallel flow, per-system fail, no-op, all-fail) → 689/689 server green |
| 2026-08-10T21:35:00Z | in_progress | Web: TaskStageName + stages type + initialStages embed_store step + UploadsView Neo4j (RAG) stage + fixtures → 348/348 web green + both typechecks clean |
| 2026-08-10T21:50:00Z | in_progress | Live smoke vs neo4j-spike (bolt 7687): ingest stores 2 chunks w/ 1024-dim vectors + Document + Entity aliases + RELATION edges; vector SEARCH scores 1.0 top hit; fulltext DE alias 'zentraler omnibusbahnhof' → ZOB München; case-insensitive nameUpper lookup; re-ingest idempotent (no dup chunks); cleaned smoke data; replaced stale 8-dim spike vector index with real 1024-dim schema index |
| 2026-08-10T22:00:00Z | done | Review: added Document.keywords (spec: keyword search uses Athena injected keywords); final verification server 689/689 + typecheck, web 348/348 + typecheck, codegraph sync; status done |

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S2/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`).
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Task

Wire the ingest path so G4.S1's refinement output (chunks + entities + relations + keywords + topic)
is embedded and stored into Neo4j — NO LLM extraction here, just embedding + index.

## Steps

- Ingest service: take `RefineOutputRef` (chunks_ref, entities, relations, keywords, frontmatter).
- Embed each chunk (qwen3-embedding via `EMBEDDING_OPENROUTER_KEY`) → store Chunk with vector.
- Store Entity/Relation/Document (+ aliases).
- Reuse the pipeline integration (G4.S1.T4) so refinement output flows to Neo4j in parallel with
  llm_wiki.
- TDD: ingest test (given refinement output → Neo4j has chunks+entities+relations).

## Acceptance

- Ingest stores Athena output into Neo4j with embeddings, no LLM extraction.

