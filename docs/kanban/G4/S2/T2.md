---
id: s2.2
title: "G4.S2.T2: Neo4j spike — deploy + validate vector/search (decision record)"
layer: T
parent: G4.S2
owner: consultant
status: backlog
milestone: M4
blocked_by: []
acceptance_criteria:
  - "See 'Acceptance' section"
---

# G4.S2.T2: Neo4j spike — deploy + validate vector/search (decision record)

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S2/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`).
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Task

Validate Neo4j 2026 Community in Docker on 6900XT before committing to self-build. Confirm it meets
the retrieval needs: vector index, Cypher SEARCH, in-index filters, case-insensitive lookup.

## Steps

- Run Neo4j 2026 Community in Docker on 6900XT (ports + auth).
- Verify `CREATE VECTOR INDEX` (HNSW, cosine) on a test `Chunk` node set.
- Verify Cypher `SEARCH` clause + `SEARCH…WHERE` (in-index filters by topic).
- Verify case-insensitive node lookup (toCUpper / toLower on indexed props).
- Write a decision record in `docs/adr/` (self-build vs keep LightRAG) with the spike results.

## Acceptance

- Neo4j runs; vector + SEARCH + filter + case-insensitive verified with a tiny dataset.
- ADR records the decision (self-build confirmed or not).

