---
id: G4.S7.T12
title: "Session switcher — per-user chat session picker (recent 10, resume) + new-chat button, UI in the agent-card area"
owner: eng-director
status: approved
layer: T
parent: G4.S7
assignee: opencode
session_id: ses_fe15bad14ffei3xRWxwgp65iRt
started_at: 2026-08-20
blocked_by: []
acceptance_criteria:
  - "DB: chat_messages rows gain a session_id column; every message belongs to a session; per user, the 10 most recent sessions are listed (each with a title/derived label + timestamp + message count)"
  - "API: GET /api/chat/sessions?userId= returns the user's 10 most recent sessions (id, title, created/updated, message count); POST /api/chat starts a NEW session by default OR resumes an existing one via session_id in the body"
  - "Server persists messages with session_id; sessions update their last-activity timestamp when a message arrives"
  - "Frontend: in the agent-card area (participants region of the chat panel), a session picker UI — a dropdown/list of the user's 10 recent sessions + a \"
  - "Restoring a session loads that session's messages (GET /api/chat/history?userId=&session_id=) into the store; switching sessions clears the current view; New chat starts an empty conversation"
  - "Server + web tests green, typecheck clean"
---

# G4.S7.T12 — Session switcher (resume-style, 10 recent sessions per user)

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read parent Spec (`docs/kanban/G4/S7/Spec.md`) + Goal + T10 (history
   pipeline) + T11 (history persistence baseline, which this ticket extends). Code:
   `server/src/agents/chat-history.ts` (current save/list, added in the T11 followup),
   `server/src/routes/chat.ts`, `web/src/stores/chat.ts`, `web/src/api/chat.ts`,
   `web/src/components/GlobalChatPanel.vue` + its agent-card / participant UI area.
2. **Use codegraph MCP**: explore the chat store + route + frontend chat store.
3. **Use `implement` + `tdd`**: TDD (RED-GREEN-REFACTOR), write failing test first.
4. **Milestone report**: add a semantic Progress Log row when a milestone is complete.
5. **Commit convention**: feature-level English commits; `codegraph sync` after.
6. **Verify + mark done**: tests green → `status: done` → commit + push (both origin and caleo).

## Context

Yesterday we added per-user chat persistence (`chat_messages`, G4.S7.T11-followup): every
message is saved per employee and the frontend restores the WHOLE conversation on load.

Now the user wants a **session switcher** like Hermes TUI's `/resume`:
- Each user keeps up to **10 recent chat sessions** to pick from.
- Picking one resumes that conversation (loads its messages).
- A "New chat" action starts a fresh empty conversation.
- The picker UI goes in the **agent-card area** of the chat panel (the region where agent
  participant cards live), not a separate modal.

This is a schema + API + frontend feature on top of the existing chat_messages table.

## Task

### A. DB / store (`server/src/agents/chat-history.ts` + migration)

1. Add `session_id` column to `chat_messages` (TEXT, default '' for legacy rows — migration:
   lazy additive ALTER like the rest of the codebase; legacy rows with ''=today's flat history
   may be treated as one legacy session or excluded from the picker, pick one and document).
2. New table `chat_sessions` (or a session row per session_id):
   - `session_id TEXT PRIMARY KEY`
   - `employee_id TEXT NOT NULL`
   - `title TEXT` (derived: first user message or "New chat"), maybe `created_at`, `updated_at`
   - Indexed for per-employee ordering by updated_at DESC.
3. Store methods:
   - `createSession(userId, title?) -> session_id`
   - `listSessions(userId, limit=10) -> { session_id, title, created_at, updated_at, message_count }[]`
   - `ensureSession(userId, sessionId) -> session exists`
   - `touchSession(userId, sessionId)` called when a message lands so updated_at refreshes
   - `saveMessage` gains `sessionId` (message row carries it)
   - `listMessages(userId, sessionId?, limit)` — when sessionId is given, filter to that session.
4. DB migration: keep lazy-CREATE + additive-ALTER pattern, idempotent, safe on existing DBs.

### B. Server routes (`server/src/routes/chat.ts`)

1. `GET /api/chat/sessions?userId=` → `{ sessions: [...] }` (max 10, most recent first).
2. `POST /api/chat` body gains optional `session_id`:
   - When absent → server creates a NEW session automatically (new conversation).
   - When present → messages persist under that session; 404/400 if the session does not
     belong to the user (or auto-create if you prefer — pick + document).
3. `GET /api/chat/history?userId=&sessionId=` → messages ONLY for that session.
4. Persisted turns carry session_id (both the user and assistant rows).

### C. Frontend (`web/src/stores/chat.ts`, `api/chat.ts`, GlobalChatPanel.vue)

1. Store state:
   - `sessions: ChatSession[]` (10 recent), `activeSessionId: string | null`.
   - `loadSessions()` action → GET /api/chat/sessions → fills the list (no message fetch).
   - `pickSession(id)` → set active, GET /api/chat/history?sessionId= → restore messages.
   - `newChat()` → clear messages, clear activeSessionId, create session on server (or lazily
     on first message), then the first message opens a new session.
   - `send()` passes `session_id` (or omits it when starting fresh) — align with server.
2. **UI placement**: the picker + new-chat live in the AGENT-CARD AREA of the chat panel
   (the region where participant cards are rendered, e.g. top of the participant list /
   next to the agent cards). A compact session dropdown/menu: recent 10 with title + relative
   time + message count, plus "New chat" item. Keep it visually consistent with the existing
   card styling (same component family, not a global sidebar).
3. Initial load: after sign-in, load sessions list (cheap) but do NOT auto-restore the last
   session's messages unless the UI already did (decide: start at "New chat" empty state by
   default, or restore most recent — mirror Hermes /resume which shows the list and lets the
   user pick; do NOT block the chat on a fetch of a full 200-message conversation).

### D. Tests

- Store: session create/list/touch; messages carry session_id; listMessages filters by
  session; legacy ''-session rows don't break listing.
- Route: GET sessions (most recent 10, capped); POST /api/chat with session_id persists under
  the session + touch updates updated_at; without session_id → new session created; GET
  history?sessionId filters.
- Web: store loadSessions/pickSession/newChat wiring; UI presence in the agent-card area
  (chat.spec / shallow DOM test).

## Acceptance

1. A user with messages across multiple sessions sees their 10 most recent in the picker
   (server test asserts ordering + count cap).
2. POST with session_id lands in that session's history and its updated_at refreshes; without
   it a fresh session_id is created (tests assert).
3. Frontend picker renders in the agent-card area; picking a session restores ONLY that
   session's messages; New chat starts empty.
4. `npm test` green on 6900XT + web vitest green; `npx tsc --noEmit` clean.

## Notes

- The persistence baseline (T11-followup) currently has ONE flat conversation per user.
  This ticket's migration must not lose it: legacy rows (session_id='') should appear as
  ONE session in the picker (title "Previous chat" or derived from first message) OR be
  folded into the user's current session — pick one and document in the Progress Log.
- Keep the API additive: old clients that POST without session_id still work (server creates
  a session for them).
- The picker is in the agent-card area per the user's explicit UI request — respect that
  placement; do not invent a new global chrome.
- Don't auto-load 200 messages on page load for the sessions list — loadSessions is a cheap
  `{id,title,time,count}` list only; full message restore happens only when the user picks.
- Remember to push BOTH remotes (origin + caleo) and sync-github after touching docs.

## Progress Log
| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-20T10:53:26.267Z | in_progress | ran bash |
| 2026-08-20T10:52:37.033Z | in_progress | ran bash |
| 2026-08-20T10:51:25.787Z | in_progress | ran bash |
| 2026-08-20T10:47:39.299Z | in_progress | ran edit |
| 2026-08-20T12:45:00.000Z | done | opencode implemented G4.S7.T12 session switcher via TDD: DB gains chat_sessions (session_id PK, employee_id, title, created/updated_at, employee+updated_at index) + additive session_id column on chat_messages (chat_messages_session_created_idx); store adds createSession/listSessions(10, most-recent-first + message_count)/ensureSession/touchSession + saveMessage(sessionId) + listMessages(sessionId) filter; legacy ''-rows folded into ONE virtual 'Previous chat' session. Routes: GET /api/chat/sessions (10-cap, per-user), POST /api/chat resolves session (absent→auto-create w/ title from first msg; present→ownership-checked, 404 if not yours) + echoes session_id (non-stream body + first SSE frame) + touches updated_at; GET /api/chat/history?sessionId filters. Web: fetchChatSessions/fetchChatHistory(sessionId)/postChat session_id + SSE onSessionId; store sessions+activeSessionId, loadSessions/pickSession/newChat, send passes active session_id + captures streamed one; sign-in now loadSessions (starts New chat, no auto full-load). UI: compact session dropdown in the agent-card area (title + msg count + relative time + New chat). Server + web chat tests green (994 server / 504 web), tsc + vue-tsc clean. Legacy decision: ''-session rows appear as ONE 'Previous chat' picker session (documented) |
| 2026-08-20T10:47:01.637Z | in_progress | ran bash |
| 2026-08-20T10:46:20.549Z | in_progress | ran bash |
| 2026-08-20T10:45:07.527Z | in_progress | ran bash |
| 2026-08-20T10:43:51.605Z | in_progress | ran bash |
| 2026-08-20T10:43:18.744Z | in_progress | ran bash |
| 2026-08-20T10:42:39.823Z | in_progress | ran read |
| 2026-08-20T10:42:08.627Z | in_progress | ran bash |
| 2026-08-20T10:41:34.372Z | in_progress | ran bash |
| 2026-08-20T10:40:54.401Z | in_progress | ran edit |
| 2026-08-20T10:40:22.249Z | in_progress | ran edit |
| 2026-08-20T10:39:47.123Z | in_progress | ran edit |
| 2026-08-20T10:39:08.841Z | in_progress | ran edit |
| 2026-08-20T10:38:37.075Z | in_progress | ran edit |
| 2026-08-20T10:37:58.754Z | in_progress | ran edit |
| 2026-08-20T10:37:21.350Z | in_progress | ran bash |
| 2026-08-20T10:36:45.633Z | in_progress | ran edit |
| 2026-08-20T10:36:12.089Z | in_progress | ran bash |
| 2026-08-20T10:35:38.958Z | in_progress | ran edit |
| 2026-08-20T10:34:56.327Z | in_progress | ran edit |
| 2026-08-20T10:34:17.059Z | in_progress | ran write |
| 2026-08-20T10:33:19.291Z | in_progress | ran bash |
| 2026-08-20T10:32:28.857Z | in_progress | ran bash |
| 2026-08-20T10:31:57.413Z | in_progress | ran write |
| 2026-08-20T10:31:17.245Z | in_progress | ran grep |
| 2026-08-20T10:30:44.413Z | in_progress | ran read |
| 2026-08-20T10:30:14.147Z | in_progress | ran grep |
| 2026-08-20T10:29:37.008Z | in_progress | ran skill |
| 2026-08-20T10:28:43.587Z | in_progress | ran read |
| 2026-08-20T10:27:45.336Z | in_progress | opencode claimed G4.S7.T12 (session ses_fe15bad14ffei3xRWxwgp65iRt) |
| (no rows yet — worker adds rows as it works) |

## Log

[2026-08-20] opencode claimed G4.S7.T12 (session ses_fe15bad14ffei3xRWxwgp65iRt)

[2026-08-20] opencode implemented G4.S7.T12 via TDD (RED first): session switcher — chat_sessions table + additive session_id column on chat_messages (legacy '' rows = ONE virtual "Previous chat" session, documented); store createSession/listSessions(10, most-recent-first + message_count)/ensureSession/touchSession + session filtering. Routes: GET /api/chat/sessions (per-user, capped 10), POST /api/chat resolves session (absent → auto-create NEW with title from first user msg; present → ownership-checked 404), echoes session_id (non-stream body + first SSE frame), touches updated_at on each message; GET /api/chat/history?sessionId scoped. Web: fetchChatSessions/fetchChatHistory(sessionId)/postChat session_id, SSE onSessionId dispatch; store sessions + activeSessionId + loadSessions/pickSession/newChat, send passes the active session (or omits for a fresh one) + captures the streamed new id; sign-in loads sessions (starts at "New chat" empty state, no auto full-conversation fetch per the user's resume-style request). UI: compact session dropdown in the AGENT-CARD area (title + msg count + relative time) + a "New chat" item, styled to match the card family (not a global sidebar). Tests: store/message/session + route (sessions cap/order/ownership/history filter/SSE frame/legacy) + web sse/store/UI — all green; server tsc + web vue-tsc clean. NOTE: pre-existing env-dependent failures unrelated to this ticket remain on this dev box (settings-view.spec 7, server agents/retrieval 3 needing live keys); the full session feature suite passes.
