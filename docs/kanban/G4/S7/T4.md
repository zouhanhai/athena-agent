---
id: G4.S7.T4
title: "Agent bidirectional connection (reverse WebSocket) — agent actively connects INTO platform WS server; platform drives it back through the tunnel (push tasks, stream tool.started/completed + results)"
owner: eng-director
status: in_progress
layer: T
parent: G4.S7
assignee: opencode
session_id: ses_ff5de6648ffeiDq75lsnBlSE85
started_at: 2026-08-16
blocked_by:
  - "G4.S7.T2"
acceptance_criteria:
  - "Platform exposes a WebSocket server endpoint (agent connects INTO it); agent initiates the outbound WS connection and registers {agent_id, token}"
  - "Communication is bidirectional over the reverse tunnel: platform pushes tasks/commands to the agent, agent streams tool.started/tool.completed + results back"
  - "Agent works behind NAT/CGNAT with no public IP (outbound connect); platform reaches it through the established tunnel"
  - "The selected remote agent's tasks (Hermes /v1/chat/completions, SSE) are driven over the connection"
  - "Server + web tests green, typecheck clean; live-verified on 6900XT"
---

# G4.S7.T4 — Agent bidirectional connection (reverse WebSocket)

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read parent Spec (`docs/kanban/G4/S7/Spec.md` §Architecture, §Chat routing) + Goal +
   `docs/remote-agent-connectivity.md` (reverse-WebSocket decision + references). Code:
   `server/src/routes/chat.ts` (existing SSE), `web/src/` Chat panel, `server/src/agents/registry.ts`
   (api_url + agent from T2).
2. **Use codegraph MCP**: explore the chat route + agent registry + any WS support.
3. **Use `implement` + `tdd`**: TDD, write failing test first.
4. **Milestone report**: add a semantic Progress Log row when a milestone is complete.
5. **Commit convention**: feature-level English commits.
6. **Verify + mark done**: tests green + live-verified on 6900XT → `status: done` → commit + push.

## Context

The AgentIDE vision: users manage remote agents from the platform. The decided connectivity model
(2026-08-15) is **reverse WebSocket**: the agent actively connects INTO the platform's WS server
(outbound, works behind NAT/CGNAT, no public IP, no admin), and the platform drives the agent back
through that tunnel. This replaces the earlier HTTP+SSE-only design. Depends on T2 (agent registers with
api_url/token + capabilities).

## Task

1. **Platform WS server**: expose a WebSocket endpoint that remote agents connect INTO (outbound).
   - Agent initiates the connection + registers `{agent_id, token}` (auth'd, per T2).
   - On connect, platform records the agent as reachable + keeps the tunnel.
2. **Bidirectional**: platform pushes tasks/commands to the agent through the tunnel; agent streams
   `tool.started` / `tool.completed` + result deltas back.
   - Hermes agent tasks: `POST /v1/chat/completions` (SSE) — driven over the connection.
3. **Behind NAT**: agent connects outbound (no public IP / no inbound port); platform reaches it via the
   established tunnel. Handle disconnect/reconnect (agent re-connects, resumes).
4. **Chat panel**: route a chat to a selected registered agent; stream tool progress + result into the
   panel (uses the reverse connection).
5. **Tests**: server (WS endpoint, bidirectional, reconnect, auth) + web (agent selector + progress rows).

## Acceptance

- Platform WS server accepts agent-outbound connections; agent registers + stays connected.
- Bidirectional: platform pushes tasks, agent streams tool.started/completed + results back.
- Works behind NAT/CGNAT (no public IP). Reconnect handled. Tests green, typecheck clean, live on 6900XT.

## Notes

- Depends on T2 (api_url/token/capabilities). Replaces earlier HTTP+SSE-only Chat routing.
- Reaches the agent over the reverse tunnel (see `docs/remote-agent-connectivity.md`).

## Progress Log
| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-16T10:33:18.823Z | in_progress | opencode claimed G4.S7.T4 (session ses_ff5de6648ffeiDq75lsnBlSE85) |

## Log

[2026-08-16] opencode claimed G4.S7.T4 (session ses_ff5de6648ffeiDq75lsnBlSE85)
