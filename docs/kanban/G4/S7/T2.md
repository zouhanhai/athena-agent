---
id: G4.S7.T2
title: "Agent onboarding via invitation — extend AgentRecord with api_url/token + invite flow (admin generates {agent_id, api_url, token} → agent registers auth'd); identity + reachability tracked"
layer: T
parent: G4.S7
owner: eng-director
status: backlog
assignee: ""
session_id: ""
priority: P1
depends_on: [G4.S7.T1]
blocked_by: [G4.S7.T1]
acceptance_criteria:
  - "AgentRecord model + DB schema gain remote fields: api_url, token (invitation-issued), agent_id"
  - "Invitation flow: admin generates {agent_id, api_url, token} invite → hands to agent → agent registers (auth'd, so platform knows which agent is where/how to reach it)"
  - "Existing POST /api/agents + manual Register form (G3.S2.T9) extended to carry the remote/api_url fields"
  - "Agent identity traceable: each agent connects with unique id + token; platform knows agent location + reachability"
  - "Server + web tests green, typecheck clean"
---

# G4.S7.T2 — Agent onboarding via invitation (remote api_url/token)

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read parent Spec (`docs/kanban/G4/S7/Spec.md`) + Goal. Code: `server/src/agents/registry.ts`
   (AgentRecord + POST /api/agents), `server/src/routes/agents.ts`, `server/src/db/` (schema),
   `web/src/` Agents panel, `docs/kanban/G3/S2/T9.md` (manual register form).
2. **Use codegraph MCP**: `codegraph explore "agents registry AgentRecord invite register api_url token"` before editing.
3. **Use `implement` + `tdd`**: TDD, write failing test first.
4. **Milestone report**: add a semantic Progress Log row when a milestone is complete.
5. **Commit convention**: feature-level English commits.
6. **Verify + mark done**: tests green → `status: done` → commit + push.

## Context

S7's AgentIDE vision: users manage remote agents from the platform. Each agent stays local; the platform
is the control plane. For the platform to control an agent it needs to know the agent's identity +
reachability. This ticket extends the existing AgentRecord (POST /api/agents + manual register form,
G3.S2.T9) with **invitation-based onboarding**: admin generates `{agent_id, api_url, token}`, the agent
registers auth'd, and the platform stores where/how to reach it. (G3.S2.T9's form should be designed
with this, not standalone.)

## Task

1. **DB schema + model**: extend AgentRecord/`agents` table with remote fields: `api_url`, `token`,
   `agent_id` (unique). Migration for existing rows (nullable / defaults).
2. **Invitation flow**: admin endpoint to generate an invite `{agent_id, api_url, token}`; a
   register/accept endpoint where the agent posts its api_url + token (auth'd) and the platform records
   reachability + status.
3. **Extend POST /api/agents + web Register form** (G3.S2.T9) to carry the remote/api_url/token fields.
4. **Agent identity**: each agent unique id + token; platform stores location + reachability; UI shows
   agent status (registered / reachable / unknown).
5. **Tests**: server + web for the invitation flow + remote fields.

## Acceptance

- AgentRecord/schema has api_url/token/agent_id; existing manual register carries them.
- Invitation flow works: admin generates invite, agent registers auth'd, platform records reachability.
- Agent identity traceable. Tests green, typecheck clean.

## Notes

- Depends on T1 (Tailscale — agents are reached over the tunnel). Part of G4.S7.
- The Chat routing (T4) consumes the api_url to reach the agent.

## Progress Log
| UTC timestamp | status | progress |
|---|---|---|
