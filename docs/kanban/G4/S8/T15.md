---
id: G4.S8.T15
title: "G4.S8.T15: weekly knowledge-base audit (weekend scheduler) — confidence re-review + file re-check + orphan refinement sweep"
owner: consultant
status: approved
layer: T
parent: G4.S8
assignee: opencode
session_id: ses_fda8de039ffeMnTHsvs2YTbX43
started_at: 2026-08-21
blocked_by:
  - "G4.S8.T14"
acceptance_criteria:
  - "In-server weekly scheduler runs the knowledge-base audit once per weekend (configurable day+time via env, default Sunday 03:00; survives server restarts by computing next-run from persisted last-run state, no duplicate run within the same week)"
  - "Audit stage 1 — review pass: runs the existing KB review service (reviewAll: re-topic / re-classify / deprecate / reinforce) over every wiki page and applies confidence re-approval"
  - "Audit stage 2 — file re-check: every WikiPage node's path must exist on disk and every wiki *.md file must have a WikiPage node; inconsistencies are repaired using the T14 cascade primitives (stale graph subtree removed, missing graph records reported)"
  - "Audit stage 3 — orphan refinement sweep: refinement directories with mtime older than the grace period (env-configurable, default 48h) that are referenced by NO Document.md_ref AND have no same-stem live wiki page are deleted; in-flight ingests are protected by the grace period; each sweep logs the removed dir list"
  - "Manual trigger: POST /api/kb/audit (admin auth) runs the same audit on demand and returns the full report JSON; EVERY run — scheduled or manual — persists an identical report row (one per run: started_at, durationMs, trigger 'scheduled'|'manual', per-stage summary) via GET /api/kb/audit/reports"
  - "Admin UI: the Admin console (admin-gated) gains a Knowledge-Base Audit section — Run-audit-now button (POST /api/kb/audit, spinner while running, 409 concurrent handled), latest report summary (per-stage: review changes / file repairs / orphans removed), and a reports history table from GET /api/kb/audit/reports showing ALL runs with a trigger badge (scheduled/manual) so weekend automatic audits are visible exactly like manual ones"
  - "CodeBrowserView delete affordance: each object row whose entity resolves to a source wiki page (existing MENTIONED_IN→Chunk→document→wikiPath resolution from T12) gains a delete action that calls DELETE /api/kb/doc with the resolved page path (T14 cascade: graph subtree + orphan entities + refinement dir) — entity-level deletion is FORBIDDEN (shared nameUpper entities span documents); after delete the list refreshes and entries whose page no longer exists lose their link automatically"
  - "Grace period + schedule + enable/disable are env-configurable (KB_AUDIT_DAY, KB_AUDIT_HOUR, KB_ORPHAN_GRACE_HOURS, KB_AUDIT_ENABLED default true)"
  - "Tests: scheduler next-run computation (no duplicate within week, restart persistence), stage-3 sweep removes only qualifying dirs (grace-period protection, md_ref protection), manual trigger endpoint admin-gated; web vitest for the Admin audit section (run button states, report rendering); full npm test green + vitest green + typecheck clean"
---

# G4.S8.T15: weekly knowledge-base audit + orphan refinement sweep

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then commit + push. Claim lock.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S8/Spec.md`) +
   this ticket's Design below. Read the actual files to confirm current shape before editing.
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing
   (start with `server/src/kb/review.ts` (existing reviewAll), `server/src/kb/store/ingest.ts`,
   `server/src/kb/ingest.ts`, `server/src/index.ts`).
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green → `status: done` + Progress Log → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Context (2026-08-21, user requirement)

User requirement (verbatim intent): “不用定时清扫。把这个和 Athena 定时审核结合起来——athena
应该每个星期周末的时候开始一次知识数据库的审核，比如重新审批置信度、重新检查各个文件，
这个时候正好可以进行孤儿清理。” — i.e. NO standalone sweeper process: the orphan
refinement sweep becomes ONE STAGE of a scheduled weekly knowledge-base audit.

Building blocks that already exist:
- KB review service (`reviewAll`: re-topic / re-classify / deprecate / reinforce over all
  pages — G4.S3.T2) with confidence handling.
- T14's cascade primitives (graph subtree removal by documentId/md_ref) for repairing
  stale graph records.
- Refinement output root (`refinement/<stem>/markdown.md`) with Document.md_ref as the
  authoritative reference (T11/T14 conventions).

## Design

### 1. Scheduler (in-server, no external cron)

A small `KbAuditScheduler` wired in `src/index.ts`:

- Config via env: `KB_AUDIT_ENABLED` (default true), `KB_AUDIT_DAY` (default "sunday"),
  `KB_AUDIT_HOUR` (default 3, local time), `KB_ORPHAN_GRACE_HOURS` (default 48).
- Computes the next occurrence and sets a timeout; after each run re-computes the next.
  Persist `last_run_started_at` (Postgres table `kb_audit_runs` also serves report
  storage) so a restart mid-week never double-runs within the same configured window.
- Missed window while the server was down: run once at startup if the last successful run
  is older than the configured week.

### 2. Audit pipeline (three stages, one report)

```ts
interface KbAuditReport {
  trigger: "scheduled" | "manual";
  startedAt: string; durationMs: number;
  review:    ReviewAllResult;          // stage 1 (existing service)
  fileCheck: { repaired: number; details: string[] };   // stage 2
  orphans:   { scannedDirs: number; removed: string[]; kept: string[] }; // stage 3
}
```

- **Stage 1 — review/confidence**: call the existing `reviewAll({})`.
- **Stage 2 — file/graph consistency**: diff WikiPage nodes vs disk *.md files
  (both directions). Stale graph subtree → remove via the T14 cascade primitives;
  missing graph record → report (do NOT auto-ingest).
- **Stage 3 — orphan refinement sweep**: list refinement output root dirs; delete a dir
  iff `age > KB_ORPHAN_GRACE_HOURS` AND no `Document.md_ref` references it AND no live
  wiki page shares its stem. Every removal is logged with its path.

### 3. Manual trigger + reports

- `POST /api/kb/audit` (admin permission): runs the same pipeline immediately (returns
  the report; concurrent invocations are rejected with 409 while one is running).
- `GET /api/kb/audit/reports?limit=` : recent persisted reports from `kb_audit_runs`.

### 4. Non-goals

- No LLM-driven content rewriting beyond what reviewAll already does.
- No deletion of QaPairs / semantic_mappings (terms) — user-maintained data.
- No standalone sweeper process — the weekly audit is the ONLY orphan sweep trigger
  besides the real-time T14 cascade.

## Acceptance (repeated)

- `npm test` full green on 6900XT.
- Fixture: refinement dir referenced by a Document (kept), unreferenced dir newer than
  grace (kept), unreferenced dir older than grace (removed); scheduler test proves
  single-run-per-week across a simulated restart.

## Progress Log
| 2026-08-21T19:10:34.149Z | in_progress | ran bash |
| 2026-08-21T19:08:37.394Z | in_progress | ran edit |
| 2026-08-21T19:07:59.512Z | in_progress | ran bash |
| 2026-08-21T19:07:18.685Z | in_progress | ran bash |
| 2026-08-21T19:06:26.197Z | in_progress | ran bash |
| 2026-08-21T19:04:32.343Z | in_progress | ran edit |
| 2026-08-21T19:01:26.436Z | in_progress | ran edit |
| 2026-08-21T19:00:47.664Z | in_progress | ran edit |
| 2026-08-21T19:00:10.716Z | in_progress | ran bash |
| 2026-08-21T18:59:30.744Z | in_progress | ran read |
| 2026-08-21T18:58:50.951Z | in_progress | ran bash |
| 2026-08-21T18:58:10.259Z | in_progress | ran bash |
| 2026-08-21T18:57:30.310Z | in_progress | ran bash |
| 2026-08-21T18:56:59.839Z | in_progress | ran bash |
| 2026-08-21T18:56:21.133Z | in_progress | ran edit |
| 2026-08-21T18:55:39.983Z | in_progress | ran write |
| 2026-08-21T18:55:01.363Z | in_progress | ran edit |
| 2026-08-21T18:54:26.607Z | in_progress | ran read |
| 2026-08-21T18:53:56.597Z | in_progress | ran edit |
| 2026-08-21T18:53:14.729Z | in_progress | ran edit |
| 2026-08-21T18:52:35.954Z | in_progress | ran bash |
| 2026-08-21T18:52:04.687Z | in_progress | ran bash |
| 2026-08-21T18:45:39.827Z | in_progress | ran bash |
| 2026-08-21T18:44:37.246Z | in_progress | ran bash |
| 2026-08-21T18:43:54.438Z | in_progress | ran bash |
| 2026-08-21T18:43:18.087Z | in_progress | ran bash |
| 2026-08-21T18:42:47.399Z | in_progress | ran bash |
| 2026-08-21T18:36:30.573Z | in_progress | ran bash |
| 2026-08-21T18:34:11.963Z | in_progress | ran bash |
| 2026-08-21T18:32:59.174Z | in_progress | ran bash |
| 2026-08-21T18:31:25.398Z | in_progress | ran edit |
| 2026-08-21T18:30:54.515Z | in_progress | ran bash |
| 2026-08-21T18:29:40.934Z | in_progress | ran bash |
| 2026-08-21T18:28:33.925Z | in_progress | ran bash |
| 2026-08-21T18:27:01.136Z | in_progress | ran bash |
| 2026-08-21T18:24:50.073Z | in_progress | ran edit |
| 2026-08-21T18:24:06.358Z | in_progress | ran write |
| 2026-08-21T18:23:21.490Z | in_progress | ran bash |
| 2026-08-21T18:22:14.019Z | in_progress | ran edit |
| 2026-08-21T18:21:43.645Z | in_progress | ran edit |
| 2026-08-21T18:21:10.575Z | in_progress | ran edit |
| 2026-08-21T18:20:37.865Z | in_progress | ran edit |
| 2026-08-21T18:19:55.472Z | in_progress | ran edit |
| 2026-08-21T18:19:15.776Z | in_progress | ran write |
| 2026-08-21T18:11:45.892Z | in_progress | ran write |
| 2026-08-21T18:10:33.801Z | in_progress | ran write |
| 2026-08-21T18:08:21.643Z | in_progress | ran write |
| 2026-08-21T18:07:46.119Z | in_progress | ran todowrite |
| 2026-08-21T17:53:50.323Z | in_progress | ran bash |
| 2026-08-21T17:53:15.978Z | in_progress | ran codegraph_codegraph_explore |
| 2026-08-21T17:52:35.211Z | in_progress | ran read |
| 2026-08-21T17:51:50.152Z | in_progress | ran read |
| 2026-08-21T17:50:37.962Z | in_progress | opencode claimed G4.S8.T15 (session ses_fda8de039ffeMnTHsvs2YTbX43) |

| UTC timestamp | status | one-line progress |
| --- | --- | --- |
| 2026-08-21T14:45:00Z | backlog | Ticket created from user requirement (weekly KB audit absorbs the orphan sweep; no standalone sweeper); blocked_by T14 (cascade primitives it reuses) |
| 2026-08-21T21:10:00Z | done | Implemented: KbAuditScheduler (env KB_AUDIT_ENABLED/DAY=sunday/HOUR=3/GRACE=48h; last-run persisted in kb_audit_runs → restart never double-runs in-week, missed window catches up once at startup) + 3-stage pipeline (reviewAll confidence re-review; WikiPage-nodes-vs-disk diff repairing stale subtrees via T14 deleteDocumentsForWikiPage, missing records report-only; orphan refinement sweep — age>grace AND no Document.md_ref AND no same-stem live page, removal list logged) + report rows for scheduled AND manual runs + POST /api/kb/audit (admin-gated, 409 concurrent) + GET /api/kb/audit/reports + Admin UI audit block (run button w/ spinner+409 msg, latest summary, history table with scheduled/manual badges) + CodeBrowserView delete-page affordance (document-level only). Server 1184/1184, web 550/550, vue-tsc 0, tsc 0 |

## Log

[2026-08-21] opencode claimed G4.S8.T15 (session ses_fda8de039ffeMnTHsvs2YTbX43)

[2026-08-21] G4.S8.T15 done. Files: server/src/kb/audit.ts (pipeline+scheduler), server/src/kb/audit-runs.ts (memory/pg stores), Neo4jIngestService.listWikiPagePaths/listMdRefs, app.ts/index.ts wiring, routes/kb.ts audit endpoints, web/src/api/kb.ts + AdminView.vue + CodeBrowserView.vue. Tests: server/test/kb/audit.test.ts (16), server/test/kb/routes-audit.test.ts (5), web admin-kb-audit.spec.ts (4) + code-browser-view delete tests (2).
| 2026-08-21T19:18:56Z | approved | Eng Director review: scheduler no-dup-window + 3-stage pipeline + admin UI verified; independent server 1184/1184 + web 550/550 all green |
