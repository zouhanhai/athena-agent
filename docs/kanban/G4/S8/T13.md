---
id: G4.S8.T13
title: "G4.S8.T13: chat add-agent picker — dedupe joined agents (canonical agent_id key + refresh on open) + athena local-agent status shows online"
owner: consultant
status: backlog
layer: T
parent: G4.S8
assignee: eng-director
started_at: 2026-08-21
blocked_by:
  - "G4.S8.T12"
acceptance_criteria:
  - "The add-agent picker in web/src/components/GlobalChatPanel.vue excludes every agent already in the chat, keyed canonically on agent_id (participant.agentId) rather than alias — adding an agent, reopening the picker, and the agent must not be listed again; same fix applied to the employee picker's key if it shares the flaw"
  - "availableAgents refreshes on EVERY picker open (drop the load-once `length === 0` cache) so newly registered agents and post-add state are always current"
  - "The registry status derivation (server/src/agents/registry.ts) no longer reports the seeded local Athena as permanently `unknown`: a local/in-process agent (no remote identity, platform-seeded) derives to a state the UI renders as online (extend AgentStatus with `local` or derive `reachable` — pick one, document it in the AgentStatus doc comment, and update the status-badge mapping in the picker/SettingsView accordingly)"
  - "The picker renders the corrected status badge (athena shows online/local, not offline); SettingsView agent rows stay consistent with the same mapping"
  - "Tests: web vitest — picker excludes joined agent (add → reopen → absent), list refreshes across opens, athena badge shows online/local; server registry test — seeded local agent derives the new status. Full npm test green + vitest green + typecheck clean"
---

# G4.S8.T13: chat add-agent picker dedupe + athena local-agent status

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then commit + push. Claim lock.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S8/Spec.md`) +
   this ticket's Design below. Read the actual files to confirm current shape before editing.
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing
   (start with `web/src/components/GlobalChatPanel.vue`, `server/src/agents/registry.ts`,
   `web/src/views/SettingsView.vue`).
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green + web typecheck → `status: done` + Progress Log → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Context (2026-08-21, user report)

Two defects in the chat add-agent picker (web/src/components/GlobalChatPanel.vue):

1. **Duplicates**: agents already in the chat can be added again. `pickableAgents()`
   filters `availableAgents` by `agent.alias ∉ participants[].id`, but (a) the identity
   key is fragile (alias vs participant id — participants carry `agentId` too; any
   mismatch lets a joined agent reappear), and (b) `availableAgents` is loaded ONCE
   (`length === 0` guard) so the list is stale after adds/registrations.
2. **Athena perpetually offline**: the platform-seeded local Athena has no remote
   identity, so `registry.ts` derives `status: "unknown"` forever (L252: no tokenHash →
   unknown). The picker badge renders that as offline. The API genuinely does not model
   "this agent IS the platform" — the user is right.

## Design

### 1. Canonical dedupe key + refresh

- `addAgent` already stores `agentId` on the participant — filter with it:
  `const joinedIds = new Set(chat.participants.map(p => p.agentId ?? p.id))` and exclude
  `availableAgents.filter(a => !joinedIds.has(a.agent_id))` (keep an alias fallback for
  legacy participants lacking agentId).
- Load `availableAgents` on EVERY open of the picker (remove the `length === 0` guard;
  keep the error handling). Same treatment for the employee picker if identical.

### 2. Athena local status

- In the registry status derivation: when the agent is the platform-seeded local Athena
  (identify via the existing seed path — e.g. no api_url/token AND the seeded agent id),
  derive `status: "reachable"` OR introduce `AgentStatus = "local"`. Choose ONE, update
  the doc comment block (L18-24) and EVERY consumer mapping status → label/badge
  (GlobalChatPanel picker rows, SettingsView agent rows). The intent: the built-in agent
  is always available (it runs in-process), never "offline".
- Do NOT touch remote-agent reachability logic (`registered`/`reachable` via last_seen_at).

### 3. Tests

- Web: picker excludes a joined agent across reopen; list refetches on reopen (spy on
  listAgents call counts); badge shows online/local for the seeded agent.
- Server: registry derives the new status for the seeded local agent; remote agents
  unchanged.

## Acceptance (repeated)

- `npm test` full green on 6900XT; `npx vitest run` green in web/; typecheck clean.
- Manual matrix: add athena → picker no longer lists it; reopen picker → still excluded;
  athena's badge reads online/local everywhere.

## Progress Log
| 2026-08-21T13:45:41.339Z | in_progress | opencode claimed G4.S8.T13 (session ses_fdb6e0ef7ffeceRQHcK9XTWKGV) |

| UTC timestamp | status | one-line progress |
| --- | --- | --- |
| 2026-08-21T12:25:00Z | backlog | Ticket created from user report (duplicate adds + athena always offline in picker); blocked_by T12 (worker serialization) |

## Log

[2026-08-21] opencode claimed G4.S8.T13 (session ses_fdb6e0ef7ffeceRQHcK9XTWKGV)
| 2026-08-21T13:46:11Z | backlog | Dispatch deferred by user — reverted to backlog, re-dispatch on request |
