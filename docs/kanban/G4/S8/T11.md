---
id: G4.S8.T11
title: "G4.S8.T11: per-DocType code renderers in WikiView (ddic schema view / cds chips / abap unit nav / ui5 structure) + code-meta API + devclass tree level"
owner: consultant
status: approved
layer: T
parent: G4.S8
assignee: opencode
session_id: ses_fdbcf9138ffeQwnfA4jO78oBMj
started_at: 2026-08-21
blocked_by:
  - "G4.S8.T9"
  - "G4.S8.T10"
acceptance_criteria:
  - "New API `GET /api/kb/wiki/code-meta?path=<wiki page path>` returns the page's structured code metadata resolved from its stored chunks_ref: { type, topic, system, devclass, transport, chunks: [{ id, path, heading_path, metadata }] } — 404 for non-code/missing pages; auth: employee session (read path, same as other KB GET routes)"
  - "WikiView dispatches per DocType when code-meta is available, falling back to today's markdown rendering when it is not: ddic → schema table component (key icon, column sort, field search box, FK cell links); cds → DDL highlight block + sourceTables/associations chips; abap → unit navigation (class/method list from chunks); ui5 → component/file structure list"
  - "FK / chip links navigate to `wiki/code/<system>/<TARGET>.md` when that page exists in the tree, else trigger a wiki search for the target name (no dead links)"
  - "Tree hierarchy: `listWikiPages` (server/src/kb/llmwiki.ts) also returns the code lineage fields (system/devclass/transport) parsed from the page's embedded code frontmatter; the topic tree builder adds a devclass (ABAP) / component (UI5) folder level under code/<system>/ for type=code pages — code/<system>/<devclass>/<page>.md; non-code pages unaffected"
  - "The selected page's heading outline (attachHeadings, G3.S5.T6) keeps working; for ddic/abap pages the heading nodes scroll to the renderer's corresponding section"
  - "CdsCodeChunk carries the parsed select-body members (wire the parser's existing `rawMembers` through `cdsViewsToChunks` as structured `members`) so code-meta exposes them; the cds renderer shows the view outline (Elements / Associations / Source tables) like HANA Studio's source outline"
  - "Web vitest component tests: renderer dispatch per type, ddic search/sort/FK-link behavior, tree devclass grouping; `npx vitest run` green + typecheck clean; full server npm test green (new code-meta route tests)"
---

# G4.S8.T11: per-DocType code renderers + code-meta API + hierarchical code tree

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then commit + push. Claim lock.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S8/Spec.md`) +
   this ticket's Design below. Read the actual files to confirm current shape before editing.
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing
   (start with `web/src/views/WikiView.vue`, `web/src/kb/wiki-tree.ts`, `server/src/kb/store/ddic.ts`,
   `server/src/kb/store/code.ts`, `server/src/kb/llmwiki.ts`).
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green + web typecheck → `status: done` + Progress Log → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`

## Context (2026-08-21, user requirement after T8/T9/T10)

Today every `type: code` wiki page renders identically: the T7 metadata panel
(system/devclass/transport badges) + generic markdown (hljs highlighting). The DDIC pages
are a static markdown field table; ABAP pages are a unit/method text wall. The structured
metadata that T3/T4/T5/T9 already persist per chunk (`sourceTables`, `associations`,
`dependencies`, `references`, `fields`) never reaches the frontend.

User requirement (verbatim intent): “code 在 llmwiki 前端也可以优化显示对吗 每一种 code 都有
自己的显示器？表结构如何在 wiki 中显示？可以都做。wiki 树里面不是平铺的所有 abap method
都放在一层而是有层次的。”

## Design

### 1. code-meta API (server)

`GET /api/kb/wiki/code-meta?path=<wiki page path>` (employee auth, read-only):

- Resolve the page → its stored `chunks_ref` (the store façades write
  `<CODE_OUTPUT_DIR>/<stem>/chunks.json` next to `markdown.md`; resolve via the existing
  store conventions — e.g. stem from the page file name, or via the Neo4j Document node's
  `mdRef`/documentId if that is cleaner. Pick ONE approach and document it in the route
  comment).
- Return `{ type, topic, system, devclass, transport, chunks }` where `chunks` carries each
  chunk's `id`, `path`, `heading_path` and its parsed metadata object (fields for ddic,
  sourceTables/associations for cds, dependencies for abap, references for ui5).
- 404 when the page is missing or not a code page. Add route tests.

### 2. Per-DocType renderers (web/src/views/WikiView.vue + components)

When `type: code` AND code-meta resolves, dispatch by `frontmatter.topic`'s channel —
detect via the chunk metadata shape (fields → ddic, sourceTables → cds, dependencies →
abap, references → ui5); otherwise fall back to today's markdown rendering:

| Channel | Renderer |
| --- | --- |
| ddic | schema table: 🔑 on key fields, clickable column sort, search box filtering rows, FK cell → link |
| cds | **HANA-Studio-style source outline** (view → Elements / Associations / Source tables, from chunk `members` + `associations` metadata) beside the DDL highlight block + chips row |
| abap | unit navigation list (object → methods from `heading_path`) that scrolls to the section |
| ui5 | component/file structure list (controller/view/manifest groupings from chunk paths) |

Keep the T7 metadata panel above whichever renderer runs.

### 3. Cross-links (no dead links)

FK cells and cds chips link to `wiki/code/<system>/<TARGET>.md` **when that page exists**
(check the loaded tree/pages list); otherwise clicking triggers the existing wiki search
with the target name.

### 4. Hierarchical code tree

- Extend `listWikiPages` (server/src/kb/llmwiki.ts L313) to also return the code lineage
  fields (`system`/`devclass`/`transport`) — they are already embedded in each code page's
  frontmatter (written by `renderCodeMarkdown`/`renderDdicMarkdown`); parse them alongside
  type/topic.
- In the topic tree builder (web/src/kb/wiki-tree.ts), pages with `type === "code"` and a
  devclass/component gain one folder level: `code/<system>/<devclass>/<page>`. UI5 uses the
  component namespace as the folder name. Pages without a devclass stay directly under
  `code/<system>/`.
- `attachHeadings` continues to attach the selected page's heading outline; ddic/abap
  heading nodes scroll to the matching renderer section (id-anchor the sections).

### 5. Tests

- Server: code-meta route (happy path, 404s, auth) in server/test/kb/.
- Web (vitest): renderer dispatch per metadata shape; ddic search/sort/FK-link; tree
  devclass grouping (topic tree snapshot with code pages). `npx vitest run` + typecheck.

## Acceptance (repeated)

- `npm test` full green on 6900XT; `npx vitest run` green in web/; typecheck clean.
- A DDIC page (MARA fixture from T9) renders as a searchable/sortable schema table with
  clickable FKs; a CDS page shows chips linking to its source tables; an ABAP page shows a
  unit/method navigator; the tree groups code pages under `code/<system>/<devclass>/`.

## Progress Log
| 2026-08-21T12:34:56.360Z | in_progress | ran bash |
| 2026-08-21T12:33:55.153Z | in_progress | ran bash |
| 2026-08-21T14:35:00Z | done | implemented G4.S8.T11 (code-meta API + per-DocType renderers + HANA-Studio cds outline + devclass tree level); server 1143/1143 green, web typecheck clean (7 pre-existing settings-view failures unrelated), commit + push |
| 2026-08-21T12:32:16.722Z | in_progress | ran bash |
| 2026-08-21T12:30:18.791Z | in_progress | ran bash |
| 2026-08-21T12:29:46.247Z | in_progress | ran bash |
| 2026-08-21T12:29:10.751Z | in_progress | ran edit |
| 2026-08-21T12:28:20.013Z | in_progress | ran read |
| 2026-08-21T12:27:33.782Z | in_progress | ran edit |
| 2026-08-21T12:27:01.451Z | in_progress | ran edit |
| 2026-08-21T12:26:03.188Z | in_progress | ran bash |
| 2026-08-21T12:25:24.856Z | in_progress | ran edit |
| 2026-08-21T12:24:43.160Z | in_progress | ran grep |
| 2026-08-21T12:24:11.363Z | in_progress | ran read |
| 2026-08-21T12:23:37.619Z | in_progress | ran bash |
| 2026-08-21T12:22:57.530Z | in_progress | ran edit |
| 2026-08-21T12:22:25.966Z | in_progress | ran bash |
| 2026-08-21T12:21:43.175Z | in_progress | ran edit |
| 2026-08-21T12:21:01.603Z | in_progress | ran edit |
| 2026-08-21T12:20:30.897Z | in_progress | ran edit |
| 2026-08-21T12:19:49.418Z | in_progress | ran write |
| 2026-08-21T12:19:06.181Z | in_progress | ran edit |
| 2026-08-21T12:18:29.510Z | in_progress | ran write |
| 2026-08-21T12:11:50.869Z | in_progress | ran edit |
| 2026-08-21T12:11:14.054Z | in_progress | ran edit |
| 2026-08-21T12:10:38.160Z | in_progress | ran edit |
| 2026-08-21T12:09:36.788Z | in_progress | ran edit |
| 2026-08-21T12:09:04.007Z | in_progress | ran edit |
| 2026-08-21T12:08:12.836Z | in_progress | ran edit |
| 2026-08-21T12:07:19.731Z | in_progress | ran edit |
| 2026-08-21T12:06:46.778Z | in_progress | ran todowrite |
| 2026-08-21T12:06:02.222Z | in_progress | ran read |
| 2026-08-21T12:05:16.245Z | in_progress | ran read |
| 2026-08-21T12:04:18.289Z | in_progress | ran grep |
| 2026-08-21T12:03:27.933Z | in_progress | ran read |
| 2026-08-21T12:02:52.418Z | in_progress | ran bash |
| 2026-08-21T12:02:19.336Z | in_progress | ran bash |
| 2026-08-21T12:01:42.092Z | in_progress | ran read |
| 2026-08-21T12:01:11.467Z | in_progress | ran read |
| 2026-08-21T12:00:40.415Z | in_progress | ran codegraph_codegraph_explore |
| 2026-08-21T12:00:07.249Z | in_progress | ran skill |
| 2026-08-21T11:59:10.420Z | in_progress | opencode claimed G4.S8.T11 (session ses_fdbcf9138ffeQwnfA4jO78oBMj) |

| UTC timestamp | status | one-line progress |
| --- | --- | --- |
| 2026-08-21T11:50:00Z | backlog | Ticket created from user requirement (per-DocType code viewers + hierarchical code tree); blocked_by T9 (ddic pages/chunks) + T10 (intake contract stable) |

## Log

[2026-08-21] opencode claimed G4.S8.T11 (session ses_fdbcf9138ffeQwnfA4jO78oBMj)
[2026-08-21] opencode implemented + marked done — server `GET /api/kb/wiki/code-meta` (chunks_ref via `<CODE_OUTPUT_DIR>/<stem>/chunks.json`, 404 for missing/non-code), `listWikiPages` returns system/devclass/transport/component (parseFrontmatter merges consecutive blocks), `CdsCodeChunk.members` (rawMembers) + `CodeProvenance.component` (UI5 frontmatter); web WikiCodeRenderer dispatches ddic/cds/abap/ui5 by chunk-metadata shape (markdown fallback), ddic schema table (key icon/sort/search/FK-link), cds HANA-Studio outline (Elements/Associations/Source tables beside highlighted DDL) + chips, abap unit nav, ui5 structure list, no-dead-link cross-links (navigate if page exists else wiki search), topic tree `code/<system>/<devclass|component>/` level; server 1143/1143 green, web typecheck clean, commit + push
| 2026-08-21T12:45:34Z | approved | Eng Director review: renderers/code-meta/tree verified; server suite green; web green except 7 PRE-EXISTING settings-view failures (reproduced pre-T11) |
