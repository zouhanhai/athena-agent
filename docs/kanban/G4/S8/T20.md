---
id: G4.S8.T20
title: "G4.S8.T20: wiki-edit incremental pipeline audit — full edit flow review, targeted tests, gap analysis"
owner: eng-director
status: done
layer: T
parent: G4.S8
assignee: opencode
session_id: ses_fd2198498ffeuivX8EBomrMgSz
started_at: 2026-08-23
blocked_by: []
acceptance_criteria:
  - "AUDIT the complete wiki-edit flow (Task section) against the CURRENT implementation (commits 22cba64 / 4f56a6c / 1d127a2 / 9bb1492 + T19) and report per-step: implemented / partial / missing, with file:line evidence."
  - "TESTS (new, TDD): ① alignChunksToMarkdown — unchanged section keeps chunk id+text; changed section text refreshed; missing heading keeps old text; ② overwrite() incremental — ONLY the changed chunk re-embeds (mock embedder counts calls), unchanged keep embedding; ③ stale-drop cross-document safety — shared entity (mentioned in another doc's chunks) is NOT dropped; ④ wiki-save end-to-end on a fixture — entities/relations/chunks updated in Neo4j, mention edges rebuilt, only diff-affected chunk embedded; ⑤ dedup purge — delete page, re-upload same file: NOT treated as duplicate (covers seeded full-path keys)."
  - "Gap report: any functional gaps found (stale-drop semantics, mention-edge rebuild timing, review-state sync after edit, chunk align on structural edits, LLM-chunks vs aligned conflict) — each with proposed fix + impact, for the eng director to decide follow-ups."
  - "Scope guard: fix ONLY defects that directly break the flow (crash/data-loss/wrong-graph). Do NOT implement new features; file follow-up tickets instead."
  - "All target tests green on 6900XT; server suite green; commit + push required before status=done."
pr: 0
branch: ""
---

# G4.S8.T20 — wiki-edit incremental pipeline audit

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read this ticket + parent Spec (`docs/kanban/G4/S8/Spec.md`) + `docs/kanban/G4/S8/T19.md` + the code list in Task.
2. **Use codegraph MCP**: `codegraph explore "wiki edit refine"` / `codegraph explore "Neo4j overwrite"`.
3. **Use `implement` + `tdd` skills**: write the target tests first (RED-GREEN).
4. **Milestone report**: add a Progress Log row per milestone (audit done / tests green / gap report written).
5. **Commit convention**: feature-level English commits; `codegraph sync` after.
6. **Verify + mark done**: tests green (6900XT) AND push succeeded → `status: done` → commit + push.

## Context

Three live edit-round bugs (2026-08-23) drove this audit:
1. **0-chunks wipe**: after the delta-mode rollout, the wiki-edit LLM no longer emits chunks; an empty chunk set made Neo4j overwrite() delete every old chunk (log said "0 chunks re-embedded", Lüsen lost its 6 chunks).
2. **Missed re-embed**: with chunks gone/mechanical, a few corrected words either re-embedded nothing or everything — never exactly the changed section.
3. **Dedup zombie**: after a server restart, ContentDedupStore.ensureSeeded registered pages under their FULL wiki path while deleteDocument purged only bare basenames — re-upload was instantly marked duplicate.

Fixes shipped: delta schema/prompt/normalize (22cba64), mechanical chunk backstop (4f56a6c),
alignChunksToMarkdown (1d127a2), three-key dedup purge + script cleanup (9bb1492).
The user defined the canonical edit flow (below) and wants it audited step-by-step against
these changes, with tests so the flow cannot regress again.

## Task

Audit this CANONICAL edit flow (user-confirmed 2026-08-23) against the code:

```
[pre] Neo4j holds chunks/entities/relations from the original ingest
1. User edits a wiki page (few words / sections) -> save (PUT /api/kb/wiki/page).
2. Run refiner — wiki-edit refine, DELTA mode: markdown=edited page, before/diff/structural.
   (server/src/agents/refine-document.ts: WIKI_EDIT_REFINE_SCHEMA, WIKI_EDIT_REFINE_SYSTEM_PROMPT,
    runWikiEditRefine, extractWikiEditRefinement, normalizeWikiEditRefinement)
3. Refiner output is an EXTRACTION delta: new_entities/new_relations (correction's NEW facts);
   edit locations come only via diff; no full markdown, no chunks.
   (server/src/kb/refiner.ts: markdown pinned from input.markdown; chunk backstop via
    readWikiEditPriorChunks + alignChunksToMarkdown)
4. Audit pass: independent LLM normalizes names / closes endpoints
   (refine-document.ts: auditWikiEditDocument / runEntityAudit); adopt only if validateRefineDelta passes.
5. Neo4j overwrite(input.ref, wikiPath, documentId):
   a. MERGE Document (keep md_ref), rebridge WikiPage.
   b. DELETE old chunks not in new chunk-id set; rewrite Sections.
   c. EMBED changed chunks only (same id+text+context+existing embedding => unchanged).
   d. MERGE entities; writeRelations (consistency-created endpoints);
      mentionPairs -> rebuilt MENTIONED_IN edges from the new chunks.
   e. STALE cleanup: DELETE relations between entities no longer mentioned ANYWHERE
      (cross-document safe: `NOT (a)-[:MENTIONED_IN]->(:Chunk)` checks the whole graph),
      DELETE fully orphaned Entity nodes.
   (server/src/kb/store/ingest.ts: overwrite())
[post] New entities/relations in graph; removed entities cleaned; changed sections'
chunks carry new text + fresh embeddings; one edit save does all of it.
```

For each step produce: implemented / partial / missing + file:line evidence.

## Acceptance

- Per-step audit report with evidence (above).
- Tests: ① alignChunksToMarkdown ② overwrite embed-only-changed (mock embedder) ③ stale-drop
  cross-doc safety ④ wiki-save end-to-end fixture ⑤ dedup re-upload not duplicate.
- Gap report (see Notes for format) with follow-up recommendations.
- Fix only flow-breaking defects; server suite green on 6900X.

## Notes

Gap-report format (one per finding, appended to the Progress Log area or as a
`## Gap Report` section at the bottom):

```
- FINDING: <title>
- STEP: <edit-flow step>
- EVIDENCE: <file:line / log / test result>
- IMPACT: <what breaks if unfixed>
- FIX? (proposed direction, only if needed for flow correctness)
- CONFIDENCE: <high/med>
```

Files: refine-document.ts / refine-output.ts / refiner.ts / store/ingest.ts / tasks.ts /
ingest.ts / dedup.ts / review-state.ts (all under server/src/).

## Progress Log
| 2026-08-23T09:50:24.769Z | in_progress | ran edit |
| 2026-08-23T09:49:32.286Z | in_progress | ran read |
| 2026-08-23T09:47:52.819Z | in_progress | ran edit |
| 2026-08-23T09:47:08.199Z | in_progress | ran bash |
| 2026-08-23T09:45:47.942Z | in_progress | ran bash |
| 2026-08-23T09:44:33.333Z | in_progress | ran edit |
| 2026-08-23T09:43:46.577Z | in_progress | ran read |
| 2026-08-23T09:43:11.401Z | in_progress | ran edit |
| 2026-08-23T09:42:26.165Z | in_progress | ran edit |
| 2026-08-23T09:35:54.617Z | in_progress | ran read |
| 2026-08-23T09:35:19.586Z | in_progress | ran edit |
| 2026-08-23T09:34:11.674Z | in_progress | ran bash |
| 2026-08-23T09:32:05.903Z | in_progress | ran read |
| 2026-08-23T09:31:01.760Z | in_progress | ran bash |
| 2026-08-23T09:30:19.749Z | in_progress | ran write |
| 2026-08-23T09:29:34.461Z | in_progress | ran bash |
| 2026-08-23T09:28:55.453Z | in_progress | ran write |
| 2026-08-23T09:26:23.078Z | in_progress | ran bash |
| 2026-08-23T09:25:44.704Z | in_progress | ran bash |
| 2026-08-23T09:25:00.756Z | in_progress | ran write |
| 2026-08-23T09:24:02.943Z | in_progress | ran todowrite |
| 2026-08-23T09:22:03.342Z | in_progress | ran read |
| 2026-08-23T09:21:21.446Z | in_progress | ran codegraph_codegraph_explore |
| 2026-08-23T09:18:47.499Z | in_progress | ran read |
| 2026-08-23T09:18:14.788Z | in_progress | ran read |
| 2026-08-23T09:17:22.157Z | in_progress | ran read |
| 2026-08-23T09:16:46.632Z | in_progress | ran read |
| 2026-08-23T09:16:08.311Z | in_progress | ran read |
| 2026-08-23T09:14:44.442Z | in_progress | opencode claimed G4.S8.T20 (session ses_fd2198498ffeuivX8EBomrMgSz) |

| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-23T09:00:00Z | backlog | Created after three live edit-round bugs (0-chunks wipe, missed re-embed, dedup zombie) — user pinned canonical edit flow for code audit + targeted tests |
| 2026-08-23T10:40:00Z | in_progress | Audit done: 7-step canonical flow traced with file:line evidence; 3 flow-breaking defects found (delta-prompt contradiction, mention-edge accumulation, residual 0-chunks wipe) |
| 2026-08-23T11:00:00Z | in_progress | Tests RED→GREEN: 11 new target tests across 4 files (align ×3, embed-only-changed, cross-doc stale-drop, wiki-save e2e, dedup purge ×2, mention-rebuild prune, zero-chunk guard, delta-prompt pin) |
| 2026-08-23T11:05:00Z | done | Gap report: 6 findings — 3 fixed in-scope (flow-breaking), 3 report-only for director decision (incl. the live review-gate frontmatter drift). Server suite 1303/1303 + typecheck clean; pushed 7fee838 |

## Log

[2026-08-23] opencode claimed G4.S8.T20 (session ses_fd2198498ffeuivX8EBomrMgSz)
[2026-08-23] done: ① per-step audit below (evidence vs commits 22cba64/4f56a6c/1d127a2/9bb1492+T19). ② 11 target tests written first (RED where defects existed), all GREEN after fixes; full server suite 1303/1303 (`npm test`), `tsc --noEmit` clean. ③ Fixed ONLY the three flow-breaking defects (commit 7fee838): delta-mode user prompt contradiction, MENTIONED_IN accumulate→rebuild prune, zero-chunks wipe guard. Report-only findings left for director follow-ups.

## Per-Step Audit Report (canonical flow vs current code)

Legend: implemented / partial / missing, evidence as `file:line` (line numbers as of commit 7fee838).

**[pre] Neo4j holds chunks/entities/relations from original ingest** — implemented.
Evidence: ingest path `server/src/kb/store/ingest.ts:224-388` (chunks+embeddings :279-330, entities :346-352, relations via writeRelations :356/:784-855, MENTIONED_IN :363-376); wiki-edit reads prior chunks from that output (`readWikiEditPriorChunks`, refiner.ts:148-162).

**1. User edits page → save (PUT /api/kb/wiki/page)** — implemented.
`routes/kb.ts:743-798`: path/content validation (:750-756), RBAC `kb.edit` (:757-768), file persist + index rebuild + rescan + before/after ragMarkdown snapshot (`saveWikiPage`, kb/ingest.ts:774-790), diff computed on rag forms (:773, computeWikiDiff kb/diff.ts:115), task submitted `submitWikiSave` tasks.ts:546-551.

**2. Refiner — wiki-edit refine, DELTA mode** — implemented (one defect found & fixed).
Schema optional-markdown refine-document.ts:1932-1993; system prompt forbids re-emission :1996-2022; user prompt carries corrected markdown + before + diff + structural :2025-2053; direct-OpenRouter run loop w/ retries :2257-2296; extract/normalize tolerate missing markdown :2108-2130 / :2084-2105 (`allowMissingMarkdown`).
DEFECT FIXED (7fee838): the user prompt's closing paragraph still said "Emit the corrected markdown VERBATIM" — contradicting rule 1 and inviting the pre-delta truncation failure back; since refiner.ts:109-111 prefers a non-empty model markdown over the user's edit, a truncated re-emission would have replaced the source of truth. Now extraction-only; pinned by test ("the USER prompt is delta-mode too").

**3. Output is an EXTRACTION delta; markdown pinned; chunk backstop** — implemented.
correctedMarkdown pinned refiner.ts:104-111; chunk backstop refiner.ts:113-125 = prior chunks.json (:148-162) → `alignChunksToMarkdown` (refine-output.ts:535-552) → mechanical fallback (refine-document.ts:2133-2141); normalize unions `new_*` into the full lists so corrections can't be dropped (:2092-2104).

**4. Audit pass (independent LLM normalizes/closes endpoints)** — implemented.
Mandatory per save: runWikiEditRefine :2277-2290 (never fatal); `auditWikiEditDocument` :2220-2255 incl. empty-relations wipe guard :2246-2250; adoption ONLY when the audited delta passes `validateRefineDelta` (runEntityAudit :1494-1500).

**5a. MERGE Document (keep md_ref), rebridge WikiPage** — implemented.
md_ref COALESCE keeps ORIGINAL refine dir store/ingest.ts:416-433; WikiPage bridge rewritten :435-448.

**5b. DELETE stale chunks not in new set; rewrite Sections** — implemented (+guard added).
Stale-chunk DETACH DELETE + section rewrite store/ingest.ts:459-480 (post-fix numbering). DEFECT FIXED (7fee838): residual 0-chunks wipe vector — empty rag body + missing prior chunks.json produced an empty keep-list and deleted every stored chunk (same class as live bug #1). Zero-chunk guard now keeps the stored subtree and warns; test pins it.

**5c. EMBED changed chunks only** — implemented.
Unchanged = same id + same text + same context + existing embedding (:482-490); batched embeds of changed only (:491-497); unchanged write variant omits embedding (:507-529). Mock-embedder test proves exactly ONE call carrying exactly the changed/un-embedded chunks.

**5d. MERGE entities; writeRelations (created endpoints); mention edges rebuilt** — partial → FIXED.
Entity MERGE :565-571; consistency layer creates missing endpoints then writes edges w/ truthful counts (:576, writeRelations :784-855); mentionPairs recomputed against NEW chunk texts incl. created endpoints (:578-587). DEFECT FIXED (7fee838): edges were MERGE-only — surviving chunks kept STALE MENTIONED_IN edges forever (edges only died with deleted chunks), so 5e's whole-graph predicate treated dropped entities as "still mentioned" and dead relations were immortal (accumulate-not-rebuild ⇒ wrong graph over repeated edits). A documentId-scoped prune now runs before the fresh pairs land (:584-591); order + scoping pinned by test.

**5e. STALE cleanup (cross-doc safe)** — implemented.
Whole-graph predicate deletes relations whose endpoints are mentioned NOWHERE (`NOT (a)-[:MENTIONED_IN]->(:Chunk)` — no documentId scoping, verified by test) and removes fully orphaned entities store/ingest.ts:604-611. Graph-model driver test proves a shared entity mentioned by ANOTHER doc's chunk survives with its relation intact.

**[post] one edit save does all of it** — implemented.
`runWikiSave` tasks.ts:998-1148 chains refinement (mechanical fallback :1045-1055) → neo4j overwrite (:1059-1118, resolves OLD documentId via wikiPath bridge) → finalize; e2e fixture test drives queue+refiner+Neo4jIngestService and asserts chunks/entities/relation-endpoint creation/mention pairs/embed-count/document-id reuse end to end.

**Bug regression pins**: #1 0-chunks wipe → backstop (4f56a6c) + zero-chunk guard (test). #2 missed re-embed → alignChunksToMarkdown (1d127a2) + overwrite classify (tests ①②④). #3 dedup zombie → three-key purge kb/ingest.ts:708-718 covering seed full-path keys (dedup.ts:123-137 seeds `page.path`; queue records basename tasks.ts:962-981) — delete→re-upload proven NOT duplicate through `deleteDocument` (tests ⑤×2).

## Gap Report

- FINDING: Wiki-edit user prompt asked for verbatim markdown re-emission while the system prompt forbade it (delta contract leak)
- STEP: 2→3
- EVIDENCE: refine-document.ts buildWikiEditRefinePrompt closing paragraph (pre-7fee838) vs WIKI_EDIT_REFINE_SYSTEM_PROMPT rule 1; refiner.ts:109-111 prefers non-empty model markdown over input.markdown
- IMPACT: a compliant-with-user-prompt model re-emits the full document; truncation/hallucination then REPLACES the user's source-of-truth text before alignment+overwrite → wrong chunks/embeddings land in RAG (the exact pre-T1 failure class)
- FIX? APPLIED in 7fee838: extraction-only closing paragraph + regression pin
- CONFIDENCE: high

- FINDING: overwrite() accumulated MENTIONED_IN edges instead of rebuilding them — stale mentions made the whole-graph stale-drop permanently blind
- STEP: 5d→5e
- EVIDENCE: store/ingest.ts mention pass was MERGE-only (pre-7fee838 :565-574); edges died only via DETACH DELETE of removed chunks; the 5e predicate consults any MENTIONED_IN edge anywhere in the graph
- IMPACT: an entity whose mention disappeared from an edited section kept a ghost edge → its relations could never be stale-dropped and detail views linked chunks that no longer mention them; drift compounds per edit
- FIX? APPLIED in 7fee838: documentId-scoped `MATCH (c:Chunk {documentId})<-[r:MENTIONED_IN]-() DELETE r` BEFORE merging fresh pairs (cross-doc safe; unchanged chunks get identical pairs re-merged)
- CONFIDENCE: high

- FINDING: residual 0-chunks wipe vector — empty RAG body + missing prior chunks.json still wiped all stored chunks
- STEP: 3→5b
- EVIDENCE: refiner.ts:120-124 falls through to mechanicalWikiEditChunks when priorChunks is empty; mechanical("") → []; unguarded `WHERE NOT c.id IN $ids` delete with an empty keep-list
- IMPACT: data loss (all chunks + embeddings gone) for degenerate pages consisting only of frontmatter/images (route rejects empty content but not empty ragAfter)
- FIX? APPLIED in 7fee838: zero-chunk wipe guard — an empty new chunk set keeps the stored subtree and warns loudly; test pins absence of both deletes
- CONFIDENCE: med (logic traced + unit-guarded; narrow production trigger)

- FINDING: wiki-edit saves do NOT sync the review gate on the wiki frontmatter (Eng Director live finding, confirmed in production)
- STEP: [post] / review-state sync after edit
- EVIDENCE: upload path stamps reviewGate keyed on quality.action (tasks.ts:844-851 → ingestLlmWiki `review` param → `review:`/`review_count:` frontmatter, ingest.ts:343); runWikiSave (tasks.ts:998-1148) never restamps in either direction. Production Lüsen: `review: required` / `review_count: 4` while quality.json issues ALL resolved (unresolved 0) — banner shows 0 pending yet the gate stays required
- IMPACT: review-state drift after EVERY wiki edit — false required-banners, or newly introduced review items never surfaced on the page (only visible as task-level reviewRequired)
- FIX? (proposed, NOT implemented — director decision): after runWikiSave's overwrite completes, apply upload-path-equivalent gate logic keyed on the wiki-edit result quality via the canonical WikiFrontmatterSyncer (action=review_required → required+count; else if pre-existing review was required and all issues resolved → clear) so the Neo4j mirror stays consistent
- CONFIDENCE: high

- FINDING: edited pages' NEW quality.json lands in a directory nothing references — review UI keeps serving the PRE-edit issue list
- STEP: [post] / review-state sync after edit
- EVIDENCE: wiki-edit refs are stored under stem `wiki-edit-<stem>` (refiner.ts:128-133); overwrite() keeps the ORIGINAL md_ref via COALESCE (store/ingest.ts:420); review-state resolves quality.json via md_ref dir first, then `<root>/<page-stem>` (review-state.ts:148-170) — neither reaches the wiki-edit dir
- IMPACT: GET review-state shows the original ingest's issues; post-edit quality findings are invisible to the operator workflow (compounds the gate-drift finding above)
- FIX? (proposed, NOT implemented): persist the last-edit ref dir on the Document node (e.g. `d.last_edit_ref`) and let loadQuality try it after md_ref, or write wiki-edit output into the SAME stem dir — touches T18's md_ref-protection design, eng-director call
- CONFIDENCE: high

- FINDING: alignChunksToMarkdown keys sections by LAST heading-path segment only — duplicate section titles collide
- STEP: 3
- EVIDENCE: refine-output.ts:540-550 (`key = heading_path.split("/").pop()`, `!byHeading.has(key)` keeps the FIRST match)
- IMPACT: documents with repeated headings ("Agenda" per day) can refresh the WRONG chunk or leave the genuinely-edited section un-refreshed (stale embedding persists) — retrieval-quality degradation, no data loss
- FIX? (proposed, NOT implemented): key by FULL heading_path chain (parent segments included); mechanical and aligned chunks already carry complete paths
- CONFIDENCE: med

## Verification

- Target tests (11, TDD RED→GREEN): server/test/refine-output-align-chunks.test.ts (①×3), server/test/kb/store-ingest-wiki-edit-flow.test.ts (②③④ + mention-rebuild prune + zero-chunk guard), server/test/kb/dedup-purge-reupload.test.ts (⑤×2), server/test/refine-document-wiki-edit.test.ts (delta-prompt pin).
- Full server suite: **1303/1303 pass**, 0 fail (`npm test` = `node --import tsx --test --test-force-exit`); `npx tsc --noEmit` clean; `codegraph sync` done.
- Pushed: 7fee838 (fix + tests).
