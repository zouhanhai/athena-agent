---
id: G4.S9.T1
title: "G4.S9.T1: Leiden community detection on the knowledge graph — memberships persisted, incremental strategy"
layer: T
parent: G4.S9
owner: eng-director
status: backlog
assignee: ""
session_id: ""
priority: P1
depends_on: []
blocked_by: []
acceptance_criteria:
  - "Community detection over Entity nodes + RELATION edges (optionally MENTIONED_IN co-weight) produces a partition: every entity gets a stable communityId persisted on the node (e.g. property community_id)."
  - "Implementation runs inside the server (no external cluster); documented choice: neo4j GDS community edition plugin OR an in-process JS/TS algorithm reading the graph over the driver (graphology louvain/leiden port or a hand-rolled implementation). Decision recorded in the ticket Progress Log with rationale (operational burden, correctness)."
  - "Incremental strategy implemented: full re-run on delete/big-ingest; local recompute (touched entity's community + neighbours) for small wiki-edit diffs, or full re-run below a size threshold — documented and tested on fixtures."
  - "Community memberships survive ingests/edits/deletes without breaking online retrieval (no lock-out, memberships are eventually consistent)."
  - "Deterministic on fixtures: the Sommerseminar corpus yields a stable partition including the CALEO-centric community (CALEO + its relations in one community)."
  - "Tests: unit tests on a small synthetic graph, integration on the Sommerseminar fixtures (loaded via the real ingest), full server suite green on 6900XT; push required before status=done."
pr: 0
branch: ""
---

# G4.S9.T1 — Leiden community detection engine

## Context

G4.S8 delivered a stable ingest/edit/delete graph (approved). G4.S9 spec section 1:
Leiden community detection over Entity+RELATION (optionally MENTIONED_IN co-weight).
The knowledge graph at fixture scale is ~20-50 entities; choose an implementation
that fits the stack (probably in-process JS/TS — no GDS server dependency) and a
strategy that stays fast at the planned scale (day-1 target: hundreds of documents,
thousands of entities).

## Task

1. Implement/reuse community detection:
   - Try `neo4j-graph-data-science` community edition availability for
     `gds.beta.community.leiden`; if the community container doesn't ship GDS or
     the license is awkward, implement in-process: read entities+relations via the
     driver, run a JS Leiden/Louvain, write `community_id` back with a batched
     `SET e.community_id = $id`.
   - Keep it deterministic (seeded) so fixtures are stable.
2. Incremental strategy:
   - delete/big-ingest → full re-run.
   - small wiki-edit diff → recompute for the changed entity's community +
     neighbours (heuristic; document limits).
   - size < threshold → always full re-run (simple and fine for our scale).
3. Tests + fixtures: synthetic small graph; then real ingest of the two Sommer docs
   and assert the CALEO-centric partition.

## Acceptance

Per frontmatter. Evidence of the implementation-choice rationale in the ticket.

## Notes

- Community id should be stable when possible (a canonical member id or a hash of
  sorted members) so refreshes don't churn member nodes.
- Do NOT touch retrieval in this ticket (T3 owns the query path).
- Keep memberships as a property; a Community node with summary is T2's job.

## Progress Log

| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-23T15:00:00Z | backlog | Created from G4.S9 spec |