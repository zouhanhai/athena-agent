---
id: G4.S10.T1
title: "G4.S10.T1: LINK engine — candidate↔graph matching, deterministic rules + LLM adjudication, retry/repair, atomic writes, wiki-edit reuse"
owner: eng-director
status: in_progress
layer: T
parent: G4.S10
assignee: opencode
session_id: ses_fcb5db16fffeQaNVTwmAf97pop
started_at: 2026-08-24
blocked_by: []
acceptance_criteria:
  - "LINK engine implemented as a channel-agnostic pure-ish function `linkCandidates({candidates, existingGraph})` returning `{merges[], new_edges[], standalone[]}`; wired into the ingest pipeline between refine and audit (order fixed: refine → link → audit → store)."
  - "Deterministic tier (no LLM): exact nameUpper / alias / substring / ≥0.92 vector similarity AND same normalized type → auto-merge; different normalized types → NEVER merge (typed-edge path instead)."
  - "LLM tier: ambiguous candidates (0.6-0.92) get one json_schema-constrained call with top-5 existing matches + evidence quotes; retries ≤3 with repair retry on schema mismatch; total failure degrades to deterministic-only without blocking ingest."
  - "Output validated: merge from∈candidates, to∈existing; edge endpoints∈candidates∪existing; no phantom nodes; evidence quotes ≤80 chars; output capped (max_tokens). No full-document/markdown re-emission anywhere in the contract."
  - "Atomic batch application: per-document merge+edge writes in one transaction; global async mutex for the write phase so parallel uploads cannot lose merges (race test proves one node per shared entity with both sources recorded)."
  - "Wiki-edit path reuses the same linkCandidates() on delta-refine candidates (before audit), so a rename like 'galleo Office'→'CALEO Office' re-links to the existing node."
  - "Tests: order test (link-before-audit), race test (two parallel docs sharing an entity → one node, both source_docs), retry-degradation test, contract-bounds test, merge-dedup test; server suite green; push required before done."
pr: 0
branch: ""
---

# G4.S10.T1 — LINK engine

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S10/Spec.md`) + the refinement pipeline (`server/src/kb/tasks.ts` runIngest / runWikiSave, `store/ingest.ts`).
2. **Use codegraph MCP**: `codegraph explore "entity link"` / `codegraph explore "refine audit write store"` before editing.
3. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
4. **Milestone report**: semantic Progress Log rows. (REAL timestamps.)
5. **Commit convention**: feature-level English commits; `codegraph sync` after.
6. **Verify + mark done**: tests green → set `status: done` → commit + push (PUSH BEFORE DONE — repeated worker discipline issue).

## Context

See G4.S10 Spec Design 1-4. The engine will be used by both the upload and the
wiki-edit pipelines, so keep it channel-agnostic (candidates in, decisions out).
The existing refine output already carries name/type/description/occurrences;
the graph store already has vector+BM25 over entities. Link is the first stage
that CONSULTS the existing graph.

## Task

1. `linkCandidates({ candidates, existingGraphApi })`:
   - query existing via exact/alias/vector≥0.92/BM25 → per-candidate top-K.
   - deterministic merges; ambiguous → LLM prompt (bounded input, json_schema).
   - validate + return decisions.
2. Wire into ingest (upload defaultIngest refiner chain) BEFORE audit; wire the
   same call into runWikiSave delta path.
3. Atomic apply: merge aliases + source_docs + relations redirect + new edges,
   in one session/txn; global mutex on the write phase.
4. Retry/repair/degrade per acceptance.

## Acceptance

Per frontmatter.

## Notes

- Reuse the refinement LLM caller (reasoning off, json_schema) — no new provider.
- Deterministic tier must be unit-testable without LLM (inject fake).
- Evidence quotes come from the candidate's occurrences and the top-k match's
  matching chunk/entity — reuse existing retrieval (mentionPairs coverage).

## Progress Log
| 2026-08-24T16:37:34.526Z | in_progress | opencode claimed G4.S10.T1 (session ses_fcb5db16fffeQaNVTwmAf97pop) |

| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-24T18:00:00Z | backlog | Created from G4.S10 spec T1 split |

## Log

[2026-08-24] opencode claimed G4.S10.T1 (session ses_fcb5db16fffeQaNVTwmAf97pop)
