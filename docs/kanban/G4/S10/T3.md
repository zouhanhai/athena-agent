---
id: G4.S10.T3
title: "G4.S10.T3: Weekly full-graph re-link — deterministic pre-scan + candidate-LLM adjudication, report + Admin visibility"
owner: eng-director
status: done
layer: T
parent: G4.S10
assignee: opencode
session_id: ses_fcaf9014cffe46qXYfOKarBkIk
started_at: 2026-08-24
blocked_by: []
acceptance_criteria:
  - "The weekly knowledge-base audit (T15 flow) runs a FULL-GRAPH re-link: ① deterministic pre-scan (no LLM) over ALL entities — embedding similarity ≥0.85 pairs (vector index, top-k), naming variants / alias dupes / same-name-different-type clusters — producing candidate pairs; ② LLM adjudication on candidate pairs only (batch, json_schema, evidence), deciding merge / typed edge / skip; ③ report of merges applied + unmerged candidates (with similarity) + new edges, stored in auditRunsStore with trigger=weekly and surfaced in the Admin UI."
  - "The re-link reuses the T1 LINK engine (linkCandidates) — no second implementation; the weekly pass just feeds full-graph pre-scan output as candidates and applies with the SAME atomic/lock/validation rules."
  - "Bounded cost: LLM runs only on candidate pairs (hundreds-thousands even at 10k-100k entity scale); pre-scan is LLM-free; report includes candidate-count & LLM-call count for observability."
  - "Audit ALSO runs the incremental re-link candidates (entities whose source_docs changed since last audit, low-degree entities) — the weekly pass covers both incremental and full sweeps."
  - "Tests: pre-scan pair discovery on fixture; weekly report row (trigger=weekly) with counts; Admin shows the report; reuse of linkCandidates proven by dependency injection; server + web suites green; push required before done."
pr: 0
branch: ""
---

# G4.S10.T3 — Weekly full-graph re-link

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S10/Spec.md` section 6) + T15 weekly audit (`server/src/kb/audit.ts`, `audit-runs.ts`) + T1's `linkCandidates` + Admin maintenance UI (`AdminView.vue`).
2. **Use codegraph MCP**: `codegraph explore "weekly audit"` / `codegraph explore "entity similarity scan"` before editing.
3. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR).
4. **Milestone report**: semantic Progress Log rows. (REAL timestamps.)
5. **Commit convention**: feature-level English commits; `codegraph sync` after.
6. **Verify + mark done**: tests green → set `status: done` → commit + push (PUSH BEFORE DONE).

## Context

Weekly audit (T15) already covers file/graph consistency + orphan cleanup.
This ticket extends it with the graph-wide re-link: merge late-arriving
duplicates/name-variants that incremental ingest-time LINK may have missed,
and expose the result in the Admin UI. Full but layered: deterministic pre-scan
(no LLM) → candidate pairs → LLM adjudication → applied + reported.

## Task

1. Pre-scan: query the entity index for near-duplicate pairs (embedding cosine
   ≥0.85 via vector index; name/alias normalization clusters; same-name
   different-type), producing candidate pairs keyed by canonical nameUpper.
2. Adjudicate candidates via linkCandidates() (inject the same engine), batched.
3. Apply with atomic writes; produce the weekly report {trigger:"weekly",
   merges, unmergedCandidates, newEdges, candidateCount, llmCalls} into
   auditRunsStore.
4. Admin UI: display the weekly re-link report (table/accordion) alongside the
   community-quality section.

## Acceptance

Per frontmatter.

## Notes

- Use the vector index already on Entity (embedding on name+description) — no
  new embedding infra.
- Keep pre-scan deterministic and cheap: top-K per node, cap pair queue.
- The report must not page unto oblivion: keep it summary + drill-into counts.

## Progress Log
| 2026-08-24T19:05:37.200Z | in_progress | ran bash |

| 2026-08-24T19:03:58.051Z | in_progress | Explored T15 audit + T1 linkCandidates + Admin UI via codegraph; RED suite test/kb/relink.test.ts (17 tests): pre-scan fixtures, DI proof, weekly report, incremental sweep, Neo4j port, audit integration |
| 2026-08-24T19:04:40.000Z | in_progress | GREEN: relink-scan.ts (vector>=0.85 top-k + alias/name-variant + same-fold-different-type lanes, dedupe, maxPairs cap, zero LLM) + store/relink-graph.ts (listEntities w/ degree+provenance, entitiesChangedSince watermark, applyMerges redirect+fold+delete under globalGraphWriteMutex, phantom-guarded createEdges w/ truthful counts) |
| 2026-08-24T19:05:20.000Z | in_progress | KbRelinkService: linkEngine DI seam (DEFAULT_LINK_ENGINE === T1 linkCandidates), batched adjudication (40/batch), counting-LLM wrapper -> llmCalls observability, incremental changed-provenance/low-degree sweep through SAME engine, merge-rename retargeting of edges |
| 2026-08-24T19:05:50.000Z | in_progress | Wired T15 flow: audit record relink block (+Postgres JSONB migration), audit stage 5 w/ previous-run watermark degrading to details line, Document.ingested_at watermark on ingest/overwrite, app.ts defaultRelinkService; web api types + AdminView summary/table column/drill-in beside community-quality |
| 2026-08-24T19:06:10.000Z | in_progress | Verified: server npm test 1444/1444 green + tsc clean; web vitest 567/567 green + vue-tsc clean; code-review fixed O(n^2) lookup, empty type alias, tautological assert |

| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-24T18:10:00Z | backlog | Created from G4.S10 spec T3 split |

## Log

[2026-08-24] opencode claimed G4.S10.T3 (session ses_fcaf9014cffe46qXYfOKarBkIk)

[2026-08-24] Implementation complete: weekly full-graph re-link (pre-scan -> linkCandidates DI -> atomic apply -> trigger=weekly report -> Admin UI). Server 1444/1444, web 567/567, typechecks clean. Pushed to remote.
