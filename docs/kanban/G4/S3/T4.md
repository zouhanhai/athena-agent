---
id: s3.4
title: "G4.S3.T4: Topic-scoped search — scope retrieval to a topic subtree"
layer: T
parent: G4.S3
owner: consultant
assignee: opencode
session_id: ses_g4s3t4
status: done
milestone: M4
blocked_by: []
acceptance_criteria:
  - "See 'Acceptance' section"
---

# G4.S3.T4: Topic-scoped search — scope retrieval to a topic subtree

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S3/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`).
3. **Use codegraph MCP**: `codegraph explore "<area>"` before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
   (REAL timestamps — do NOT fabricate; if stuck in a long reasoning loop take concrete action.)
6. **Commit convention**: feature-level English commits; `codegraph sync` after.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`


## Background

Let a query search within a topic subtree (e.g. docs under `sap/`), pre-filtering candidates by wiki
frontmatter topic before semantic scoring (G4.S3 core).

## Task

1. Add a `topic` scope to `/api/kb/search` (and the Neo4j retrieval service) that pre-filters
   candidates by the wiki frontmatter topic subtree before semantic scoring.
2. Works for agentic-RAG scoping + precision on large corpora.
3. TDD: topic-scoped search only returns results within the topic subtree.
4. Server + web tests green, typecheck clean.

## Acceptance

- Semantic search can scope to a topic subtree (topic-scoped search).

## Progress Log

| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-12T07:01:00Z | in_progress | Claimed; context gathered (retrieval.ts, store/retrieval.ts, routes/kb.ts, tests). Existing topic filter is exact-match single topic; need subtree expansion. Starting TDD. |
| 2026-08-12T09:12:00Z | in_progress | RED→GREEN: store-level `topics` option (vector+BM25+graph), KnowledgeRetrievalService subtree expansion from wiki frontmatter + keyword-hit scoping. Server kb tests 51/51, routes 12/12; typecheck clean. |
| 2026-08-12T09:22:00Z | done | Committed + pushed (705e3ff), codegraph synced. Server 745 pass (3 pre-existing review.test date/float failures on clean checkout), web 347/347, both typechecks clean. Acceptance met: semantic search scopes to a topic subtree. |
