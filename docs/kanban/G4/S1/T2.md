---
id: t2
title: "G4.S1.T2: refine_document output contract + document-refinement skill + prompt"
layer: T
parent: G4.S1
owner: eng-director
status: in_progress
assignee: opencode
session_id: ses_g4s1_t2_opencode
started_at: 2026-08-09
completed_at: ""
blocked_by: []
acceptance_criteria:
  - "Output contract schema defined: markdown / frontmatter(type,topic) / chunks / entities / relations / keywords / quality"
  - "Entity fields: name(title-case)/type/description; relation: source/target/keywords/description (binary); keyword: relationship+query"
  - "`document-refinement` SKILL.md created (header re-level rules + taxonomy ref + quality checklist + entity/relation extraction guidance)"
  - "Single prompt (or prompt template) produces all outputs in ONE full-doc LLM pass (constrained sampling)"
  - "Prompt uses docs/taxonomy.md for type/topic + cross-RAG extraction best practices (G4.S1 Spec)"
---

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/<S>/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`) for the full design + acceptance criteria.
3. **Use codegraph MCP**: `codegraph explore "<area>"` (or `codegraph_explore` tool) before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`


# G4.S1.T2: refine_document output contract + skill + prompt

## Task

Define `refine_document`'s output contract and the `document-refinement` skill + prompt (per G4.S1 Spec,
including the cross-RAG extraction comparison).

## Output contract (JSON schema, constrainedSampling)

```jsonc
{
  "markdown": "...",                    // re-leveled markdown (header hierarchy fixed)
  "frontmatter": { "type": "...", "topic": "..." },   // from docs/taxonomy.md
  "chunks": [{ "id": "c1", "text": "...", "heading_path": "..." }],  // paragraph-semantic, ~1200 tok
  "entities": [{ "name": "...", "type": "org|person|...", "description": "..." }],
  "relations": [{ "source": "...", "target": "...", "keywords": ["..."], "description": "..." }],
  "keywords": ["..."],
  "quality": { "complete": true, "confidence": 0.85, "issues": ["..."], "action": "auto_accept|review_required" }
}
```

## `document-refinement` skill (SKILL.md)

Teaches the refinement pass:
- **Header re-level**: semantic `#`/`##`/`###` (fix flat h2, e.g. Sommerseminar 16×h2).
- **Classification**: type (CALEO types) + topic (hierarchical tree, docs/taxonomy.md).
- **Entity/relation extraction**: name title-case normalization, binary relations, only direct/meaningful
  (from the cross-RAG comparison in the Spec).
- **Quality checklist**: completeness vs source, table/figure capture, garbled text.
- **Single full-doc pass** (context-efficient).

## Prompt template

One prompt (PromptTemplate) doing re-header + classify + entity/relation/keyword + quality in one pass,
constrained by the JSON schema.

## Deliverables

- Output contract schema (TypeBox for ToolDefinition params).
- `document-refinement` SKILL.md.
- Prompt template.

## Acceptance (see frontmatter)

## Progress Log

| Timestamp (UTC) | Status | Progress |
|-----------------|--------|----------|
| 2026-08-09 20:35:00Z | in_progress | Claimed T2 (ses_g4s1_t2_opencode); read Spec + Goal + T1 impl; regenerated kanban index |
| 2026-08-09 20:45:00Z | in_progress | TDD RED: wrote refine-document-contract.test.ts (9 tests: schema shape, chunk/entity/relation/quality fields, prompt taxonomy + cross-RAG, SKILL.md) — failed on missing exports |
| 2026-08-09 20:50:00Z | in_progress | GREEN: updated output contract to T2 (chunks heading_path; relations source/target/keywords/description); embedded taxonomy + cross-RAG best practices in one-pass prompt; created server/.pi/skills/document-refinement/SKILL.md; updated T1 test to new contract |
| 2026-08-09 20:55:00Z | in_progress | Full server suite 623/623 green + typecheck clean; docling test 13 OK; skill loads via loadSkillsFromDir |
| 2026-08-09 21:00:00Z | in_progress | Committed feature (refine_document contract + document-refinement skill + prompt) |
