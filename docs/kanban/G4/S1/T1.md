---
id: t1
title: "G4.S1.T1: refine_document Pi custom tool (tool def + athena provider + models.json)"
layer: T
parent: G4.S1
owner: eng-director
status: done
assignee: opencode
session_id: ses_017eee793ffe6GbmuHdTkByUMD
started_at: 2026-08-09
completed_at: 2026-08-09
blocked_by: []
acceptance_criteria:
  - "Register `refine_document` as a Pi custom tool (ToolDefinition) in the athena agent"
  - "Use `constrainedSampling` (JSON schema) so the tool returns structured output, not free-text JSON"
  - "Uses the dedicated `athena` OpenRouter provider (independent key) + deepseek-v4-flash-latest, thinkingLevel high"
  - "Add `athena` provider to `~/.pi/agent/models.json` so getModel('athena', ...) resolves"
  - "Input = docling markdown; output = { markdown, frontmatter, chunks, entities, relations, keywords, quality }"
---

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then commit + push. Claim lock.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/<S>/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`) for the full design + acceptance criteria.
3. **Use codegraph MCP**: `codegraph explore "<area>"` (or `codegraph_explore` tool) before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`


# G4.S1.T1: refine_document Pi custom tool

## Task

Register the Athena document-refinement LLM pass as a Pi **custom tool** `refine_document` (per G4.S1 Spec).

## Steps

1. **Tool registration** — add `refine_document` to the athena agent's `customTools` (`ToolDefinition`):
   - params: `{ markdown: string }` (docling output), optional `{ topic_hint?: string }`.
   - `constrainedSampling` with the output JSON schema (see T2) so the result is schema-conformant.
   - `executionMode` sequential.
2. **Provider/model** — use `modelRuntime.getModel("athena", "~deepseek/deepseek-v4-flash-latest")`
   (dedicated `athena` OpenRouter provider = independent key for independent cache/cost).
3. **models.json** — add `athena` provider entry (same model set as `openrouter`) so getModel resolves.
4. **thinkingLevel high** — header semantic re-leveling needs higher reasoning.
5. The tool internally: reads full md → LLM pass → returns the output contract (T2). Big-output
   handling in T3.

## Deliverables

- `refine_document` Pi custom tool wired into the agent (server/src/agents).
- `athena` provider in models.json (6900XT + local).
- Tool returns the structured output contract.

## Acceptance (see frontmatter)

## Progress Log

| Timestamp (UTC) | Status | Progress |
|-----------------|--------|----------|
| 2026-08-09 18:50:00Z | in_progress | Claimed T1 (ses_017eee793ffe6GbmuHdTkByUMD); read Spec + read Pi SDK ToolDefinition/ModelRuntime types |
| 2026-08-09 19:10:00Z | in_progress | TDD: wrote refine-document.test.ts (9 tests); implemented server/src/agents/refine-document.ts |
| 2026-08-09 19:20:00Z | in_progress | Wired refine_document into createAgent customTools; added athena provider to ~/.pi/agent/models.json; typecheck + 9/9 tests green |
| 2026-08-09 19:35:00Z | in_progress | Full server suite 611/611 green (fixed stale kanban-scan live-board assertion: G3 done); verified getModel('athena', ...) resolves with stored key |
| 2026-08-09 19:40:00Z | done | refine_document tool + athena provider shipped; committed feature + marked done |
