---
id: t5
title: "G4.S1.T5: fallback + tests (LLM failure → raw docling; quality.review_required)"
layer: T
parent: G4.S1
owner: eng-director
status: backlog
assignee: ""
started_at: ""
blocked_by: []
acceptance_criteria:
  - "refine_document failure → fallback to raw docling output (never worse than today)"
  - "quality.action=review_required → surfaced for operator review (log/Uploads flag)"
  - "Tests: tool output contract, header re-level, classification, entity/relation/keyword, chunking, fallback"
  - "All existing server/web tests stay green"
---

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/<S>/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`) for the full design + acceptance criteria.
3. **Use codegraph MCP**: `codegraph explore "<area>"` (or `codegraph_explore` tool) before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`


# G4.S1.T5: fallback + tests

## Task

Graceful degradation + test coverage for the refinement step (per G4.S1 Spec).

## Fallback

- `refine_document` LLM failure → use the **raw docling output** (no regression vs today).
- `quality.action = review_required` → flag for operator review (log + Uploads UI badge).

## Tests

- Output contract: markdown/frontmatter/chunks/entities/relations/keywords/quality shape.
- Header re-level (flat-h2 doc → proper hierarchy).
- Classification (type/topic from taxonomy).
- Entity/relation/keyword extraction (schema, binary relations, title-case names).
- Chunking (>1MB route vs sub-1MB).
- Fallback (LLM error → raw docling).

## Deliverables

- Fallback path (LLM fail → raw docling).
- review_required surface (log + Uploads).
- Test suite for S1.

## Acceptance (see frontmatter)
