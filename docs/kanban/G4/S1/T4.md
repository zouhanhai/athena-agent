---
id: t4
title: "G4.S1.T4: pipeline integration (refinement stage in tasks.ts + fold llm_wiki classify + downstream)"
layer: T
parent: G4.S1
owner: eng-director
status: backlog
assignee: ""
started_at: ""
blocked_by: []
acceptance_criteria:
  - "New `refinement` stage in tasks.ts: parsing → refinement → ingesting_lightrag ‖ ingesting_llmwiki"
  - "llm_wiki `classify` folded into refinement (type/topic decided by Athena once)"
  - "Downstream consume Athena output: llm_wiki writes page+frontmatter+index (pure I/O, no LLM); RAG (S2) receives injected chunks/entities/keywords"
  - "DOCLING_OPENROUTER_KEY used by docling VLM (parse_doc.py) — separate from refinement"
  - "Progress/status surface for the refinement stage (Uploads page)"
---

## Worker Workflow (REQUIRED — follow in order)

1. **Git claim-lock (FIRST)**: set `status: in_progress`, `assignee: opencode`,
   `session_id: <your session id>` in the frontmatter, then regenerate the kanban index
   (`cd server && npx tsx scripts/write-index.ts`), and commit claim + index together in ONE commit, push.
2. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/<S>/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`) for the full design + acceptance criteria.
3. **Use codegraph MCP**: `codegraph explore "<area>"` (or `codegraph_explore` tool) before editing.
4. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write a failing test first.
5. **Report progress**: append a row to the Progress Log table at the bottom on each real change.
6. **Commit convention**: feature-level English commits; `codegraph sync` after changes.
7. **Verify + mark done**: tests green → `status: done` + Progress Log update +
   **regenerate kanban index** (`cd server && npx tsx scripts/write-index.ts`) → commit + push.

Full: `docs/kanban/TICKET-WORKFLOW.md`


# G4.S1.T4: pipeline integration

## Task

Wire the refinement step into the ingest pipeline (per G4.S1 Spec: Athena is the single full-doc LLM pass).

## Pipeline change

Current (tasks.ts): `parsing (docling)` → `ingesting_lightrag ‖ ingesting_llmwiki`.
New: `parsing (docling)` → **`refinement (refine_document)`** → `ingesting_lightrag ‖ ingesting_llmwiki`.

## Fold llm_wiki classify

- `llm_wiki.classify` (type+topic) is folded into refinement — Athena decides type/topic once.
- Downstream llm_wiki just writes page + frontmatter + rebuilds index (pure I/O, no LLM).

## Downstream consumption

- **llm_wiki**: writes re-leveled md + frontmatter (type/topic) → wiki dir + index.
- **RAG (G4.S2)**: receives Athena-injected chunks/entities/keywords/topic → embeds + indexes (no LLM).
- Key separation: `DOCLING_OPENROUTER_KEY` (docling VLM) vs `ATHENA` provider (refinement) vs
  `EMBEDDING_OPENROUTER_KEY` (S2 embedding) — independent.

## Progress surface

- Refinement stage status/progress on the Uploads page (like parsing/ingesting stages).

## Deliverables

- `refinement` stage in tasks.ts + coordinator.
- classify folded; downstream consumption wired.
- Uploads progress for refinement.

## Acceptance (see frontmatter)
