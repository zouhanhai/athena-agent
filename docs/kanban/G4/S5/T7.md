---
id: G4.S5.T7
title: "Spec state machine: full lifecycle (backlog → decomposed/已分ticket → in_progress → done → in_review → approved/rejected)"
owner: eng-director
status: done
completed_at: 2026-08-13
layer: T
parent: G4.S5
assignee: opencode
session_id: ses_003f888d4ffe1s8yBqPkxDc3Zv
started_at: 2026-08-13
blocked_by:
  - "G4.S5.T6"
acceptance_criteria:
  - "Spec status is a typed enum with the full lifecycle: backlog, decomposed (已分ticket / tickets created), in_progress, done, in_review, approved, rejected, canceled"
  - "A Spec state machine is enforced: backlog → decomposed → in_progress → done → in_review → approved/rejected (plus rejected → re-decompose/new tickets; canceled terminal)"
  - "Transitions triggered by a plan agent / human reviewer (not auto)"
  - "Existing Spec `active` status is mapped/absorbed (e.g. `active` ≡ `in_progress` for backward compatibility) so current Specs still parse"
  - "status-map: kanbanSpecStatusToProjectStatus covers the new states (backlog/decomposed/in_progress → Backlog/In Progress, done → Done, in_review → In Review, approved → Approved, rejected/canceled → a sensible column)"
  - "The Project board Status field gains the new Spec columns (ensureStatusFieldOptions) so Spec cards can move to them"
  - "Schema validation rejects an unknown Spec status; state-machine canTransition applies to Specs"
  - "Server tests green, typecheck clean; live-verified on 6900XT"
---

# G4.S5.T7 — Spec state machine (full lifecycle matching tickets)

> **Superseded by G4.S6.T2 (2026-08-14):** the `decomposed` state was removed and the Spec lifecycle is
> now **ticket-driven** — `backlog → in_progress` happens automatically when the Spec's FIRST ticket is
> claimed (no manual decomposed step), and the Spec never auto-advances to done (review-gated). The spec
> main issue's open/closed is synced to the Spec status (done/approved → closed). See `docs/kanban/G4/S6/T2.md`.

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S5/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`). Code: `server/src/kanban/schema.ts`, `state-machine.ts`,
   `status-map.ts`, `github-sync.ts`, `lifecycle.ts`.
2. **Use codegraph MCP**: `codegraph explore "spec state machine"` before editing.
3. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
4. **Milestone report**: add a semantic Progress Log row when a milestone is complete — not on every
   change. (REAL timestamps, do NOT fabricate.)
5. **Commit convention**: feature-level English commits; `codegraph sync` after.
6. **Verify + mark done**: tests green → set `status: done` → commit + push.

## Context

The ticket state machine (state-machine.ts) is `backlog → in_progress → done → in_review →
approved/rejected`. Specs currently use a loose string status (`backlog/active/in_progress/done`) with
no enforced state machine — a Spec is only moved by hand and had no review/approval stages. The user
(2026-08-13) wants Specs to have a FULL lifecycle that also captures the planning phase:

- **backlog** — Spec defined, tickets NOT yet decomposed
- **decomposed** (已分 ticket) — tickets have been decomposed/created
- **in_progress** — tickets executing
- **done** — tickets complete
- **in_review** — acceptance review
- **approved** / **rejected** — review outcome (rejected → re-decompose/new tickets)

Transitions are triggered by a plan agent / human reviewer (like a ticket's acceptance flow), not auto.

## Task

1. **Typed Spec status** (`server/src/kanban/schema.ts`):
   - Add `SPEC_STATUSES = ["backlog","decomposed","in_progress","done","in_review","approved","rejected","canceled"]`
     + `export type SpecStatus`.
   - `SpecFrontmatter.status: SpecStatus` (typed), validated against SPEC_STATUSES.
   - Backward compat: treat existing `active` as `in_progress` (map `active` → `in_progress` on parse,
     or accept it as an alias) so current Specs parse.

2. **Spec state machine** (`server/src/kanban/state-machine.ts`):
   - Add a Spec state machine: `backlog → decomposed → in_progress → done → in_review →
     approved/rejected`; rejected → (re-decompose into new tickets / new backlog); canceled terminal.
   - `canTransition`/`transitionsFrom`/`transitionsTo`/`transitionId` work for Specs.
   - Transitions are triggered by a plan agent / human reviewer (not auto from ticket completion).

3. **status-map** (`server/src/kanban/status-map.ts`):
   - Extend `KANBAN_SPEC_STATUS_TO_PROJECT_STATUS`: `backlog/decomposed/in_progress → "Backlog"/"In
     Progress"`, `done → "Done"`, `in_review → "In Review"`, `approved → "Approved"`,
     `rejected/canceled → "Rejected"` (or sensible columns).
   - `ensureStatusFieldOptions` in github-sync must add these columns to the Project board Status field.

4. **Tests**:
   - schema test: Spec status validated against SPEC_STATUSES; `active` alias accepted.
   - state-machine test: Spec transitions (backlog→in_progress→done→in_review→approved; in_review→rejected).
   - status-map test: new Spec states map to Project columns.
   - github-sync test: ensureStatusFieldOptions adds the new Spec columns.
   - Server tests green, typecheck clean.

## Acceptance

- Spec status is a typed enum with full lifecycle.
- Spec state machine enforced; plan agent / reviewer trigger transitions.
- Existing `active` Specs still parse (mapped to in_progress).
- Project board has the new Spec columns; Spec cards move correctly.
- Tests green, typecheck clean, live-verified on 6900XT.

## Notes

- Depends on T6 (Spec-card board). Related to T7 (frontend detail panel). Verify on 6900XT.

## Progress Log
| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-13T16:59:17.418Z | in_progress | ran edit |
| 2026-08-13T16:58:44.808Z | in_progress | ran bash |
| 2026-08-13T16:58:13.202Z | in_progress | ran bash |
| 2026-08-13T16:57:28.879Z | in_progress | ran bash |
| 2026-08-13T16:56:37.590Z | in_progress | ran bash |
| 2026-08-13T16:56:04.883Z | in_progress | ran edit |
| 2026-08-13T16:55:34.642Z | in_progress | ran edit |
| 2026-08-13T16:55:03.927Z | in_progress | ran bash |
| 2026-08-13T16:54:30.188Z | in_progress | ran edit |
| 2026-08-13T16:53:56.454Z | in_progress | ran read |
| 2026-08-13T16:53:01.749Z | in_progress | ran grep |
| 2026-08-13T16:51:44.302Z | in_progress | ran grep |
| 2026-08-13T16:51:03.048Z | in_progress | ran read |
| 2026-08-13T16:49:56.518Z | in_progress | ran read |
| 2026-08-13T16:49:54.285Z | in_progress | opencode claimed G4.S5.T7 (session ses_003f888d4ffe1s8yBqPkxDc3Zv) |
| 2026-08-13T16:57:10.000Z | in_progress | Schema + state machine milestone: typed SpecStatus (SPEC_STATUSES with decomposed) with active→in_progress backward-compat alias in parseSpec; SPEC_STATE_MACHINE (backlog→decomposed→in_progress→done→in_review→approved/rejected, rejected→re-decompose, canceled terminal) + kind-dispatched canTransition/transitionsFrom/transitionsTo/transitionId/actorFor; specTransitionId + SPEC_TRANSITION_ACTOR (eng-director plan agent / reviewer) |
| 2026-08-13T16:58:40.000Z | in_progress | Status-map + board columns milestone: KANBAN_SPEC_STATUS_TO_PROJECT_STATUS covers all 8 Spec states (rejected/canceled→Rejected); KANBAN_SPEC_STATUS_OPTIONS + statusFieldOptions() push the Spec columns via ensureStatusFieldOptions in createSpecIssue + sync CLI; buildSpec now emits backlog; index.ts exports wired |
| 2026-08-13T16:59:05.000Z | done | Acceptance met: Server 1035/1035 tests green + typecheck 0 (new schema/state-machine/status-map/github-sync spec tests; legacy active→in_progress). Real board parses with zero errors (21 specs incl. 8 active). codegraph sync + feature commit; T7 done |

## Log

[2026-08-13] opencode claimed G4.S5.T7 (session ses_003f888d4ffe1s8yBqPkxDc3Zv)
[2026-08-13] opencode completed G4.S5.T7 (status done)
