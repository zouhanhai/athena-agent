---
id: G4.S5.T10
title: "Ticket sub-issue title includes the description (`G4.S5.T1 <title>` like Spec) + OpenCode plugin auto-syncs md→GitHub on worker done"
owner: eng-director
status: done
layer: T
parent: G4.S5
assignee: opencode
session_id: ses_003a15ac4ffenU9THEmWWz0k4j
started_at: 2026-08-13
blocked_by:
  - "G4.S5.T9"
acceptance_criteria:
  - "Ticket sub-issue titles are `G4.S5.T1 <ticket title>` (ref + stripped title), matching how Spec main-issue titles are built — so the GitHub board card shows a readable description, not just the ref"
  - "Existing ticket sub-issues (created by earlier syncs with title = bare ref) have their titles updated on the next sync (idempotent, by-title resolution + update)"
  - "When a worker sets a ticket `status: done` (session.idle event), the plugin ALSO runs the md→GitHub sync for the ticket's parent Spec so the GitHub board Status column updates automatically (no manual `sync-github sync` needed)"
  - "The plugin derives the parent specRef from the ticket ref (G4.S5.T9 → G4.S5), resolves a GitHub credential, and runs the sync best-effort (a sync failure is logged but does NOT block or roll back the index done commit)"
  - "Spec main-issue title behavior unchanged (already `G4.S5 <title>`)"
  - "Plugin tests green (mock sync invocation), server tests green, typecheck/vue-tsc clean; live-verified on 6900XT"
---

# G4.S5.T10 — Ticket title shows description + plugin auto-sync on done

## Worker Workflow (REQUIRED — follow in order)

1. **Find context**: read this ticket's parent Spec (`docs/kanban/G4/S5/Spec.md`) + Goal
   (`docs/kanban/G4/Goal.md`). Code: `server/src/kanban/github-sync.ts` (`buildIssueForTicket`),
   `opencode-plugin/src/done-commit.ts`, `opencode-plugin/src/athena-worker.ts`.
2. **Use codegraph MCP**: `codegraph explore "buildIssueForTicket done commit plugin sync"` before editing.
3. **Use `implement` + `tdd` skills**: TDD (RED-GREEN-REFACTOR), write failing test first.
4. **Milestone report**: add a semantic Progress Log row when a milestone is complete — not on every
   change. (REAL timestamps, do NOT fabricate.)
5. **Commit convention**: feature-level English commits; `codegraph sync` after.
6. **Verify + mark done**: tests green → set `status: done` → commit + push.

## Context

Two related S5 finishing items (2026-08-13):

1. **Ticket card title**: on the live GitHub board a Spec card shows `G4.S5 Kanban ↔ GitHub Issues
   bidirectional sync + planning feedback loop` (title), but a ticket card shows only `G4.S5.T1` (just
   the ref). `buildIssueForTicket` sets `title = ticketRef` (bare ref); the Spec main issue uses
   `specIssueTitle` = `ref + stripped title`. Make tickets consistent: `ticketRef + stripped title`.

2. **Plugin auto-sync**: the md→GitHub sync is manual (`sync-github.ts sync <specRef>`); worker done
   leaves the GitHub board Status column stale until someone runs it (observed: T9 was done in md but
   still In Progress on GitHub). The S4 plugin already hooks `session.idle` to commit the kanban index
   on done — extend it to ALSO run the md→GitHub sync for the ticket's parent Spec.

## Task

1. **`buildIssueForTicket`** (`server/src/kanban/github-sync.ts`):
   - Change the title to `${ticketRef} ${stripRefPrefix(ticket.title, ticketRef)}` (strip a leading
     `G4.S5.T1:` prefix from the ticket title if present, like `stripRefPrefix` does for specs).
   - The body stays the same (status/assignee/blocked_by + description + board-file link).
   - `createSpecIssue` updates existing ticket sub-issues by title (it already resolves by title via
     `getIssueByTitle`); ensure idempotent updates still work.

2. **Plugin auto-sync** (`opencode-plugin/src/done-commit.ts` + `athena-worker.ts`):
   - In the `session.idle` done handler (where the index done commit happens), after the commit, ALSO
     run the md→GitHub sync for the ticket's parent Spec: derive `specRef` from the ticket ref
     (`G4.S5.T9` → `G4.S5`), resolve a GitHub credential (athena credential store / configured token /
     env), then run the sync (reuse the `sync-github.ts sync <specRef>` logic or a function from the
     athena core the plugin already imports).
   - Best-effort: a sync failure is logged but does NOT block or roll back the index done commit.

3. **Tests**:
   - Server (`server/test/kanban-github-sync.test.ts`): assert `buildIssueForTicket` returns
     `{ title: "G4.S5.T1 <ticket title>", body }`; `stripRefPrefix` strips a `G4.S5.T1:` prefix.
   - Plugin (`opencode-plugin/test/done-commit.test.ts`): mock the sync invocation; assert that when a
     ticket is done, the plugin triggers `sync <specRef>`; a sync failure still allows the index commit.
   - Server + plugin tests green, typecheck clean.

## Acceptance

- Ticket sub-issue title = `ref + stripped ticket title`; existing tickets updated on next sync.
- Worker done → plugin auto-runs md→GitHub sync for the parent Spec; GitHub board Status column updates
  automatically.
- Sync failure is best-effort (doesn't block done flow).
- Spec title behavior unchanged.
- Tests green, typecheck clean, live-verified on 6900XT.

## Notes

- Depends on T9. Verify on 6900XT (authoritative). After done, re-run the sync CLI so existing ticket
  sub-issues get their new titles.

## Progress Log
| UTC timestamp | status | progress |
|---|---|---|
| 2026-08-13T18:41:27.338Z | in_progress | ran bash |
| 2026-08-13T18:39:52.605Z | in_progress | ran bash |
| 2026-08-13T18:39:02.627Z | in_progress | ran bash |
| 2026-08-13T18:38:41.877Z | done | G4.S5.T10 complete: ticket sub-issue title = ref + stripped title (buildIssueForTicket + idempotent bare-ref fallback in createSpecIssue/sync CLI); plugin auto-sync on done (auto-sync.ts + session.idle); server 1044/1044, plugin 27/27, typechecks clean; live-verified on 6900XT (existing sub-issues re-titled, T10 sub-issue #11 created via plugin sync path) |
| 2026-08-13T18:38:28.688Z | in_progress | ran bash |
| 2026-08-13T18:37:32.438Z | in_progress | ran bash |
| 2026-08-13T18:37:02.425Z | in_progress | ran bash |
| 2026-08-13T18:35:51.687Z | in_progress | ran bash |
| 2026-08-13T18:35:12.162Z | in_progress | ran bash |
| 2026-08-13T18:34:38.797Z | in_progress | ran bash |
| 2026-08-13T18:33:11.825Z | in_progress | ran bash |
| 2026-08-13T18:32:39.997Z | in_progress | ran bash |
| 2026-08-13T18:31:57.792Z | in_progress | ran bash |
| 2026-08-13T18:31:27.146Z | in_progress | ran edit |
| 2026-08-13T18:30:54.007Z | in_progress | ran write |
| 2026-08-13T18:30:15.429Z | in_progress | ran bash |
| 2026-08-13T18:29:44.577Z | in_progress | ran bash |
| 2026-08-13T18:28:17.353Z | in_progress | ran bash |
| 2026-08-13T18:27:46.944Z | in_progress | ran skill |
| 2026-08-13T18:27:16.164Z | in_progress | ran bash |
| 2026-08-13T18:26:30.838Z | in_progress | ran bash |
| 2026-08-13T18:25:11.353Z | in_progress | ran read |
| 2026-08-13T18:25:09.346Z | in_progress | opencode claimed G4.S5.T10 (session ses_003a15ac4ffenU9THEmWWz0k4j) |

## Log

[2026-08-13] opencode claimed G4.S5.T10 (session ses_003a15ac4ffenU9THEmWWz0k4j)
